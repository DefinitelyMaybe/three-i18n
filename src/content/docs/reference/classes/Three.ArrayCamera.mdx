---
editUrl: false
next: false
prev: false
title: "ArrayCamera"
---

Defined in: three/src/cameras/ArrayCamera.js:14

This type of camera can be used in order to efficiently render a scene with a
predefined set of cameras. This is an important performance aspect for
rendering VR scenes.

An instance of `ArrayCamera` always has an array of sub cameras. It's mandatory
to define for each sub camera the `viewport` property which determines the
part of the viewport that is rendered with this camera.

## Extends

- [`PerspectiveCamera`](Three.PerspectiveCamera.html)

## Constructors

### Constructor

> **new ArrayCamera**(`array`?): `ArrayCamera`

Defined in: three/src/cameras/ArrayCamera.js:21

Constructs a new array camera.

#### Parameters

##### array?

[`PerspectiveCamera`](Three.PerspectiveCamera.html)[] = `[]`

An array of perspective sub cameras.

#### Returns

`ArrayCamera`

#### Overrides

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`constructor`](Three.PerspectiveCamera.html#constructor)

## Properties

### \_listeners

> **\_listeners**: `object`

Defined in: three/src/core/EventDispatcher.js:33

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`_listeners`](Three.PerspectiveCamera.html#_listeners)

***

### animations

> **animations**: `AnimationClip`[]

Defined in: three/src/core/Object3D.js:331

An array holding the animation clips of the 3D object.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`animations`](Three.PerspectiveCamera.html#animations)

***

### aspect

> **aspect**: `number`

Defined in: three/src/cameras/PerspectiveCamera.js:102

The aspect ratio, usually the canvas width / canvas height.

#### Default

```ts
1
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`aspect`](Three.PerspectiveCamera.html#aspect)

***

### cameras

> **cameras**: [`PerspectiveCamera`](Three.PerspectiveCamera.html)[]

Defined in: three/src/cameras/ArrayCamera.js:39

An array of perspective sub cameras.

***

### castShadow

> **castShadow**: `boolean`

Defined in: three/src/core/Object3D.js:296

When set to `true`, the 3D object gets rendered into shadow maps.

#### Default

```ts
false
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`castShadow`](Three.PerspectiveCamera.html#castshadow)

***

### children

> **children**: [`Object3D`](Three.Object3D.html)[]

Defined in: three/src/core/Object3D.js:127

An array holding the child 3D objects of this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`children`](Three.PerspectiveCamera.html#children)

***

### coordinateSystem

> **coordinateSystem**: `any`

Defined in: three/src/cameras/Camera.js:58

The coordinate system in which the camera is used.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`coordinateSystem`](Three.PerspectiveCamera.html#coordinatesystem)

***

### customDepthMaterial

> **customDepthMaterial**: `any`

Defined in: three/src/core/Object3D.js:344

Custom depth material to be used when rendering to the depth map. Can only be used
in context of meshes. When shadow-casting with a [DirectionalLight](Three.DirectionalLight.html) or [SpotLight](Three.SpotLight.html),
if you are modifying vertex positions in the vertex shader you must specify a custom depth
material for proper shadows.

Only relevant in context of [WebGLRenderer](Three.WebGLRenderer.html).

#### Default

```ts
undefined
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`customDepthMaterial`](Three.PerspectiveCamera.html#customdepthmaterial)

***

### customDistanceMaterial

> **customDistanceMaterial**: `any`

Defined in: three/src/core/Object3D.js:354

Same as [Object3D#customDepthMaterial](Three.Object3D.html#customdepthmaterial), but used with [PointLight](Three.PointLight.html).

Only relevant in context of [WebGLRenderer](Three.WebGLRenderer.html).

#### Default

```ts
undefined
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`customDistanceMaterial`](Three.PerspectiveCamera.html#customdistancematerial)

***

### far

> **far**: `number`

Defined in: three/src/cameras/PerspectiveCamera.js:84

The camera's far plane. Must be greater than the
current value of [PerspectiveCamera#near](Three.PerspectiveCamera.html#near).

#### Default

```ts
2000
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`far`](Three.PerspectiveCamera.html#far)

***

### filmGauge

> **filmGauge**: `number`

Defined in: three/src/cameras/PerspectiveCamera.js:121

Film size used for the larger axis. Default is `35` (millimeters). This
parameter does not influence the projection matrix unless [PerspectiveCamera#filmOffset](Three.PerspectiveCamera.html#filmoffset)
is set to a nonzero value.

#### Default

```ts
35
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`filmGauge`](Three.PerspectiveCamera.html#filmgauge)

***

### filmOffset

> **filmOffset**: `number`

Defined in: three/src/cameras/PerspectiveCamera.js:129

Horizontal off-center offset in the same unit as [PerspectiveCamera#filmGauge](Three.PerspectiveCamera.html#filmgauge).

#### Default

```ts
0
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`filmOffset`](Three.PerspectiveCamera.html#filmoffset)

***

### focus

> **focus**: `number`

Defined in: three/src/cameras/PerspectiveCamera.js:94

Object distance used for stereoscopy and depth-of-field effects. This
parameter does not influence the projection matrix unless a
[StereoCamera](Three.StereoCamera.html) is being used.

#### Default

```ts
10
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`focus`](Three.PerspectiveCamera.html#focus)

***

### fov

> **fov**: `number`

Defined in: three/src/cameras/PerspectiveCamera.js:55

The vertical field of view, from bottom to top of view,
in degrees.

#### Default

```ts
50
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`fov`](Three.PerspectiveCamera.html#fov)

***

### frustumCulled

> **frustumCulled**: `boolean`

Defined in: three/src/core/Object3D.js:312

When set to `true`, the 3D object is honored by view frustum culling.

#### Default

```ts
true
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`frustumCulled`](Three.PerspectiveCamera.html#frustumculled)

***

### index

> **index**: `number`

Defined in: three/src/cameras/ArrayCamera.js:40

***

### isArrayCamera

> `readonly` **isArrayCamera**: `boolean`

Defined in: three/src/cameras/ArrayCamera.js:32

This flag can be used for type testing.

#### Default

```ts
true
```

***

### isCamera

> `readonly` **isCamera**: `boolean`

Defined in: three/src/cameras/Camera.js:28

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`isCamera`](Three.PerspectiveCamera.html#iscamera)

***

### isObject3D

> `readonly` **isObject3D**: `boolean`

Defined in: three/src/core/Object3D.js:79

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`isObject3D`](Three.PerspectiveCamera.html#isobject3d)

***

### isPerspectiveCamera

> `readonly` **isPerspectiveCamera**: `boolean`

Defined in: three/src/cameras/PerspectiveCamera.js:44

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`isPerspectiveCamera`](Three.PerspectiveCamera.html#isperspectivecamera)

***

### layers

> **layers**: [`Layers`](Three.Layers.html)

Defined in: three/src/core/Object3D.js:280

The layer membership of the 3D object. The 3D object is only visible if it has
at least one layer in common with the camera in use. This property can also be
used to filter out unwanted objects in ray-intersection tests when using [Raycaster](Three.Raycaster.html).

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`layers`](Three.PerspectiveCamera.html#layers)

***

### matrix

> **matrix**: [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/core/Object3D.js:232

Represents the object's transformation matrix in local space.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`matrix`](Three.PerspectiveCamera.html#matrix)

***

### matrixAutoUpdate

> **matrixAutoUpdate**: `boolean`

Defined in: three/src/core/Object3D.js:251

When set to `true`, the engine automatically computes the local matrix from position,
rotation and scale every frame.

The default values for all 3D objects is defined by `Object3D.DEFAULT_MATRIX_AUTO_UPDATE`.

#### Default

```ts
true
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`matrixAutoUpdate`](Three.PerspectiveCamera.html#matrixautoupdate)

***

### matrixWorld

> **matrixWorld**: [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/core/Object3D.js:240

Represents the object's transformation matrix in world space.
If the 3D object has no parent, then it's identical to the local transformation matrix

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`matrixWorld`](Three.PerspectiveCamera.html#matrixworld)

***

### matrixWorldAutoUpdate

> **matrixWorldAutoUpdate**: `boolean`

Defined in: three/src/core/Object3D.js:262

When set to `true`, the engine automatically computes the world matrix from the current local
matrix and the object's transformation hierarchy.

The default values for all 3D objects is defined by `Object3D.DEFAULT_MATRIX_WORLD_AUTO_UPDATE`.

#### Default

```ts
true
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`matrixWorldAutoUpdate`](Three.PerspectiveCamera.html#matrixworldautoupdate)

***

### matrixWorldInverse

> **matrixWorldInverse**: [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/cameras/Camera.js:37

The inverse of the camera's world matrix.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`matrixWorldInverse`](Three.PerspectiveCamera.html#matrixworldinverse)

***

### matrixWorldNeedsUpdate

> **matrixWorldNeedsUpdate**: `boolean`

Defined in: three/src/core/Object3D.js:271

When set to `true`, it calculates the world matrix in that frame and resets this property
to `false`.

#### Default

```ts
false
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`matrixWorldNeedsUpdate`](Three.PerspectiveCamera.html#matrixworldneedsupdate)

***

### name

> **name**: `string`

Defined in: three/src/core/Object3D.js:103

The name of the 3D object.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`name`](Three.PerspectiveCamera.html#name)

***

### near

> **near**: `number`

Defined in: three/src/cameras/PerspectiveCamera.js:75

The camera's near plane. The valid range is greater than `0`
and less than the current value of [PerspectiveCamera#far](Three.PerspectiveCamera.html#far).

Note that, unlike for the [OrthographicCamera](Three.OrthographicCamera.html), `0` is <em>not</em> a
valid value for a perspective camera's near plane.

#### Default

```ts
0.1
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`near`](Three.PerspectiveCamera.html#near)

***

### parent

> **parent**: [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:120

A reference to the parent object.

#### Default

```ts
null
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`parent`](Three.PerspectiveCamera.html#parent)

***

### projectionMatrix

> **projectionMatrix**: [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/cameras/Camera.js:44

The camera's projection matrix.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`projectionMatrix`](Three.PerspectiveCamera.html#projectionmatrix)

***

### projectionMatrixInverse

> **projectionMatrixInverse**: [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/cameras/Camera.js:51

The inverse of the camera's projection matrix.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`projectionMatrixInverse`](Three.PerspectiveCamera.html#projectionmatrixinverse)

***

### receiveShadow

> **receiveShadow**: `boolean`

Defined in: three/src/core/Object3D.js:304

When set to `true`, the 3D object is affected by shadows in the scene.

#### Default

```ts
false
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`receiveShadow`](Three.PerspectiveCamera.html#receiveshadow)

***

### renderOrder

> **renderOrder**: `number`

Defined in: three/src/core/Object3D.js:324

This value allows the default rendering order of scene graph objects to be
overridden although opaque and transparent objects remain sorted independently.
When this property is set for an instance of [Group](Three.Group.html),all descendants
objects will be sorted and rendered together. Sorting is from lowest to highest
render order.

#### Default

```ts
0
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`renderOrder`](Three.PerspectiveCamera.html#renderorder)

***

### type

> **type**: `string`

Defined in: three/src/cameras/PerspectiveCamera.js:46

The type property is used for detecting the object type
in context of serialization/deserialization.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`type`](Three.PerspectiveCamera.html#type)

***

### up

> **up**: [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:137

Defines the `up` direction of the 3D object which influences
the orientation via methods like [Object3D#lookAt](Three.Object3D.html#lookat).

The default values for all 3D objects is defined by `Object3D.DEFAULT_UP`.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`up`](Three.PerspectiveCamera.html#up)

***

### userData

> **userData**: `any`

Defined in: three/src/core/Object3D.js:362

An object that can be used to store custom data about the 3D object. It
should not hold references to functions as these will not be cloned.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`userData`](Three.PerspectiveCamera.html#userdata)

***

### uuid

> `readonly` **uuid**: `string`

Defined in: three/src/core/Object3D.js:96

The UUID of the 3D object.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`uuid`](Three.PerspectiveCamera.html#uuid)

***

### view

> **view**: `any`

Defined in: three/src/cameras/PerspectiveCamera.js:111

Represents the frustum window specification. This property should not be edited
directly but via [PerspectiveCamera#setViewOffset](Three.PerspectiveCamera.html#setviewoffset) and [PerspectiveCamera#clearViewOffset](Three.PerspectiveCamera.html#clearviewoffset).

#### Default

```ts
null
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`view`](Three.PerspectiveCamera.html#view)

***

### visible

> **visible**: `boolean`

Defined in: three/src/core/Object3D.js:288

When set to `true`, the 3D object gets rendered.

#### Default

```ts
true
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`visible`](Three.PerspectiveCamera.html#visible)

***

### zoom

> **zoom**: `number`

Defined in: three/src/cameras/PerspectiveCamera.js:63

The zoom factor of the camera.

#### Default

```ts
1
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`zoom`](Three.PerspectiveCamera.html#zoom)

***

### DEFAULT\_MATRIX\_AUTO\_UPDATE

> `static` **DEFAULT\_MATRIX\_AUTO\_UPDATE**: `boolean`

Defined in: three/src/core/Object3D.js:1605

The default setting for [Object3D#matrixAutoUpdate](Three.Object3D.html#matrixautoupdate) for
newly created 3D objects.

#### Static

#### Default

```ts
true
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`DEFAULT_MATRIX_AUTO_UPDATE`](Three.PerspectiveCamera.html#default_matrix_auto_update)

***

### DEFAULT\_MATRIX\_WORLD\_AUTO\_UPDATE

> `static` **DEFAULT\_MATRIX\_WORLD\_AUTO\_UPDATE**: `boolean`

Defined in: three/src/core/Object3D.js:1615

The default setting for [Object3D#matrixWorldAutoUpdate](Three.Object3D.html#matrixworldautoupdate) for
newly created 3D objects.

#### Static

#### Default

```ts
true
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`DEFAULT_MATRIX_WORLD_AUTO_UPDATE`](Three.PerspectiveCamera.html#default_matrix_world_auto_update)

***

### DEFAULT\_UP

> `static` **DEFAULT\_UP**: [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:1595

The default up direction for objects, also used as the default
position for [DirectionalLight](Three.DirectionalLight.html) and [HemisphereLight](Three.HemisphereLight.html).

#### Static

#### Default

```ts
(0,1,0)
```

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`DEFAULT_UP`](Three.PerspectiveCamera.html#default_up)

## Methods

### add()

> **add**(`object`, ...`args`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:720

Adds the given 3D object as a child to this 3D object. An arbitrary number of
objects may be added. Any current parent on an object passed in here will be
removed, since an object can have at most one parent.

#### Parameters

##### object

[`Object3D`](Three.Object3D.html)

The 3D object to add.

##### args

...`any`

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#added

#### Fires

Object3D#childadded

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`add`](Three.PerspectiveCamera.html#add)

***

### addEventListener()

> **addEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:31

Adds the given event listener to the given event type.

#### Parameters

##### type

`string`

The type of event to listen to.

##### listener

`Function`

The function that gets called when the event is fired.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`addEventListener`](Three.PerspectiveCamera.html#addeventlistener)

***

### applyMatrix4()

> **applyMatrix4**(`matrix`): `void`

Defined in: three/src/core/Object3D.js:422

Applies the given transformation matrix to the object and updates the object's position,
rotation and scale.

#### Parameters

##### matrix

[`Matrix4`](Three.Matrix4.html)

The transformation matrix.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`applyMatrix4`](Three.PerspectiveCamera.html#applymatrix4)

***

### applyQuaternion()

> **applyQuaternion**(`q`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:438

Applies a rotation represented by given the quaternion to the 3D object.

#### Parameters

##### q

[`Quaternion`](Three.Quaternion.html)

The quaternion.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`applyQuaternion`](Three.PerspectiveCamera.html#applyquaternion)

***

### attach()

> **attach**(`object`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:848

Adds the given 3D object as a child of this 3D object, while maintaining the object's world
transform. This method does not support scene graphs having non-uniformly-scaled nodes(s).

#### Parameters

##### object

[`Object3D`](Three.Object3D.html)

The 3D object to attach.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#added

#### Fires

Object3D#childadded

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`attach`](Three.PerspectiveCamera.html#attach)

***

### clear()

> **clear**(): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:833

Removes all child objects.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#removed

#### Fires

Object3D#childremoved

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`clear`](Three.PerspectiveCamera.html#clear)

***

### clearViewOffset()

> **clearViewOffset**(): `void`

Defined in: three/src/cameras/PerspectiveCamera.js:337

Removes the view offset from the projection matrix.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`clearViewOffset`](Three.PerspectiveCamera.html#clearviewoffset)

***

### clone()

> **clone**(): `any`

Defined in: three/src/cameras/Camera.js:108

Returns a new 3D object with copied values from this instance.

#### Returns

`any`

A clone of this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`clone`](Three.PerspectiveCamera.html#clone)

***

### copy()

> **copy**(`source`, `recursive`): `ArrayCamera`

Defined in: three/src/cameras/PerspectiveCamera.js:135

Copies the values of the given 3D object to this instance.

#### Parameters

##### source

`any`

The 3D object to copy.

##### recursive

`any`

When set to `true`, descendants of the 3D object are cloned.

#### Returns

`ArrayCamera`

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`copy`](Three.PerspectiveCamera.html#copy)

***

### dispatchEvent()

> **dispatchEvent**(`event`): `void`

Defined in: three/src/core/EventDispatcher.js:101

Dispatches an event object.

#### Parameters

##### event

`any`

The event that gets fired.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`dispatchEvent`](Three.PerspectiveCamera.html#dispatchevent)

***

### getEffectiveFOV()

> **getEffectiveFOV**(): `number`

Defined in: three/src/cameras/PerspectiveCamera.js:193

Returns the current vertical field of view angle in degrees considering [PerspectiveCamera#zoom](Three.PerspectiveCamera.html#zoom).

#### Returns

`number`

The effective FOV.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getEffectiveFOV`](Three.PerspectiveCamera.html#geteffectivefov)

***

### getFilmHeight()

> **getFilmHeight**(): `number`

Defined in: three/src/cameras/PerspectiveCamera.js:219

Returns the height of the image on the film. If [PerspectiveCamera#aspect](Three.PerspectiveCamera.html#aspect) is greater than or
equal to one (landscape format), the result equals [PerspectiveCamera#filmGauge](Three.PerspectiveCamera.html#filmgauge).

#### Returns

`number`

The film width.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getFilmHeight`](Three.PerspectiveCamera.html#getfilmheight)

***

### getFilmWidth()

> **getFilmWidth**(): `number`

Defined in: three/src/cameras/PerspectiveCamera.js:206

Returns the width of the image on the film. If [PerspectiveCamera#aspect](Three.PerspectiveCamera.html#aspect) is greater than or
equal to one (landscape format), the result equals [PerspectiveCamera#filmGauge](Three.PerspectiveCamera.html#filmgauge).

#### Returns

`number`

The film width.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getFilmWidth`](Three.PerspectiveCamera.html#getfilmwidth)

***

### getFocalLength()

> **getFocalLength**(): `number`

Defined in: three/src/cameras/PerspectiveCamera.js:180

Returns the focal length from the current [PerspectiveCamera#fov](Three.PerspectiveCamera.html#fov) and
[PerspectiveCamera#filmGauge](Three.PerspectiveCamera.html#filmgauge).

#### Returns

`number`

The computed focal length.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getFocalLength`](Three.PerspectiveCamera.html#getfocallength)

***

### getObjectById()

> **getObjectById**(`id`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:891

Searches through the 3D object and its children, starting with the 3D object
itself, and returns the first with a matching ID.

#### Parameters

##### id

`number`

The id.

#### Returns

[`Object3D`](Three.Object3D.html)

The found 3D object. Returns `undefined` if no 3D object has been found.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getObjectById`](Three.PerspectiveCamera.html#getobjectbyid)

***

### getObjectByName()

> **getObjectByName**(`name`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:904

Searches through the 3D object and its children, starting with the 3D object
itself, and returns the first with a matching name.

#### Parameters

##### name

`string`

The name.

#### Returns

[`Object3D`](Three.Object3D.html)

The found 3D object. Returns `undefined` if no 3D object has been found.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getObjectByName`](Three.PerspectiveCamera.html#getobjectbyname)

***

### getObjectByProperty()

> **getObjectByProperty**(`name`, `value`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:918

Searches through the 3D object and its children, starting with the 3D object
itself, and returns the first with a matching property value.

#### Parameters

##### name

`string`

The name of the property.

##### value

`any`

The value.

#### Returns

[`Object3D`](Three.Object3D.html)

The found 3D object. Returns `undefined` if no 3D object has been found.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getObjectByProperty`](Three.PerspectiveCamera.html#getobjectbyproperty)

***

### getObjectsByProperty()

> **getObjectsByProperty**(`name`, `value`, `result`): [`Object3D`](Three.Object3D.html)[]

Defined in: three/src/core/Object3D.js:948

Searches through the 3D object and its children, starting with the 3D object
itself, and returns all 3D objects with a matching property value.

#### Parameters

##### name

`string`

The name of the property.

##### value

`any`

The value.

##### result

[`Object3D`](Three.Object3D.html)[] = `[]`

The method stores the result in this array.

#### Returns

[`Object3D`](Three.Object3D.html)[]

The found 3D objects.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getObjectsByProperty`](Three.PerspectiveCamera.html#getobjectsbyproperty)

***

### getViewBounds()

> **getViewBounds**(`distance`, `minTarget`, `maxTarget`): `void`

Defined in: three/src/cameras/PerspectiveCamera.js:234

Computes the 2D bounds of the camera's viewable rectangle at a given distance along the viewing direction.
Sets `minTarget` and `maxTarget` to the coordinates of the lower-left and upper-right corners of the view rectangle.

#### Parameters

##### distance

`number`

The viewing distance.

##### minTarget

[`Vector2`](Three.Vector2.html)

The lower-left corner of the view rectangle is written into this vector.

##### maxTarget

[`Vector2`](Three.Vector2.html)

The upper-right corner of the view rectangle is written into this vector.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getViewBounds`](Three.PerspectiveCamera.html#getviewbounds)

***

### getViewSize()

> **getViewSize**(`distance`, `target`): [`Vector2`](Three.Vector2.html)

Defined in: three/src/cameras/PerspectiveCamera.js:253

Computes the width and height of the camera's viewable rectangle at a given distance along the viewing direction.

#### Parameters

##### distance

`number`

The viewing distance.

##### target

[`Vector2`](Three.Vector2.html)

The target vector that is used to store result where x is width and y is height.

#### Returns

[`Vector2`](Three.Vector2.html)

The view size.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getViewSize`](Three.PerspectiveCamera.html#getviewsize)

***

### getWorldDirection()

> **getWorldDirection**(`target`): `Vector3`

Defined in: three/src/cameras/Camera.js:86

Returns a vector representing the ("look") direction of the 3D object in world space.

This method is overwritten since cameras have a different forward vector compared to other
3D objects. A camera looks down its local, negative z-axis by default.

#### Parameters

##### target

`Vector3`

The target vector the result is stored to.

#### Returns

`Vector3`

The 3D object's direction in world space.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getWorldDirection`](Three.PerspectiveCamera.html#getworlddirection)

***

### getWorldPosition()

> **getWorldPosition**(`target`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:970

Returns a vector representing the position of the 3D object in world space.

#### Parameters

##### target

[`Vector3`](Three.Vector3.html)

The target vector the result is stored to.

#### Returns

[`Vector3`](Three.Vector3.html)

The 3D object's position in world space.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getWorldPosition`](Three.PerspectiveCamera.html#getworldposition)

***

### getWorldQuaternion()

> **getWorldQuaternion**(`target`): [`Quaternion`](Three.Quaternion.html)

Defined in: three/src/core/Object3D.js:984

Returns a Quaternion representing the position of the 3D object in world space.

#### Parameters

##### target

[`Quaternion`](Three.Quaternion.html)

The target Quaternion the result is stored to.

#### Returns

[`Quaternion`](Three.Quaternion.html)

The 3D object's rotation in world space.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getWorldQuaternion`](Three.PerspectiveCamera.html#getworldquaternion)

***

### getWorldScale()

> **getWorldScale**(`target`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:1000

Returns a vector representing the scale of the 3D object in world space.

#### Parameters

##### target

[`Vector3`](Three.Vector3.html)

The target vector the result is stored to.

#### Returns

[`Vector3`](Three.Vector3.html)

The 3D object's scale in world space.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`getWorldScale`](Three.PerspectiveCamera.html#getworldscale)

***

### hasEventListener()

> **hasEventListener**(`type`, `listener`): `boolean`

Defined in: three/src/core/EventDispatcher.js:58

Returns `true` if the given event listener has been added to the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to check.

#### Returns

`boolean`

Whether the given event listener has been added to the given event type.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`hasEventListener`](Three.PerspectiveCamera.html#haseventlistener)

***

### localToWorld()

> **localToWorld**(`vector`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:637

Converts the given vector from this 3D object's local space to world space.

#### Parameters

##### vector

[`Vector3`](Three.Vector3.html)

The vector to convert.

#### Returns

[`Vector3`](Three.Vector3.html)

The converted vector.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`localToWorld`](Three.PerspectiveCamera.html#localtoworld)

***

### lookAt()

> **lookAt**(`x`, `y`?, `z`?): `void`

Defined in: three/src/core/Object3D.js:668

Rotates the object to face a point in world space.

This method does not support objects having non-uniformly-scaled parent(s).

#### Parameters

##### x

The x coordinate in world space. Alternatively, a vector representing a position in world space

`number` | [`Vector3`](Three.Vector3.html)

##### y?

`number`

The y coordinate in world space.

##### z?

`number`

The z coordinate in world space.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`lookAt`](Three.PerspectiveCamera.html#lookat)

***

### onAfterRender()

> **onAfterRender**(): `void`

Defined in: three/src/core/Object3D.js:414

A callback that is executed immediately after a 3D object is rendered.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`onAfterRender`](Three.PerspectiveCamera.html#onafterrender)

***

### onAfterShadow()

> **onAfterShadow**(): `void`

Defined in: three/src/core/Object3D.js:390

A callback that is executed immediately after a 3D object is rendered to a shadow map.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`onAfterShadow`](Three.PerspectiveCamera.html#onaftershadow)

***

### onBeforeRender()

> **onBeforeRender**(): `void`

Defined in: three/src/core/Object3D.js:402

A callback that is executed immediately before a 3D object is rendered.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`onBeforeRender`](Three.PerspectiveCamera.html#onbeforerender)

***

### onBeforeShadow()

> **onBeforeShadow**(): `void`

Defined in: three/src/core/Object3D.js:377

A callback that is executed immediately before a 3D object is rendered to a shadow map.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`onBeforeShadow`](Three.PerspectiveCamera.html#onbeforeshadow)

***

### raycast()

> `abstract` **raycast**(): `void`

Defined in: three/src/core/Object3D.js:1035

Abstract method to get intersections between a casted ray and this
3D object. Renderable 3D objects such as [Mesh](Three.Mesh.html), [Line](Three.Line.html) or [Points](Three.Points.html)
implement this method in order to use raycasting.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`raycast`](Three.PerspectiveCamera.html#raycast)

***

### remove()

> **remove**(`object`, ...`args`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:772

Removes the given 3D object as child from this 3D object.
An arbitrary number of objects may be removed.

#### Parameters

##### object

[`Object3D`](Three.Object3D.html)

The 3D object to remove.

##### args

...`any`

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#removed

#### Fires

Object3D#childremoved

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`remove`](Three.PerspectiveCamera.html#remove)

***

### removeEventListener()

> **removeEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:74

Removes the given event listener from the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to remove.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`removeEventListener`](Three.PerspectiveCamera.html#removeeventlistener)

***

### removeFromParent()

> **removeFromParent**(): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:812

Removes this 3D object from its current parent.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#removed

#### Fires

Object3D#childremoved

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`removeFromParent`](Three.PerspectiveCamera.html#removefromparent)

***

### rotateOnAxis()

> **rotateOnAxis**(`axis`, `angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:505

Rotates the 3D object along an axis in local space.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`rotateOnAxis`](Three.PerspectiveCamera.html#rotateonaxis)

***

### rotateOnWorldAxis()

> **rotateOnWorldAxis**(`axis`, `angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:525

Rotates the 3D object along an axis in world space.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`rotateOnWorldAxis`](Three.PerspectiveCamera.html#rotateonworldaxis)

***

### rotateX()

> **rotateX**(`angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:545

Rotates the 3D object around its X axis in local space.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`rotateX`](Three.PerspectiveCamera.html#rotatex)

***

### rotateY()

> **rotateY**(`angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:557

Rotates the 3D object around its Y axis in local space.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`rotateY`](Three.PerspectiveCamera.html#rotatey)

***

### rotateZ()

> **rotateZ**(`angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:569

Rotates the 3D object around its Z axis in local space.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`rotateZ`](Three.PerspectiveCamera.html#rotatez)

***

### setFocalLength()

> **setFocalLength**(`focalLength`): `void`

Defined in: three/src/cameras/PerspectiveCamera.js:164

Sets the FOV by focal length in respect to the current [PerspectiveCamera#filmGauge](Three.PerspectiveCamera.html#filmgauge).

The default film gauge is 35, so that the focal length can be specified for
a 35mm (full frame) camera.

#### Parameters

##### focalLength

`number`

Values for focal length and film gauge must have the same unit.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`setFocalLength`](Three.PerspectiveCamera.html#setfocallength)

***

### setRotationFromAxisAngle()

> **setRotationFromAxisAngle**(`axis`, `angle`): `void`

Defined in: three/src/core/Object3D.js:452

Sets the given rotation represented as an axis/angle couple to the 3D object.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### angle

`number`

The angle in radians.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`setRotationFromAxisAngle`](Three.PerspectiveCamera.html#setrotationfromaxisangle)

***

### setRotationFromEuler()

> **setRotationFromEuler**(`euler`): `void`

Defined in: three/src/core/Object3D.js:465

Sets the given rotation represented as Euler angles to the 3D object.

#### Parameters

##### euler

[`Euler`](Three.Euler.html)

The Euler angles.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`setRotationFromEuler`](Three.PerspectiveCamera.html#setrotationfromeuler)

***

### setRotationFromMatrix()

> **setRotationFromMatrix**(`m`): `void`

Defined in: three/src/core/Object3D.js:477

Sets the given rotation represented as rotation matrix to the 3D object.

#### Parameters

##### m

[`Matrix4`](Three.Matrix4.html)

Although a 4x4 matrix is expected, the upper 3x3 portion must be
a pure rotation matrix (i.e, unscaled).

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`setRotationFromMatrix`](Three.PerspectiveCamera.html#setrotationfrommatrix)

***

### setRotationFromQuaternion()

> **setRotationFromQuaternion**(`q`): `void`

Defined in: three/src/core/Object3D.js:490

Sets the given rotation represented as a Quaternion to the 3D object.

#### Parameters

##### q

[`Quaternion`](Three.Quaternion.html)

The Quaternion

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`setRotationFromQuaternion`](Three.PerspectiveCamera.html#setrotationfromquaternion)

***

### setViewOffset()

> **setViewOffset**(`fullWidth`, `fullHeight`, `x`, `y`, `width`, `height`): `void`

Defined in: three/src/cameras/PerspectiveCamera.js:304

Sets an offset in a larger frustum. This is useful for multi-window or
multi-monitor/multi-machine setups.

For example, if you have 3x2 monitors and each monitor is 1920x1080 and
the monitors are in grid like this
```
  +---+---+---+
  | A | B | C |
  +---+---+---+
  | D | E | F |
  +---+---+---+
```
then for each monitor you would call it like this:
```js
const w = 1920;
const h = 1080;
const fullWidth = w * 3;
const fullHeight = h * 2;

// --A--
camera.setViewOffset( fullWidth, fullHeight, w * 0, h * 0, w, h );
// --B--
camera.setViewOffset( fullWidth, fullHeight, w * 1, h * 0, w, h );
// --C--
camera.setViewOffset( fullWidth, fullHeight, w * 2, h * 0, w, h );
// --D--
camera.setViewOffset( fullWidth, fullHeight, w * 0, h * 1, w, h );
// --E--
camera.setViewOffset( fullWidth, fullHeight, w * 1, h * 1, w, h );
// --F--
camera.setViewOffset( fullWidth, fullHeight, w * 2, h * 1, w, h );
```

Note there is no reason monitors have to be the same size or in a grid.

#### Parameters

##### fullWidth

`number`

The full width of multiview setup.

##### fullHeight

`number`

The full height of multiview setup.

##### x

`number`

The horizontal offset of the subcamera.

##### y

`number`

The vertical offset of the subcamera.

##### width

`number`

The width of subcamera.

##### height

`number`

The height of subcamera.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`setViewOffset`](Three.PerspectiveCamera.html#setviewoffset)

***

### toJSON()

> **toJSON**(`meta`): `any`

Defined in: three/src/cameras/PerspectiveCamera.js:383

Serializes the 3D object into JSON.

#### Parameters

##### meta

`any`

An optional value holding meta information about the serialization.

#### Returns

`any`

A JSON object representing the serialized 3D object.

#### See

[ObjectLoader#parse](Three.ObjectLoader.html#parse)

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`toJSON`](Three.PerspectiveCamera.html#tojson)

***

### translateOnAxis()

> **translateOnAxis**(`axis`, `distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:582

Translate the 3D object by a distance along the given axis in local space.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`translateOnAxis`](Three.PerspectiveCamera.html#translateonaxis)

***

### translateX()

> **translateX**(`distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:601

Translate the 3D object by a distance along its X-axis in local space.

#### Parameters

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`translateX`](Three.PerspectiveCamera.html#translatex)

***

### translateY()

> **translateY**(`distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:613

Translate the 3D object by a distance along its Y-axis in local space.

#### Parameters

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`translateY`](Three.PerspectiveCamera.html#translatey)

***

### translateZ()

> **translateZ**(`distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:625

Translate the 3D object by a distance along its Z-axis in local space.

#### Parameters

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`translateZ`](Three.PerspectiveCamera.html#translatez)

***

### traverse()

> **traverse**(`callback`): `void`

Defined in: three/src/core/Object3D.js:1044

Executes the callback on this 3D object and all descendants.

Note: Modifying the scene graph inside the callback is discouraged.

#### Parameters

##### callback

`Function`

A callback function that allows to process the current 3D object.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`traverse`](Three.PerspectiveCamera.html#traverse)

***

### traverseAncestors()

> **traverseAncestors**(`callback`): `void`

Defined in: three/src/core/Object3D.js:1089

Like [Object3D#traverse](Three.Object3D.html#traverse), but the callback will only be executed for all ancestors.

Note: Modifying the scene graph inside the callback is discouraged.

#### Parameters

##### callback

`Function`

A callback function that allows to process the current 3D object.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`traverseAncestors`](Three.PerspectiveCamera.html#traverseancestors)

***

### traverseVisible()

> **traverseVisible**(`callback`): `void`

Defined in: three/src/core/Object3D.js:1066

Like [Object3D#traverse](Three.Object3D.html#traverse), but the callback will only be executed for visible 3D objects.
Descendants of invisible 3D objects are not traversed.

Note: Modifying the scene graph inside the callback is discouraged.

#### Parameters

##### callback

`Function`

A callback function that allows to process the current 3D object.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`traverseVisible`](Three.PerspectiveCamera.html#traversevisible)

***

### updateMatrix()

> **updateMatrix**(): `void`

Defined in: three/src/core/Object3D.js:1107

Updates the transformation matrix in local space by computing it from the current
position, rotation and scale values.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`updateMatrix`](Three.PerspectiveCamera.html#updatematrix)

***

### updateMatrixWorld()

> **updateMatrixWorld**(`force`): `void`

Defined in: three/src/cameras/Camera.js:92

Updates the transformation matrix in world space of this 3D objects and its descendants.

To ensure correct results, this method also recomputes the 3D object's transformation matrix in
local space. The computation of the local and world matrix can be controlled with the
[Object3D#matrixAutoUpdate](Three.Object3D.html#matrixautoupdate) and [Object3D#matrixWorldAutoUpdate](Three.Object3D.html#matrixworldautoupdate) flags which are both
`true` by default.  Set these flags to `false` if you need more control over the update matrix process.

#### Parameters

##### force

`any`

When set to `true`, a recomputation of world matrices is forced even
when [Object3D#matrixWorldAutoUpdate](Three.Object3D.html#matrixworldautoupdate) is set to `false`.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`updateMatrixWorld`](Three.PerspectiveCamera.html#updatematrixworld)

***

### updateProjectionMatrix()

> **updateProjectionMatrix**(): `void`

Defined in: three/src/cameras/PerspectiveCamera.js:353

Updates the camera's projection matrix. Must be called after any change of
camera properties.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`updateProjectionMatrix`](Three.PerspectiveCamera.html#updateprojectionmatrix)

***

### updateWorldMatrix()

> **updateWorldMatrix**(`updateParents`, `updateChildren`): `void`

Defined in: three/src/cameras/Camera.js:100

An alternative version of [Object3D#updateMatrixWorld](Three.Object3D.html#updatematrixworld) with more control over the
update of ancestor and descendant nodes.

#### Parameters

##### updateParents

`any`

Whether ancestor nodes should be updated or not.

##### updateChildren

`any`

Whether descendant nodes should be updated or not.

#### Returns

`void`

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`updateWorldMatrix`](Three.PerspectiveCamera.html#updateworldmatrix)

***

### worldToLocal()

> **worldToLocal**(`vector`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:651

Converts the given vector from this 3D object's word space to local space.

#### Parameters

##### vector

[`Vector3`](Three.Vector3.html)

The vector to convert.

#### Returns

[`Vector3`](Three.Vector3.html)

The converted vector.

#### Inherited from

[`PerspectiveCamera`](Three.PerspectiveCamera.html).[`worldToLocal`](Three.PerspectiveCamera.html#worldtolocal)
