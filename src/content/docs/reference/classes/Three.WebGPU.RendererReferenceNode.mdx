---
editUrl: false
next: false
prev: false
title: "RendererReferenceNode"
---

Defined in: three/src/nodes/accessors/RendererReferenceNode.js:16

This node is a special type of reference node which is intended
for linking renderer properties with node values.
```js
const exposureNode = rendererReference( 'toneMappingExposure', 'float', renderer );
```
When changing `renderer.toneMappingExposure`, the node value of `exposureNode` will
automatically be updated.

## Extends

- `ReferenceBaseNode`

## Constructors

### Constructor

> **new RendererReferenceNode**(`property`, `inputType`, `renderer`?): `RendererReferenceNode`

Defined in: three/src/nodes/accessors/RendererReferenceNode.js:32

Constructs a new renderer reference node.

#### Parameters

##### property

`string`

The name of the property the node refers to.

##### inputType

`string`

The uniform type that should be used to represent the property value.

##### renderer?

`Renderer` = `null`

The renderer the property belongs to. When no renderer is set,
the node refers to the renderer of the current state.

#### Returns

`RendererReferenceNode`

#### Overrides

`ReferenceBaseNode.constructor`

## Properties

### \_listeners

> **\_listeners**: `object`

Defined in: three/src/core/EventDispatcher.js:33

#### Inherited from

`ReferenceBaseNode._listeners`

***

### count

> **count**: `number`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:134

When the linked property is an array, this parameter defines its length.

#### Default

```ts
null
```

#### Inherited from

`ReferenceBaseNode.count`

***

### global

> **global**: `boolean`

Defined in: three/src/nodes/core/Node.js:88

Whether this node is global or not. This property is relevant for the internal
node caching system. All nodes which should be declared just once should
set this flag to `true` (a typical example is [AttributeNode](Three.WebGPU.AttributeNode.html)).

#### Default

```ts
false
```

#### Inherited from

`ReferenceBaseNode.global`

***

### group

> **group**: `UniformGroupNode`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:167

The uniform group of the internal uniform.

#### Default

```ts
null
```

#### Inherited from

`ReferenceBaseNode.group`

***

### isNode

> `readonly` **isNode**: `boolean`

Defined in: three/src/nodes/core/Node.js:97

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

`ReferenceBaseNode.isNode`

***

### node

> **node**: `UniformNode`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:159

The uniform node that holds the value of the reference node.

#### Default

```ts
null
```

#### Inherited from

`ReferenceBaseNode.node`

***

### nodeType

> **nodeType**: `string`

Defined in: three/src/nodes/core/Node.js:37

The node type. This represents the result type of the node (e.g. `float` or `vec3`).

#### Default

```ts
null
```

#### Inherited from

`ReferenceBaseNode.nodeType`

***

### object

> **object**: `any`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:126

The object the property belongs to.

#### Default

```ts
null
```

#### Inherited from

`ReferenceBaseNode.object`

***

### properties

> **properties**: `string`[]

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:142

The property name might have dots so nested properties can be referred.
The hierarchy of the names is stored inside this array.

#### Inherited from

`ReferenceBaseNode.properties`

***

### property

> **property**: `string`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:111

The name of the property the node refers to.

#### Inherited from

`ReferenceBaseNode.property`

***

### reference

> **reference**: `any`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:151

Points to the current referred object. This property exists next to [ReferenceNode#object](Three.WebGPU.ReferenceNode.html#object)
since the final reference might be updated from calling code.

#### Default

```ts
null
```

#### Inherited from

`ReferenceBaseNode.reference`

***

### renderer

> **renderer**: `Renderer`

Defined in: three/src/nodes/accessors/RendererReferenceNode.js:43

The renderer the property belongs to. When no renderer is set,
the node refers to the renderer of the current state.

#### Default

```ts
null
```

***

### uniformType

> **uniformType**: `string`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:118

The uniform type that should be used to represent the property value.

#### Inherited from

`ReferenceBaseNode.uniformType`

***

### updateAfterType

> **updateAfterType**: `string`

Defined in: three/src/nodes/core/Node.js:61

The update type of the node's [Node#updateAfter](Three.WebGPU.Node.html#updateafter) method. Possible values are listed in [NodeUpdateType](../variables/Three.WebGPU.NodeUpdateType.html).

#### Default

```ts
'none'
```

#### Inherited from

`ReferenceBaseNode.updateAfterType`

***

### updateBeforeType

> **updateBeforeType**: `string`

Defined in: three/src/nodes/core/Node.js:53

The update type of the node's [Node#updateBefore](Three.WebGPU.Node.html#updatebefore) method. Possible values are listed in [NodeUpdateType](../variables/Three.WebGPU.NodeUpdateType.html).

#### Default

```ts
'none'
```

#### Inherited from

`ReferenceBaseNode.updateBeforeType`

***

### updateType

> **updateType**: `string`

Defined in: three/src/nodes/core/Node.js:45

The update type of the node's [Node#update](Three.WebGPU.Node.html#update) method. Possible values are listed in [NodeUpdateType](../variables/Three.WebGPU.NodeUpdateType.html).

#### Default

```ts
'none'
```

#### Inherited from

`ReferenceBaseNode.updateType`

***

### uuid

> `readonly` **uuid**: `string`

Defined in: three/src/nodes/core/Node.js:69

The UUID of the node.

#### Inherited from

`ReferenceBaseNode.uuid`

***

### version

> `readonly` **version**: `number`

Defined in: three/src/nodes/core/Node.js:78

The version of the node. The version automatically is increased when [Node#needsUpdate](Three.WebGPU.Node.html#needsupdate) is set to `true`.

#### Default

```ts
0
```

#### Inherited from

`ReferenceBaseNode.version`

## Accessors

### needsUpdate

#### Set Signature

> **set** **needsUpdate**(`value`): `void`

Defined in: three/src/nodes/core/Node.js:130

Set this property to `true` when the node should be regenerated.

##### Default

```ts
false
```

##### Parameters

###### value

`boolean`

##### Returns

`void`

#### Inherited from

`ReferenceBaseNode.needsUpdate`

***

### type

#### Get Signature

> **get** **type**(): `string`

Defined in: three/src/nodes/core/Node.js:146

The type of the class. The value is usually the constructor name.

##### Returns

`string`

#### Inherited from

`ReferenceBaseNode.type`

***

### type

#### Get Signature

> **get** `static` **type**(): `string`

Defined in: three/src/nodes/accessors/RendererReferenceNode.js:18

##### Returns

`string`

#### Overrides

`ReferenceBaseNode.type`

## Methods

### addEventListener()

> **addEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:31

Adds the given event listener to the given event type.

#### Parameters

##### type

`string`

The type of event to listen to.

##### listener

`Function`

The function that gets called when the event is fired.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.addEventListener`

***

### analyze()

> **analyze**(`builder`): `void`

Defined in: three/src/nodes/core/Node.js:502

Represents the analyze stage which is the second step of the build process, see [Node#build](Three.WebGPU.Node.html#build) method.
This stage analyzes the node hierarchy and ensures descendent nodes are built.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.analyze`

***

### build()

> **build**(`builder`, `output`): `string`

Defined in: three/src/nodes/core/Node.js:596

This method performs the build of a node. The behavior of this method as well as its return value depend
on the current build stage (setup, analyze or generate).

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

##### output

`string` = `null`

Can be used to define the output type.

#### Returns

`string`

When this method is executed in the setup or analyze stage, `null` is returned. In the generate stage, the generated shader string.

#### Inherited from

`ReferenceBaseNode.build`

***

### customCacheKey()

> **customCacheKey**(): `number`

Defined in: three/src/nodes/core/Node.js:338

Generate a custom cache key for this node.

#### Returns

`number`

The cache key of the node.

#### Inherited from

`ReferenceBaseNode.customCacheKey`

***

### deserialize()

> **deserialize**(`json`): `void`

Defined in: three/src/nodes/core/Node.js:771

Deserializes the node from the given JSON.

#### Parameters

##### json

`any`

The JSON object.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.deserialize`

***

### dispatchEvent()

> **dispatchEvent**(`event`): `void`

Defined in: three/src/core/EventDispatcher.js:101

Dispatches an event object.

#### Parameters

##### event

`any`

The event that gets fired.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.dispatchEvent`

***

### dispose()

> **dispose**(): `void`

Defined in: three/src/nodes/core/Node.js:282

Calling this method dispatches the `dispose` event. This event can be used
to register event listeners for clean up tasks.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.dispose`

***

### element()

> **element**(`indexNode`): `ReferenceElementNode`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:200

When the referred property is array-like, this method can be used
to access elements via an index node.

#### Parameters

##### indexNode

`IndexNode`

indexNode.

#### Returns

`ReferenceElementNode`

A reference to an element.

#### Inherited from

`ReferenceBaseNode.element`

***

### generate()

> **generate**(`builder`, `output`): `string`

Defined in: three/src/nodes/core/Node.js:534

Represents the generate stage which is the third step of the build process, see [Node#build](Three.WebGPU.Node.html#build) method.
This state builds the output node and returns the resulting shader string.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

##### output

`string`

Can be used to define the output type.

#### Returns

`string`

The generated shader string.

#### Inherited from

`ReferenceBaseNode.generate`

***

### getCacheKey()

> **getCacheKey**(`force`?): `number`

Defined in: three/src/nodes/core/Node.js:318

Returns the cache key for this node.

#### Parameters

##### force?

`boolean` = `false`

When set to `true`, a recomputation of the cache key is forced.

#### Returns

`number`

The cache key of the node.

#### Inherited from

`ReferenceBaseNode.getCacheKey`

***

### getChildren()

> **getChildren**(): `Generator`\<`any`, `void`, `unknown`\>

Defined in: three/src/nodes/core/Node.js:268

Generator function that can be used to iterate over the child nodes.

#### Returns

`Generator`\<`any`, `void`, `unknown`\>

#### Generator

#### Yields

A child node.

#### Inherited from

`ReferenceBaseNode.getChildren`

***

### getElementType()

> **getElementType**(`builder`): `string`

Defined in: three/src/nodes/core/Node.js:410

Certain types are composed of multiple elements. For example a `vec3`
is composed of three `float` values. This method returns the type of
these elements.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`string`

The type of the node.

#### Inherited from

`ReferenceBaseNode.getElementType`

***

### getHash()

> **getHash**(): `string`

Defined in: three/src/nodes/core/Node.js:363

Returns the hash of the node which is used to identify the node. By default it's
the [Node#uuid](Three.WebGPU.Node.html#uuid) however derived node classes might have to overwrite this method
depending on their implementation.

#### Returns

`string`

The hash.

#### Inherited from

`ReferenceBaseNode.getHash`

***

### getMemberType()

> **getMemberType**(): `string`

Defined in: three/src/nodes/core/Node.js:426

Returns the node member type for the given name.

#### Returns

`string`

The type of the node.

#### Inherited from

`ReferenceBaseNode.getMemberType`

***

### getNodeType()

> **getNodeType**(`builder`): `string`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:233

This method is overwritten since the node type is inferred from
the type of the reference node.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`string`

The node type.

#### Inherited from

`ReferenceBaseNode.getNodeType`

***

### getScope()

> **getScope**(): [`Node`](Three.WebGPU.Node.html)

Defined in: three/src/nodes/core/Node.js:349

Returns the references to this node which is by default `this`.

#### Returns

[`Node`](Three.WebGPU.Node.html)

A reference to this node.

#### Inherited from

`ReferenceBaseNode.getScope`

***

### getSelf()

> **getSelf**(): [`Node`](Three.WebGPU.Node.html)

Defined in: three/src/nodes/core/Node.js:227

The `this` reference might point to a Proxy so this method can be used
to get the reference to the actual node instance.

#### Returns

[`Node`](Three.WebGPU.Node.html)

A reference to the node.

#### Inherited from

`ReferenceBaseNode.getSelf`

***

### getSerializeChildren()

> **getSerializeChildren**(): `any`[]

Defined in: three/src/nodes/core/Node.js:721

Returns the child nodes as a JSON object.

#### Returns

`any`[]

An iterable list of serialized child objects as JSON.

#### Inherited from

`ReferenceBaseNode.getSerializeChildren`

***

### getShared()

> **getShared**(`builder`): [`Node`](Three.WebGPU.Node.html)

Defined in: three/src/nodes/core/Node.js:461

This method is used during the build process of a node and ensures
equal nodes are not built multiple times but just once. For example if
`attribute( 'uv' )` is used multiple times by the user, the build
process makes sure to process just the first node.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

[`Node`](Three.WebGPU.Node.html)

The shared node if possible. Otherwise `this` is returned.

#### Inherited from

`ReferenceBaseNode.getShared`

***

### getUpdateAfterType()

> **getUpdateAfterType**(): `object`

Defined in: three/src/nodes/core/Node.js:396

Returns the update type of [Node#updateAfter](Three.WebGPU.Node.html#updateafter).

#### Returns

`object`

The update type.

##### FRAME

> **FRAME**: `string` = `'frame'`

##### NONE

> **NONE**: `string` = `'none'`

##### OBJECT

> **OBJECT**: `string` = `'object'`

##### RENDER

> **RENDER**: `string` = `'render'`

#### Inherited from

`ReferenceBaseNode.getUpdateAfterType`

***

### getUpdateBeforeType()

> **getUpdateBeforeType**(): `object`

Defined in: three/src/nodes/core/Node.js:385

Returns the update type of [Node#updateBefore](Three.WebGPU.Node.html#updatebefore).

#### Returns

`object`

The update type.

##### FRAME

> **FRAME**: `string` = `'frame'`

##### NONE

> **NONE**: `string` = `'none'`

##### OBJECT

> **OBJECT**: `string` = `'object'`

##### RENDER

> **RENDER**: `string` = `'render'`

#### Inherited from

`ReferenceBaseNode.getUpdateBeforeType`

***

### getUpdateType()

> **getUpdateType**(): `object`

Defined in: three/src/nodes/core/Node.js:374

Returns the update type of [Node#update](Three.WebGPU.Node.html#update).

#### Returns

`object`

The update type.

##### FRAME

> **FRAME**: `string` = `'frame'`

##### NONE

> **NONE**: `string` = `'none'`

##### OBJECT

> **OBJECT**: `string` = `'object'`

##### RENDER

> **RENDER**: `string` = `'render'`

#### Inherited from

`ReferenceBaseNode.getUpdateType`

***

### getValueFromReference()

> **getValueFromReference**(`object`?): `any`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:252

Returns the property value from the given referred object.

#### Parameters

##### object?

`any` = `...`

The object to retrieve the property value from.

#### Returns

`any`

The value.

#### Inherited from

`ReferenceBaseNode.getValueFromReference`

***

### hasEventListener()

> **hasEventListener**(`type`, `listener`): `boolean`

Defined in: three/src/core/EventDispatcher.js:58

Returns `true` if the given event listener has been added to the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to check.

#### Returns

`boolean`

Whether the given event listener has been added to the given event type.

#### Inherited from

`ReferenceBaseNode.hasEventListener`

***

### isGlobal()

> **isGlobal**(): `boolean`

Defined in: three/src/nodes/core/Node.js:256

By default this method returns the value of the [Node#global](Three.WebGPU.Node.html#global) flag. This method
can be overwritten in derived classes if an analytical way is required to determine the
global status.

#### Returns

`boolean`

Whether this node is global or not.

#### Inherited from

`ReferenceBaseNode.isGlobal`

***

### onFrameUpdate()

> **onFrameUpdate**(`callback`): [`Node`](Three.WebGPU.Node.html)

Defined in: three/src/nodes/core/Node.js:175

Convenient method for defining [Node#update](Three.WebGPU.Node.html#update). Similar to [Node#onUpdate](Three.WebGPU.Node.html#onupdate), but
this method automatically sets the update type to `FRAME`.

#### Parameters

##### callback

`Function`

The update method.

#### Returns

[`Node`](Three.WebGPU.Node.html)

A reference to this node.

#### Inherited from

`ReferenceBaseNode.onFrameUpdate`

***

### onObjectUpdate()

> **onObjectUpdate**(`callback`): [`Node`](Three.WebGPU.Node.html)

Defined in: three/src/nodes/core/Node.js:201

Convenient method for defining [Node#update](Three.WebGPU.Node.html#update). Similar to [Node#onUpdate](Three.WebGPU.Node.html#onupdate), but
this method automatically sets the update type to `OBJECT`.

#### Parameters

##### callback

`Function`

The update method.

#### Returns

[`Node`](Three.WebGPU.Node.html)

A reference to this node.

#### Inherited from

`ReferenceBaseNode.onObjectUpdate`

***

### onReference()

> **onReference**(`callback`): [`Node`](Three.WebGPU.Node.html)

Defined in: three/src/nodes/core/Node.js:213

Convenient method for defining [Node#updateReference](Three.WebGPU.Node.html#updatereference).

#### Parameters

##### callback

`Function`

The update method.

#### Returns

[`Node`](Three.WebGPU.Node.html)

A reference to this node.

#### Inherited from

`ReferenceBaseNode.onReference`

***

### onRenderUpdate()

> **onRenderUpdate**(`callback`): [`Node`](Three.WebGPU.Node.html)

Defined in: three/src/nodes/core/Node.js:188

Convenient method for defining [Node#update](Three.WebGPU.Node.html#update). Similar to [Node#onUpdate](Three.WebGPU.Node.html#onupdate), but
this method automatically sets the update type to `RENDER`.

#### Parameters

##### callback

`Function`

The update method.

#### Returns

[`Node`](Three.WebGPU.Node.html)

A reference to this node.

#### Inherited from

`ReferenceBaseNode.onRenderUpdate`

***

### onUpdate()

> **onUpdate**(`callback`, `updateType`): [`Node`](Three.WebGPU.Node.html)

Defined in: three/src/nodes/core/Node.js:159

Convenient method for defining [Node#update](Three.WebGPU.Node.html#update).

#### Parameters

##### callback

`Function`

The update method.

##### updateType

`string`

The update type.

#### Returns

[`Node`](Three.WebGPU.Node.html)

A reference to this node.

#### Inherited from

`ReferenceBaseNode.onUpdate`

***

### removeEventListener()

> **removeEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:74

Removes the given event listener from the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to remove.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.removeEventListener`

***

### serialize()

> **serialize**(`json`): `void`

Defined in: three/src/nodes/core/Node.js:732

Serializes the node to JSON.

#### Parameters

##### json

`any`

The output JSON object.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.serialize`

***

### setGroup()

> **setGroup**(`group`): `ReferenceBaseNode`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:185

Sets the uniform group for this reference node.

#### Parameters

##### group

`UniformGroupNode`

The uniform group to set.

#### Returns

`ReferenceBaseNode`

A reference to this node.

#### Inherited from

`ReferenceBaseNode.setGroup`

***

### setNodeType()

> **setNodeType**(`uniformType`): `void`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:212

Sets the node type which automatically defines the internal
uniform type.

#### Parameters

##### uniformType

`string`

The type to set.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.setNodeType`

***

### setup()

> **setup**(): `UniformNode`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:288

The output of the reference node is the internal uniform node.

#### Returns

`UniformNode`

The output node.

#### Inherited from

`ReferenceBaseNode.setup`

***

### toJSON()

> **toJSON**(`meta`): `any`

Defined in: three/src/nodes/core/Node.js:825

Serializes the node into the three.js JSON Object/Scene format.

#### Parameters

##### meta

`any`

An optional JSON object that already holds serialized data from other scene objects.

#### Returns

`any`

The serialized node.

#### Inherited from

`ReferenceBaseNode.toJSON`

***

### traverse()

> **traverse**(`callback`): `void`

Defined in: three/src/nodes/core/Node.js:300

Can be used to traverse through the node's hierarchy.

#### Parameters

##### callback

`traverseCallback`

A callback that is executed per node.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.traverse`

***

### update()

> **update**(): `void`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:301

Overwritten to to update the internal uniform value.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.update`

***

### updateAfter()

> `abstract` **updateAfter**(): `boolean`

Defined in: three/src/nodes/core/Node.js:568

The method can be implemented to update the node's internal state after it was used to render an object.
The [Node#updateAfterType](Three.WebGPU.Node.html#updateaftertype) property defines how often the update is executed.

#### Returns

`boolean`

An optional bool that indicates whether the implementation actually performed an update or not (e.g. due to caching).

#### Inherited from

`ReferenceBaseNode.updateAfter`

***

### updateBefore()

> `abstract` **updateBefore**(): `boolean`

Defined in: three/src/nodes/core/Node.js:554

The method can be implemented to update the node's internal state before it is used to render an object.
The [Node#updateBeforeType](Three.WebGPU.Node.html#updatebeforetype) property defines how often the update is executed.

#### Returns

`boolean`

An optional bool that indicates whether the implementation actually performed an update or not (e.g. due to caching).

#### Inherited from

`ReferenceBaseNode.updateBefore`

***

### updateReference()

> **updateReference**(`state`): `any`

Defined in: three/src/nodes/accessors/RendererReferenceNode.js:56

Updates the reference based on the given state. The state is only evaluated
[RendererReferenceNode#renderer](#renderer) is not set.

#### Parameters

##### state

`any`

The current state.

#### Returns

`any`

The updated reference.

#### Overrides

`ReferenceBaseNode.updateReference`

***

### updateValue()

> **updateValue**(): `void`

Defined in: three/src/nodes/accessors/ReferenceBaseNode.js:311

Retrieves the value from the referred object property and uses it
to updated the internal uniform.

#### Returns

`void`

#### Inherited from

`ReferenceBaseNode.updateValue`
