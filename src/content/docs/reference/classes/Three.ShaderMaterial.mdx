---
editUrl: false
next: false
prev: false
title: "ShaderMaterial"
---

Defined in: three/src/materials/ShaderMaterial.js:39

A material rendered with custom shaders. A shader is a small program written in GLSL.
that runs on the GPU. You may want to use a custom shader if you need to implement an
effect not included with any of the built-in materials.

There are the following notes to bear in mind when using a `ShaderMaterial`:

- `ShaderMaterial` can only be used with [WebGLRenderer](Three.WebGLRenderer.html).
- Built in attributes and uniforms are passed to the shaders along with your code. If
you don't want that, use [RawShaderMaterial](Three.RawShaderMaterial.html) instead.
- You can use the directive `#pragma unroll_loop_start` and `#pragma unroll_loop_end`
in order to unroll a `for` loop in GLSL by the shader preprocessor. The directive has
to be placed right above the loop. The loop formatting has to correspond to a defined standard.
  - The loop has to be [normalized][https://en.wikipedia.org/wiki/Normalized\_loop](https://en.wikipedia.org/wiki/Normalized_loop).
  - The loop variable has to be *i*.
  - The value `UNROLLED_LOOP_INDEX` will be replaced with the explicitly
value of *i* for the given iteration and can be used in preprocessor
statements.

```js
const material = new THREE.ShaderMaterial( {
	uniforms: {
		time: { value: 1.0 },
		resolution: { value: new THREE.Vector2() }
	},
	vertexShader: document.getElementById( 'vertexShader' ).textContent,
	fragmentShader: document.getElementById( 'fragmentShader' ).textContent
} );
```

## Extends

- [`Material`](Three.Material.html)

## Extended by

- [`RawShaderMaterial`](Three.RawShaderMaterial.html)

## Constructors

### Constructor

> **new ShaderMaterial**(`parameters`?): `ShaderMaterial`

Defined in: three/src/materials/ShaderMaterial.js:50

Constructs a new shader material.

#### Parameters

##### parameters?

`any`

An object with one or more properties
defining the material's appearance. Any property of the material
(including any property from inherited materials) can be passed
in here. Color values can be passed any type of value accepted
by [Color#set](Three.Color.html#set).

#### Returns

`ShaderMaterial`

#### Overrides

[`Material`](Three.Material.html).[`constructor`](Three.Material.html#constructor)

## Properties

### \_alphaTest

> **\_alphaTest**: `number`

Defined in: three/src/materials/Material.js:474

#### Inherited from

[`Material`](Three.Material.html).[`_alphaTest`](Three.Material.html#_alphatest)

***

### \_listeners

> **\_listeners**: `object`

Defined in: three/src/core/EventDispatcher.js:33

#### Inherited from

[`Material`](Three.Material.html).[`_listeners`](Three.Material.html#_listeners)

***

### allowOverride

> **allowOverride**: `boolean`

Defined in: three/src/materials/Material.js:436

Whether it's possible to override the material with [Scene#overrideMaterial](Three.Scene.html#overridematerial) or not.

#### Default

```ts
true
```

#### Inherited from

[`Material`](Three.Material.html).[`allowOverride`](Three.Material.html#allowoverride)

***

### alphaHash

> **alphaHash**: `boolean`

Defined in: three/src/materials/Material.js:132

Enables alpha hashed transparency, an alternative to [Material#transparent](Three.Material.html#transparent) or
[Material#alphaTest](Three.Material.html#alphatest). The material will not be rendered if opacity is lower than
a random threshold. Randomization introduces some grain or noise, but approximates alpha
blending without the associated problems of sorting. Using TAA can reduce the resulting noise.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`alphaHash`](Three.Material.html#alphahash)

***

### alphaToCoverage

> **alphaToCoverage**: `boolean`

Defined in: three/src/materials/Material.js:406

Whether alpha to coverage should be enabled or not. Can only be used with MSAA-enabled contexts
(meaning when the renderer was created with *antialias* parameter set to `true`). Enabling this
will smooth aliasing on clip plane edges and alphaTest-clipped edges.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`alphaToCoverage`](Three.Material.html#alphatocoverage)

***

### blendAlpha

> **blendAlpha**: `number`

Defined in: three/src/materials/Material.js:200

Represents the alpha value of the constant blend color.

This property has only an effect when using custom blending with `ConstantAlpha` or `OneMinusConstantAlpha`.

#### Default

```ts
0
```

#### Inherited from

[`Material`](Three.Material.html).[`blendAlpha`](Three.Material.html#blendalpha)

***

### blendColor

> **blendColor**: [`Color`](Three.Color.html)

Defined in: three/src/materials/Material.js:190

Represents the RGB values of the constant blend color.

This property has only an effect when using custom blending with `ConstantColor` or `OneMinusConstantColor`.

#### Default

```ts
(0,0,0)
```

#### Inherited from

[`Material`](Three.Material.html).[`blendColor`](Three.Material.html#blendcolor)

***

### blendDst

> **blendDst**: `any`

Defined in: three/src/materials/Material.js:148

Defines the blending destination factor.

#### Default

```ts
OneMinusSrcAlphaFactor
```

#### Inherited from

[`Material`](Three.Material.html).[`blendDst`](Three.Material.html#blenddst)

***

### blendDstAlpha

> **blendDstAlpha**: `any`

Defined in: three/src/materials/Material.js:172

Defines the blending destination alpha factor.

#### Default

```ts
null
```

#### Inherited from

[`Material`](Three.Material.html).[`blendDstAlpha`](Three.Material.html#blenddstalpha)

***

### blendEquation

> **blendEquation**: `any`

Defined in: three/src/materials/Material.js:156

Defines the blending equation.

#### Default

```ts
OneMinusSrcAlphaFactor
```

#### Inherited from

[`Material`](Three.Material.html).[`blendEquation`](Three.Material.html#blendequation)

***

### blendEquationAlpha

> **blendEquationAlpha**: `any`

Defined in: three/src/materials/Material.js:180

Defines the blending equation of the alpha channel.

#### Default

```ts
OneMinusSrcAlphaFactor
```

#### Inherited from

[`Material`](Three.Material.html).[`blendEquationAlpha`](Three.Material.html#blendequationalpha)

***

### blending

> **blending**: `any`

Defined in: three/src/materials/Material.js:77

Defines the blending type of the material.

It must be set to `CustomBlending` if custom blending properties like
[Material#blendSrc](Three.Material.html#blendsrc), [Material#blendDst](Three.Material.html#blenddst) or [Material#blendEquation](Three.Material.html#blendequation)
should have any effect.

#### Default

```ts
NormalBlending
```

#### Inherited from

[`Material`](Three.Material.html).[`blending`](Three.Material.html#blending)

***

### blendSrc

> **blendSrc**: `any`

Defined in: three/src/materials/Material.js:140

Defines the blending source factor.

#### Default

```ts
SrcAlphaFactor
```

#### Inherited from

[`Material`](Three.Material.html).[`blendSrc`](Three.Material.html#blendsrc)

***

### blendSrcAlpha

> **blendSrcAlpha**: `any`

Defined in: three/src/materials/Material.js:164

Defines the blending source alpha factor.

#### Default

```ts
null
```

#### Inherited from

[`Material`](Three.Material.html).[`blendSrcAlpha`](Three.Material.html#blendsrcalpha)

***

### clipIntersection

> **clipIntersection**: `boolean`

Defined in: three/src/materials/Material.js:317

Changes the behavior of clipping planes so that only their intersection is
clipped, rather than their union.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`clipIntersection`](Three.Material.html#clipintersection)

***

### clipping

> **clipping**: `boolean`

Defined in: three/src/materials/ShaderMaterial.js:185

Defines whether this material supports clipping; `true` to let the renderer
pass the clippingPlanes uniform.

#### Default

```ts
false
```

***

### clippingPlanes

> **clippingPlanes**: `Plane`[]

Defined in: three/src/materials/Material.js:308

User-defined clipping planes specified as THREE.Plane objects in world
space. These planes apply to the objects this material is attached to.
Points in space whose signed distance to the plane is negative are clipped
(not rendered). This requires [WebGLRenderer#localClippingEnabled](Three.WebGLRenderer.html#localclippingenabled) to
be `true`.

#### Default

```ts
null
```

#### Inherited from

[`Material`](Three.Material.html).[`clippingPlanes`](Three.Material.html#clippingplanes)

***

### clipShadows

> **clipShadows**: `boolean`

Defined in: three/src/materials/Material.js:326

Defines whether to clip shadows according to the clipping planes specified
on this material.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`clipShadows`](Three.Material.html#clipshadows)

***

### colorWrite

> **colorWrite**: `boolean`

Defined in: three/src/materials/Material.js:350

Whether to render the material's color.

This can be used in conjunction with Object3D#renderOder to create invisible
objects that occlude other objects.

#### Default

```ts
true
```

#### Inherited from

[`Material`](Three.Material.html).[`colorWrite`](Three.Material.html#colorwrite)

***

### defaultAttributeValues

> **defaultAttributeValues**: `any`

Defined in: three/src/materials/ShaderMaterial.js:219

When the rendered geometry doesn't include these attributes but the
material does, these default values will be passed to the shaders. This
avoids errors when buffer data is missing.

- color: [ 1, 1, 1 ]
- uv: [ 0, 0 ]
- uv1: [ 0, 0 ]

***

### defines

> **defines**: `any`

Defined in: three/src/materials/ShaderMaterial.js:83

Defines custom constants using `#define` directives within the GLSL code
for both the vertex shader and the fragment shader; each key/value pair
yields another directive.
```js
defines: {
	FOO: 15,
	BAR: true
}
```
Yields the lines:
```
#define FOO 15
#define BAR true
```

***

### depthFunc

> **depthFunc**: `any`

Defined in: three/src/materials/Material.js:208

Defines the depth function.

#### Default

```ts
LessEqualDepth
```

#### Inherited from

[`Material`](Three.Material.html).[`depthFunc`](Three.Material.html#depthfunc)

***

### depthTest

> **depthTest**: `boolean`

Defined in: three/src/materials/Material.js:217

Whether to have depth test enabled when rendering this material.
When the depth test is disabled, the depth write will also be implicitly disabled.

#### Default

```ts
true
```

#### Inherited from

[`Material`](Three.Material.html).[`depthTest`](Three.Material.html#depthtest)

***

### depthWrite

> **depthWrite**: `boolean`

Defined in: three/src/materials/Material.js:228

Whether rendering this material has any effect on the depth buffer.

When drawing 2D overlays it can be useful to disable the depth writing in
order to layer several things together without creating z-index artifacts.

#### Default

```ts
true
```

#### Inherited from

[`Material`](Three.Material.html).[`depthWrite`](Three.Material.html#depthwrite)

***

### dithering

> **dithering**: `boolean`

Defined in: three/src/materials/Material.js:396

Whether to apply dithering to the color to remove the appearance of banding.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`dithering`](Three.Material.html#dithering)

***

### extensions

> **extensions**: `object`

Defined in: three/src/materials/ShaderMaterial.js:203

This object allows to enable certain WebGL 2 extensions.

- clipCullDistance: set to `true` to use vertex shader clipping
- multiDraw: set to `true` to use vertex shader multi_draw / enable gl_DrawID

#### clipCullDistance

> **clipCullDistance**: `false`

#### multiDraw

> **multiDraw**: `false`

***

### fog

> **fog**: `boolean`

Defined in: three/src/materials/ShaderMaterial.js:167

Define whether the material color is affected by global fog settings; `true`
to pass fog uniforms to the shader.

#### Default

```ts
false
```

***

### forceSinglePass

> **forceSinglePass**: `boolean`

Defined in: three/src/materials/Material.js:428

Whether double-sided, transparent objects should be rendered with a single pass or not.

The engine renders double-sided, transparent objects with two draw calls (back faces first,
then front faces) to mitigate transparency artifacts. There are scenarios however where this
approach produces no quality gains but still doubles draw calls e.g. when rendering flat
vegetation like grass sprites. In these cases, set the `forceSinglePass` flag to `true` to
disable the two pass rendering to avoid performance issues.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`forceSinglePass`](Three.Material.html#forcesinglepass)

***

### fragmentShader

> **fragmentShader**: `string`

Defined in: three/src/materials/ShaderMaterial.js:128

Fragment shader GLSL code. This is the actual code for the shader.

***

### glslVersion

> **glslVersion**: `any`

Defined in: three/src/materials/ShaderMaterial.js:249

Defines the GLSL version of custom shader code.

#### Default

```ts
null
```

***

### index0AttributeName

> **index0AttributeName**: `string`

Defined in: three/src/materials/ShaderMaterial.js:232

If set, this calls [gl.bindAttribLocation][https://developer.mozilla.org/en-US/docs/Web/API/WebGLRenderingContext/bindAttribLocation](https://developer.mozilla.org/en-US/docs/Web/API/WebGLRenderingContext/bindAttribLocation)
to bind a generic vertex index to an attribute variable.

#### Default

```ts
undefined
```

***

### isMaterial

> `readonly` **isMaterial**: `boolean`

Defined in: three/src/materials/Material.js:32

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`Material`](Three.Material.html).[`isMaterial`](Three.Material.html#ismaterial)

***

### isShaderMaterial

> `readonly` **isShaderMaterial**: `boolean`

Defined in: three/src/materials/ShaderMaterial.js:61

This flag can be used for type testing.

#### Default

```ts
true
```

***

### lights

> **lights**: `boolean`

Defined in: three/src/materials/ShaderMaterial.js:176

Defines whether this material uses lighting; `true` to pass uniform data
related to lighting to this shader.

#### Default

```ts
false
```

***

### linewidth

> **linewidth**: `number`

Defined in: three/src/materials/ShaderMaterial.js:139

Controls line thickness or lines.

WebGL and WebGPU ignore this setting and always render line primitives with a
width of one pixel.

#### Default

```ts
1
```

***

### name

> **name**: `string`

Defined in: three/src/materials/Material.js:56

The name of the material.

#### Inherited from

[`Material`](Three.Material.html).[`name`](Three.Material.html#name)

***

### opacity

> **opacity**: `number`

Defined in: three/src/materials/Material.js:108

Defines how transparent the material is.
A value of `0.0` indicates fully transparent, `1.0` is fully opaque.

If the [Material#transparent](Three.Material.html#transparent) is not set to `true`,
the material will remain fully opaque and this value will only affect its color.

#### Default

```ts
1
```

#### Inherited from

[`Material`](Three.Material.html).[`opacity`](Three.Material.html#opacity)

***

### polygonOffset

> **polygonOffset**: `boolean`

Defined in: three/src/materials/Material.js:372

Whether to use polygon offset or not. When enabled, each fragment's depth value will
be offset after it is interpolated from the depth values of the appropriate vertices.
The offset is added before the depth test is performed and before the value is written
into the depth buffer.

Can be useful for rendering hidden-line images, for applying decals to surfaces, and for
rendering solids with highlighted edges.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`polygonOffset`](Three.Material.html#polygonoffset)

***

### polygonOffsetFactor

> **polygonOffsetFactor**: `number`

Defined in: three/src/materials/Material.js:380

Specifies a scale factor that is used to create a variable depth offset for each polygon.

#### Default

```ts
0
```

#### Inherited from

[`Material`](Three.Material.html).[`polygonOffsetFactor`](Three.Material.html#polygonoffsetfactor)

***

### polygonOffsetUnits

> **polygonOffsetUnits**: `number`

Defined in: three/src/materials/Material.js:388

Is multiplied by an implementation-specific value to create a constant depth offset.

#### Default

```ts
0
```

#### Inherited from

[`Material`](Three.Material.html).[`polygonOffsetUnits`](Three.Material.html#polygonoffsetunits)

***

### precision

> **precision**: `"highp"` \| `"mediump"` \| `"lowp"`

Defined in: three/src/materials/Material.js:358

Override the renderer's default precision for this material.

#### Default

```ts
null
```

#### Inherited from

[`Material`](Three.Material.html).[`precision`](Three.Material.html#precision)

***

### premultipliedAlpha

> **premultipliedAlpha**: `boolean`

Defined in: three/src/materials/Material.js:414

Whether to premultiply the alpha (transparency) value.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`premultipliedAlpha`](Three.Material.html#premultipliedalpha)

***

### shadowSide

> **shadowSide**: `any`

Defined in: three/src/materials/Material.js:339

Defines which side of faces cast shadows. If `null`, the side casting shadows
is determined as follows:

- When [Material#side](Three.Material.html#side) is set to `FrontSide`, the back side cast shadows.
- When [Material#side](Three.Material.html#side) is set to `BackSide`, the front side cast shadows.
- When [Material#side](Three.Material.html#side) is set to `DoubleSide`, both sides cast shadows.

#### Default

```ts
null
```

#### Inherited from

[`Material`](Three.Material.html).[`shadowSide`](Three.Material.html#shadowside)

***

### side

> **side**: `any`

Defined in: three/src/materials/Material.js:85

Defines which side of faces will be rendered - front, back or both.

#### Default

```ts
FrontSide
```

#### Inherited from

[`Material`](Three.Material.html).[`side`](Three.Material.html#side)

***

### stencilFail

> **stencilFail**: `any`

Defined in: three/src/materials/Material.js:268

Which stencil operation to perform when the comparison function returns `false`.

#### Default

```ts
KeepStencilOp
```

#### Inherited from

[`Material`](Three.Material.html).[`stencilFail`](Three.Material.html#stencilfail)

***

### stencilFunc

> **stencilFunc**: `any`

Defined in: three/src/materials/Material.js:244

The stencil comparison function to use.

#### Default

```ts
AlwaysStencilFunc
```

#### Inherited from

[`Material`](Three.Material.html).[`stencilFunc`](Three.Material.html#stencilfunc)

***

### stencilFuncMask

> **stencilFuncMask**: `number`

Defined in: three/src/materials/Material.js:260

The bit mask to use when comparing against the stencil buffer.

#### Default

```ts
0xff
```

#### Inherited from

[`Material`](Three.Material.html).[`stencilFuncMask`](Three.Material.html#stencilfuncmask)

***

### stencilRef

> **stencilRef**: `number`

Defined in: three/src/materials/Material.js:252

The value to use when performing stencil comparisons or stencil operations.

#### Default

```ts
0
```

#### Inherited from

[`Material`](Three.Material.html).[`stencilRef`](Three.Material.html#stencilref)

***

### stencilWrite

> **stencilWrite**: `boolean`

Defined in: three/src/materials/Material.js:296

Whether stencil operations are performed against the stencil buffer. In
order to perform writes or comparisons against the stencil buffer this
value must be `true`.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`stencilWrite`](Three.Material.html#stencilwrite)

***

### stencilWriteMask

> **stencilWriteMask**: `number`

Defined in: three/src/materials/Material.js:236

The bit mask to use when writing to the stencil buffer.

#### Default

```ts
0xff
```

#### Inherited from

[`Material`](Three.Material.html).[`stencilWriteMask`](Three.Material.html#stencilwritemask)

***

### stencilZFail

> **stencilZFail**: `any`

Defined in: three/src/materials/Material.js:277

Which stencil operation to perform when the comparison function returns
`true` but the depth test fails.

#### Default

```ts
KeepStencilOp
```

#### Inherited from

[`Material`](Three.Material.html).[`stencilZFail`](Three.Material.html#stencilzfail)

***

### stencilZPass

> **stencilZPass**: `any`

Defined in: three/src/materials/Material.js:286

Which stencil operation to perform when the comparison function returns
`true` and the depth test passes.

#### Default

```ts
KeepStencilOp
```

#### Inherited from

[`Material`](Three.Material.html).[`stencilZPass`](Three.Material.html#stencilzpass)

***

### toneMapped

> **toneMapped**: `boolean`

Defined in: three/src/materials/Material.js:455

Defines whether this material is tone mapped according to the renderer's tone mapping setting.

It is ignored when rendering to a render target or using post processing or when using
`WebGPURenderer`. In all these cases, all materials are honored by tone mapping.

#### Default

```ts
true
```

#### Inherited from

[`Material`](Three.Material.html).[`toneMapped`](Three.Material.html#tonemapped)

***

### transparent

> **transparent**: `boolean`

Defined in: three/src/materials/Material.js:121

Defines whether this material is transparent. This has an effect on
rendering as transparent objects need special treatment and are rendered
after non-transparent objects.

When set to true, the extent to which the material is transparent is
controlled by [Material#opacity](Three.Material.html#opacity).

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`transparent`](Three.Material.html#transparent)

***

### type

> **type**: `string`

Defined in: three/src/materials/ShaderMaterial.js:63

The type property is used for detecting the object type
in context of serialization/deserialization.

#### Inherited from

[`Material`](Three.Material.html).[`type`](Three.Material.html#type)

***

### uniforms

> **uniforms**: `any`

Defined in: three/src/materials/ShaderMaterial.js:107

An object of the form:
```js
{
	"uniform1": { value: 1.0 },
	"uniform2": { value: 2 }
}
```
specifying the uniforms to be passed to the shader code; keys are uniform
names, values are definitions of the form
```
{
	value: 1.0
}
```
where `value` is the value of the uniform. Names must match the name of
the uniform, as defined in the GLSL code. Note that uniforms are refreshed
on every frame, so updating the value of the uniform will immediately
update the value available to the GLSL code.

***

### uniformsGroups

> **uniformsGroups**: `UniformsGroup`[]

Defined in: three/src/materials/ShaderMaterial.js:114

An array holding uniforms groups for configuring UBOs.

***

### uniformsNeedUpdate

> **uniformsNeedUpdate**: `boolean`

Defined in: three/src/materials/ShaderMaterial.js:241

Can be used to force a uniform update while changing uniforms in
[Object3D#onBeforeRender](Three.Object3D.html#onbeforerender).

#### Default

```ts
false
```

***

### userData

> **userData**: `any`

Defined in: three/src/materials/Material.js:463

An object that can be used to store custom data about the Material. It
should not hold references to functions as these will not be cloned.

#### Inherited from

[`Material`](Three.Material.html).[`userData`](Three.Material.html#userdata)

***

### uuid

> `readonly` **uuid**: `string`

Defined in: three/src/materials/Material.js:49

The UUID of the material.

#### Inherited from

[`Material`](Three.Material.html).[`uuid`](Three.Material.html#uuid)

***

### version

> `readonly` **version**: `number`

Defined in: three/src/materials/Material.js:472

This starts at `0` and counts how many times [Material#needsUpdate](Three.Material.html#needsupdate) is set to `true`.

#### Default

```ts
0
```

#### Inherited from

[`Material`](Three.Material.html).[`version`](Three.Material.html#version)

***

### vertexColors

> **vertexColors**: `boolean`

Defined in: three/src/materials/Material.js:96

If set to `true`, vertex colors should be used.

The engine supports RGB and RGBA vertex colors depending on whether a three (RGB) or
four (RGBA) component color buffer attribute is used.

#### Default

```ts
false
```

#### Inherited from

[`Material`](Three.Material.html).[`vertexColors`](Three.Material.html#vertexcolors)

***

### vertexShader

> **vertexShader**: `string`

Defined in: three/src/materials/ShaderMaterial.js:121

Vertex shader GLSL code. This is the actual code for the shader.

***

### visible

> **visible**: `boolean`

Defined in: three/src/materials/Material.js:444

Defines whether 3D objects using this material are visible.

#### Default

```ts
true
```

#### Inherited from

[`Material`](Three.Material.html).[`visible`](Three.Material.html#visible)

***

### wireframe

> **wireframe**: `boolean`

Defined in: three/src/materials/ShaderMaterial.js:147

Renders the geometry as a wireframe.

#### Default

```ts
false
```

***

### wireframeLinewidth

> **wireframeLinewidth**: `number`

Defined in: three/src/materials/ShaderMaterial.js:158

Controls the thickness of the wireframe.

WebGL and WebGPU ignore this property and always render
1 pixel wide lines.

#### Default

```ts
1
```

## Accessors

### alphaTest

#### Get Signature

> **get** **alphaTest**(): `number`

Defined in: three/src/materials/Material.js:486

Sets the alpha value to be used when running an alpha test. The material
will not be rendered if the opacity is lower than this value.

##### Default

```ts
0
```

##### Returns

`number`

#### Inherited from

[`Material`](Three.Material.html).[`alphaTest`](Three.Material.html#alphatest)

***

### needsUpdate

#### Set Signature

> **set** **needsUpdate**(`value`): `void`

Defined in: three/src/materials/Material.js:994

Setting this property to `true` indicates the engine the material
needs to be recompiled.

##### Default

```ts
false
```

##### Parameters

###### value

`boolean`

##### Returns

`void`

#### Inherited from

[`Material`](Three.Material.html).[`needsUpdate`](Three.Material.html#needsupdate)

## Methods

### addEventListener()

> **addEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:31

Adds the given event listener to the given event type.

#### Parameters

##### type

`string`

The type of event to listen to.

##### listener

`Function`

The function that gets called when the event is fired.

#### Returns

`void`

#### Inherited from

[`Material`](Three.Material.html).[`addEventListener`](Three.Material.html#addeventlistener)

***

### clone()

> **clone**(): [`Material`](Three.Material.html)

Defined in: three/src/materials/Material.js:875

Returns a new material with copied values from this instance.

#### Returns

[`Material`](Three.Material.html)

A clone of this instance.

#### Inherited from

[`Material`](Three.Material.html).[`clone`](Three.Material.html#clone)

***

### copy()

> **copy**(`source`): `ShaderMaterial`

Defined in: three/src/materials/ShaderMaterial.js:259

Copies the values of the given material to this instance.

#### Parameters

##### source

`any`

The material to copy.

#### Returns

`ShaderMaterial`

A reference to this instance.

#### Overrides

[`Material`](Three.Material.html).[`copy`](Three.Material.html#copy)

***

### customProgramCacheKey()

> **customProgramCacheKey**(): `string`

Defined in: three/src/materials/Material.js:541

In case [Material#onBeforeCompile](Three.Material.html#onbeforecompile) is used, this callback can be used to identify
values of settings used in `onBeforeCompile()`, so three.js can reuse a cached
shader or recompile the shader for this material as needed.

This method can only be used when rendering with [WebGLRenderer](Three.WebGLRenderer.html).

#### Returns

`string`

The custom program cache key.

#### Inherited from

[`Material`](Three.Material.html).[`customProgramCacheKey`](Three.Material.html#customprogramcachekey)

***

### dispatchEvent()

> **dispatchEvent**(`event`): `void`

Defined in: three/src/core/EventDispatcher.js:101

Dispatches an event object.

#### Parameters

##### event

`any`

The event that gets fired.

#### Returns

`void`

#### Inherited from

[`Material`](Three.Material.html).[`dispatchEvent`](Three.Material.html#dispatchevent)

***

### dispose()

> **dispose**(): `void`

Defined in: three/src/materials/Material.js:974

Frees the GPU-related resources allocated by this instance. Call this
method whenever this instance is no longer used in your app.

#### Returns

`void`

#### Fires

Material#dispose

#### Inherited from

[`Material`](Three.Material.html).[`dispose`](Three.Material.html#dispose)

***

### hasEventListener()

> **hasEventListener**(`type`, `listener`): `boolean`

Defined in: three/src/core/EventDispatcher.js:58

Returns `true` if the given event listener has been added to the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to check.

#### Returns

`boolean`

Whether the given event listener has been added to the given event type.

#### Inherited from

[`Material`](Three.Material.html).[`hasEventListener`](Three.Material.html#haseventlistener)

***

### onBeforeCompile()

> **onBeforeCompile**(): `void`

Defined in: three/src/materials/Material.js:530

An optional callback that is executed immediately before the shader
program is compiled. This function is called with the shader source code
as a parameter. Useful for the modification of built-in materials.

This method can only be used when rendering with [WebGLRenderer](Three.WebGLRenderer.html). The
recommended approach when customizing materials is to use `WebGPURenderer` with the new
Node Material system and [TSL][https://github.com/mrdoob/three.js/wiki/Three.js-Shading-Language](https://github.com/mrdoob/three.js/wiki/Three.js-Shading-Language).

#### Returns

`void`

#### Inherited from

[`Material`](Three.Material.html).[`onBeforeCompile`](Three.Material.html#onbeforecompile)

***

### onBeforeRender()

> **onBeforeRender**(): `void`

Defined in: three/src/materials/Material.js:516

An optional callback that is executed immediately before the material is used to render a 3D object.

This method can only be used when rendering with [WebGLRenderer](Three.WebGLRenderer.html).

#### Returns

`void`

#### Inherited from

[`Material`](Three.Material.html).[`onBeforeRender`](Three.Material.html#onbeforerender)

***

### onBuild()

> **onBuild**(): `void`

Defined in: three/src/materials/Material.js:1000

#### Returns

`void`

#### Inherited from

[`Material`](Three.Material.html).[`onBuild`](Three.Material.html#onbuild)

***

### removeEventListener()

> **removeEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:74

Removes the given event listener from the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to remove.

#### Returns

`void`

#### Inherited from

[`Material`](Three.Material.html).[`removeEventListener`](Three.Material.html#removeeventlistener)

***

### setValues()

> **setValues**(`values`?): `void`

Defined in: three/src/materials/Material.js:554

This method can be used to set default values from parameter objects.
It is a generic implementation so it can be used with different types
of materials.

#### Parameters

##### values?

`any`

The material values to set.

#### Returns

`void`

#### Inherited from

[`Material`](Three.Material.html).[`setValues`](Three.Material.html#setvalues)

***

### toJSON()

> **toJSON**(`meta`): `any`

Defined in: three/src/materials/ShaderMaterial.js:286

Serializes the material into JSON.

#### Parameters

##### meta

`any`

An optional value holding meta information about the serialization.

#### Returns

`any`

A JSON object representing the serialized material.

#### See

[ObjectLoader#parse](Three.ObjectLoader.html#parse)

#### Overrides

[`Material`](Three.Material.html).[`toJSON`](Three.Material.html#tojson)
