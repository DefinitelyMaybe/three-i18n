---
editUrl: false
next: false
prev: false
title: "ConeGeometry"
---

Defined in: three/src/geometries/ConeGeometry.js:15

A geometry class for representing a cone.

```js
const geometry = new THREE.ConeGeometry( 5, 20, 32 );
const material = new THREE.MeshBasicMaterial( { color: 0xffff00 } );
const cone = new THREE.Mesh(geometry, material );
scene.add( cone );
```

## Extends

- [`CylinderGeometry`](Three.CylinderGeometry.html)

## Constructors

### Constructor

> **new ConeGeometry**(`radius`?, `height`?, `radialSegments`?, `heightSegments`?, `openEnded`?, `thetaStart`?, `thetaLength`?): `ConeGeometry`

Defined in: three/src/geometries/ConeGeometry.js:29

Constructs a new cone geometry.

#### Parameters

##### radius?

`number` = `1`

Radius of the cone base.

##### height?

`number` = `1`

Height of the cone.

##### radialSegments?

`number` = `32`

Number of segmented faces around the circumference of the cone.

##### heightSegments?

`number` = `1`

Number of rows of faces along the height of the cone.

##### openEnded?

`boolean` = `false`

Whether the base of the cone is open or capped.

##### thetaStart?

`number` = `0`

Start angle for first segment, in radians.

##### thetaLength?

`number` = `...`

The central angle, often called theta, of the circular sector, in radians.
The default value results in a complete cone.

#### Returns

`ConeGeometry`

#### Overrides

[`CylinderGeometry`](Three.CylinderGeometry.html).[`constructor`](Three.CylinderGeometry.html#constructor)

## Properties

### \_listeners

> **\_listeners**: `object`

Defined in: three/src/core/EventDispatcher.js:33

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`_listeners`](Three.CylinderGeometry.html#_listeners)

***

### attributes

> **attributes**: `object`

Defined in: three/src/core/BufferGeometry.js:121

This dictionary has as id the name of the attribute to be set and as value
the buffer attribute to set it to. Rather than accessing this property directly,
use `setAttribute()` and `getAttribute()` to access attributes of this geometry.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`attributes`](Three.CylinderGeometry.html#attributes)

***

### boundingBox

> **boundingBox**: [`Box3`](Three.Box3.html)

Defined in: three/src/core/BufferGeometry.js:162

Bounding box for the geometry which can be calculated with `computeBoundingBox()`.

#### Default

```ts
null
```

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`boundingBox`](Three.CylinderGeometry.html#boundingbox)

***

### boundingSphere

> **boundingSphere**: [`Sphere`](Three.Sphere.html)

Defined in: three/src/core/BufferGeometry.js:170

Bounding sphere for the geometry which can be calculated with `computeBoundingSphere()`.

#### Default

```ts
null
```

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`boundingSphere`](Three.CylinderGeometry.html#boundingsphere)

***

### drawRange

> **drawRange**: `object`

Defined in: three/src/core/BufferGeometry.js:178

Determines the part of the geometry to render. This should not be set directly,
instead use `setDrawRange()`.

#### count

> **count**: `number`

#### start

> **start**: `number`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`drawRange`](Three.CylinderGeometry.html#drawrange)

***

### groups

> **groups**: `any`[]

Defined in: three/src/core/BufferGeometry.js:154

Split the geometry into groups, each of which will be rendered in a
separate draw call. This allows an array of materials to be used with the geometry.

Use `addGroup()` and `clearGroups()` to edit groups, rather than modifying this array directly.

Every vertex and index must belong to exactly one group — groups must not share vertices or
indices, and must not leave vertices or indices unused.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`groups`](Three.CylinderGeometry.html#groups)

***

### index

> **index**: [`BufferAttribute`](Three.BufferAttribute.html)

Defined in: three/src/core/BufferGeometry.js:101

Allows for vertices to be re-used across multiple triangles; this is
called using "indexed triangles". Each triangle is associated with the
indices of three vertices. This attribute therefore stores the index of
each vertex for each triangular face. If this attribute is not set, the
renderer assumes that each three contiguous positions represent a single triangle.

#### Default

```ts
null
```

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`index`](Three.CylinderGeometry.html#index)

***

### indirect

> **indirect**: [`BufferAttribute`](Three.BufferAttribute.html)

Defined in: three/src/core/BufferGeometry.js:112

A (storage) buffer attribute which was generated with a compute shader and
now defines indirect draw calls.

Can only be used with WebGPURenderer and a WebGPU backend.

#### Default

```ts
null
```

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`indirect`](Three.CylinderGeometry.html#indirect)

***

### isBufferGeometry

> `readonly` **isBufferGeometry**: `boolean`

Defined in: three/src/core/BufferGeometry.js:64

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`isBufferGeometry`](Three.CylinderGeometry.html#isbuffergeometry)

***

### morphAttributes

> **morphAttributes**: `any`

Defined in: three/src/core/BufferGeometry.js:131

This dictionary holds the morph targets of the geometry.

Note: Once the geometry has been rendered, the morph attribute data cannot
be changed. You will have to call `dispose()?, and create a new geometry instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`morphAttributes`](Three.CylinderGeometry.html#morphattributes)

***

### morphTargetsRelative

> **morphTargetsRelative**: `boolean`

Defined in: three/src/core/BufferGeometry.js:141

Used to control the morph target behavior; when set to `true`, the morph
target data is treated as relative offsets, rather than as absolute
positions/normals.

#### Default

```ts
false
```

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`morphTargetsRelative`](Three.CylinderGeometry.html#morphtargetsrelative)

***

### name

> **name**: `string`

Defined in: three/src/core/BufferGeometry.js:88

The name of the geometry.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`name`](Three.CylinderGeometry.html#name)

***

### parameters

> **parameters**: `any`

Defined in: three/src/geometries/CylinderGeometry.js:46

Holds the constructor parameters that have been
used to generate the geometry. Any modification
after instantiation does not change the geometry.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`parameters`](Three.CylinderGeometry.html#parameters)

***

### type

> **type**: `string`

Defined in: three/src/geometries/ConeGeometry.js:33

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`type`](Three.CylinderGeometry.html#type)

***

### userData

> **userData**: `any`

Defined in: three/src/core/BufferGeometry.js:186

An object that can be used to store custom data about the geometry.
It should not hold references to functions as these will not be cloned.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`userData`](Three.CylinderGeometry.html#userdata)

***

### uuid

> `readonly` **uuid**: `string`

Defined in: three/src/core/BufferGeometry.js:81

The UUID of the geometry.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`uuid`](Three.CylinderGeometry.html#uuid)

## Methods

### addEventListener()

> **addEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:31

Adds the given event listener to the given event type.

#### Parameters

##### type

`string`

The type of event to listen to.

##### listener

`Function`

The function that gets called when the event is fired.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`addEventListener`](Three.CylinderGeometry.html#addeventlistener)

***

### addGroup()

> **addGroup**(`start`, `count`, `materialIndex`?): `void`

Defined in: three/src/core/BufferGeometry.js:310

Adds a group to this geometry.

#### Parameters

##### start

`number`

The first element in this draw call. That is the first
vertex for non-indexed geometry, otherwise the first triangle index.

##### count

`number`

Specifies how many vertices (or indices) are part of this group.

##### materialIndex?

`number` = `0`

The material array index to use.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`addGroup`](Three.CylinderGeometry.html#addgroup)

***

### applyMatrix4()

> **applyMatrix4**(`matrix`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:351

Applies the given 4x4 transformation matrix to the geometry.

#### Parameters

##### matrix

[`Matrix4`](Three.Matrix4.html)

The matrix to apply.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`applyMatrix4`](Three.CylinderGeometry.html#applymatrix4)

***

### applyQuaternion()

> **applyQuaternion**(`q`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:407

Applies the rotation represented by the Quaternion to the geometry.

#### Parameters

##### q

`Quaternion`

The Quaternion to apply.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`applyQuaternion`](Three.CylinderGeometry.html#applyquaternion)

***

### center()

> **center**(): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:546

Center the geometry based on its bounding box.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`center`](Three.CylinderGeometry.html#center)

***

### clearGroups()

> **clearGroups**(): `void`

Defined in: three/src/core/BufferGeometry.js:325

Clears all groups.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`clearGroups`](Three.CylinderGeometry.html#cleargroups)

***

### clone()

> **clone**(): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:1318

Returns a new geometry with copied values from this instance.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A clone of this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`clone`](Three.CylinderGeometry.html#clone)

***

### computeBoundingBox()

> **computeBoundingBox**(): `void`

Defined in: three/src/core/BufferGeometry.js:616

Computes the bounding box of the geometry, and updates the `boundingBox` member.
The bounding box is not computed by the engine; it must be computed by your app.
You may need to recompute the bounding box if the geometry vertices are modified.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`computeBoundingBox`](Three.CylinderGeometry.html#computeboundingbox)

***

### computeBoundingSphere()

> **computeBoundingSphere**(): `void`

Defined in: three/src/core/BufferGeometry.js:691

Computes the bounding sphere of the geometry, and updates the `boundingSphere` member.
The engine automatically computes the bounding sphere when it is needed, e.g., for ray casting or view frustum culling.
You may need to recompute the bounding sphere if the geometry vertices are modified.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`computeBoundingSphere`](Three.CylinderGeometry.html#computeboundingsphere)

***

### computeTangents()

> **computeTangents**(): `void`

Defined in: three/src/core/BufferGeometry.js:810

Calculates and adds a tangent attribute to this geometry.

The computation is only supported for indexed geometries and if position, normal, and uv attributes
are defined. When using a tangent space normal map, prefer the MikkTSpace algorithm provided by
BufferGeometryUtils#computeMikkTSpaceTangents instead.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`computeTangents`](Three.CylinderGeometry.html#computetangents)

***

### computeVertexNormals()

> **computeVertexNormals**(): `void`

Defined in: three/src/core/BufferGeometry.js:975

Computes vertex normals for the given vertex data. For indexed geometries, the method sets
each vertex normal to be the average of the face normals of the faces that share that vertex.
For non-indexed geometries, vertices are not shared, and the method sets each vertex normal
to be the same as the face normal.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`computeVertexNormals`](Three.CylinderGeometry.html#computevertexnormals)

***

### copy()

> **copy**(`source`): `ConeGeometry`

Defined in: three/src/geometries/CylinderGeometry.js:306

Copies the values of the given geometry to this instance.

#### Parameters

##### source

`any`

The geometry to copy.

#### Returns

`ConeGeometry`

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`copy`](Three.CylinderGeometry.html#copy)

***

### deleteAttribute()

> **deleteAttribute**(`name`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:282

Deletes the attribute for the given name.

#### Parameters

##### name

`string`

The attribute name to delete.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`deleteAttribute`](Three.CylinderGeometry.html#deleteattribute)

***

### dispatchEvent()

> **dispatchEvent**(`event`): `void`

Defined in: three/src/core/EventDispatcher.js:101

Dispatches an event object.

#### Parameters

##### event

`any`

The event that gets fired.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`dispatchEvent`](Three.CylinderGeometry.html#dispatchevent)

***

### dispose()

> **dispose**(): `void`

Defined in: three/src/core/BufferGeometry.js:1441

Frees the GPU-related resources allocated by this instance. Call this
method whenever this instance is no longer used in your app.

#### Returns

`void`

#### Fires

BufferGeometry#dispose

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`dispose`](Three.CylinderGeometry.html#dispose)

***

### getAttribute()

> **getAttribute**(`name`): `any`

Defined in: three/src/core/BufferGeometry.js:255

Returns the buffer attribute for the given name.

#### Parameters

##### name

`string`

The attribute name.

#### Returns

`any`

The buffer attribute.
Returns `undefined` if not attribute has been found.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`getAttribute`](Three.CylinderGeometry.html#getattribute)

***

### getIndex()

> **getIndex**(): [`BufferAttribute`](Three.BufferAttribute.html)

Defined in: three/src/core/BufferGeometry.js:195

Returns the index of this geometry.

#### Returns

[`BufferAttribute`](Three.BufferAttribute.html)

The index. Returns `null` if no index is defined.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`getIndex`](Three.CylinderGeometry.html#getindex)

***

### getIndirect()

> **getIndirect**(): [`BufferAttribute`](Three.BufferAttribute.html)

Defined in: three/src/core/BufferGeometry.js:242

Returns the indirect attribute of this geometry.

#### Returns

[`BufferAttribute`](Three.BufferAttribute.html)

The indirect attribute. Returns `null` if no indirect attribute is defined.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`getIndirect`](Three.CylinderGeometry.html#getindirect)

***

### hasAttribute()

> **hasAttribute**(`name`): `boolean`

Defined in: three/src/core/BufferGeometry.js:296

Returns `true` if this geometry has an attribute for the given name.

#### Parameters

##### name

`string`

The attribute name.

#### Returns

`boolean`

Whether this geometry has an attribute for the given name or not.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`hasAttribute`](Three.CylinderGeometry.html#hasattribute)

***

### hasEventListener()

> **hasEventListener**(`type`, `listener`): `boolean`

Defined in: three/src/core/EventDispatcher.js:58

Returns `true` if the given event listener has been added to the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to check.

#### Returns

`boolean`

Whether the given event listener has been added to the given event type.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`hasEventListener`](Three.CylinderGeometry.html#haseventlistener)

***

### lookAt()

> **lookAt**(`vector`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:529

Rotates the geometry to face a point in 3D space. This is typically done as a one time
operation, and not during a loop. Use [Object3D#lookAt](Three.Object3D.html#lookat) for typical
real-time mesh rotation.

#### Parameters

##### vector

[`Vector3`](Three.Vector3.html)

The target point.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`lookAt`](Three.CylinderGeometry.html#lookat)

***

### normalizeNormals()

> **normalizeNormals**(): `void`

Defined in: three/src/core/BufferGeometry.js:1071

Ensures every normal vector in a geometry will have a magnitude of `1`. This will
correct lighting on the geometry surfaces.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`normalizeNormals`](Three.CylinderGeometry.html#normalizenormals)

***

### removeEventListener()

> **removeEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:74

Removes the given event listener from the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to remove.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`removeEventListener`](Three.CylinderGeometry.html#removeeventlistener)

***

### rotateX()

> **rotateX**(`angle`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:425

Rotates the geometry about the X axis. This is typically done as a one time
operation, and not during a loop. Use Object3D#rotation for typical
real-time mesh rotation.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`rotateX`](Three.CylinderGeometry.html#rotatex)

***

### rotateY()

> **rotateY**(`angle`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:445

Rotates the geometry about the Y axis. This is typically done as a one time
operation, and not during a loop. Use Object3D#rotation for typical
real-time mesh rotation.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`rotateY`](Three.CylinderGeometry.html#rotatey)

***

### rotateZ()

> **rotateZ**(`angle`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:465

Rotates the geometry about the Z axis. This is typically done as a one time
operation, and not during a loop. Use Object3D#rotation for typical
real-time mesh rotation.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`rotateZ`](Three.CylinderGeometry.html#rotatez)

***

### scale()

> **scale**(`x`, `y`, `z`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:509

Scales the geometry. This is typically done as a one time
operation, and not during a loop. Use Object3D#scale for typical
real-time mesh rotation.

#### Parameters

##### x

`number`

The x scale.

##### y

`number`

The y scale.

##### z

`number`

The z scale.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`scale`](Three.CylinderGeometry.html#scale)

***

### setAttribute()

> **setAttribute**(`name`, `attribute`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:268

Sets the given attribute for the given name.

#### Parameters

##### name

`string`

The attribute name.

##### attribute

`any`

The attribute to set.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`setAttribute`](Three.CylinderGeometry.html#setattribute)

***

### setDrawRange()

> **setDrawRange**(`start`, `count`): `void`

Defined in: three/src/core/BufferGeometry.js:338

Sets the draw range for this geometry.

#### Parameters

##### start

`number`

The first vertex for non-indexed geometry, otherwise the first triangle index.

##### count

`number`

For non-indexed BufferGeometry, `count` is the number of vertices to render.
For indexed BufferGeometry, `count` is the number of indices to render.

#### Returns

`void`

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`setDrawRange`](Three.CylinderGeometry.html#setdrawrange)

***

### setFromPoints()

> **setFromPoints**(`points`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:569

Defines a geometry by creating a `position` attribute based on the given array of points. The array
can hold 2D or 3D vectors. When using two-dimensional data, the `z` coordinate for all vertices is
set to `0`.

If the method is used with an existing `position` attribute, the vertex data are overwritten with the
data from the array. The length of the array must match the vertex count.

#### Parameters

##### points

The points.

[`Vector3`](Three.Vector3.html)[] | [`Vector2`](Three.Vector2.html)[]

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`setFromPoints`](Three.CylinderGeometry.html#setfrompoints)

***

### setIndex()

> **setIndex**(`index`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:207

Sets the given index to this geometry.

#### Parameters

##### index

The index to set.

`number`[] | [`BufferAttribute`](Three.BufferAttribute.html)

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`setIndex`](Three.CylinderGeometry.html#setindex)

***

### setIndirect()

> **setIndirect**(`indirect`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:229

Sets the given indirect attribute to this geometry.

#### Parameters

##### indirect

[`BufferAttribute`](Three.BufferAttribute.html)

The attribute holding indirect draw calls.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`setIndirect`](Three.CylinderGeometry.html#setindirect)

***

### toJSON()

> **toJSON**(): `any`

Defined in: three/src/core/BufferGeometry.js:1200

Serializes the geometry into JSON.

#### Returns

`any`

A JSON object representing the serialized geometry.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`toJSON`](Three.CylinderGeometry.html#tojson)

***

### toNonIndexed()

> **toNonIndexed**(): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:1093

Return a new non-index version of this indexed geometry. If the geometry
is already non-indexed, the method is a NOOP.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

The non-indexed version of this indexed geometry.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`toNonIndexed`](Three.CylinderGeometry.html#tononindexed)

***

### translate()

> **translate**(`x`, `y`, `z`): [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/core/BufferGeometry.js:487

Translates the geometry. This is typically done as a one time
operation, and not during a loop. Use Object3D#position for typical
real-time mesh rotation.

#### Parameters

##### x

`number`

The x offset.

##### y

`number`

The y offset.

##### z

`number`

The z offset.

#### Returns

[`BufferGeometry`](Three.BufferGeometry.html)

A reference to this instance.

#### Inherited from

[`CylinderGeometry`](Three.CylinderGeometry.html).[`translate`](Three.CylinderGeometry.html#translate)

***

### fromJSON()

> `static` **fromJSON**(`data`): `ConeGeometry`

Defined in: three/src/geometries/ConeGeometry.js:61

Factory method for creating an instance of this class from the given
JSON object.

#### Parameters

##### data

`any`

A JSON object representing the serialized geometry.

#### Returns

`ConeGeometry`

A new instance.

#### Overrides

[`CylinderGeometry`](Three.CylinderGeometry.html).[`fromJSON`](Three.CylinderGeometry.html#fromjson)
