---
editUrl: false
next: false
prev: false
title: "VideoFrameTexture"
---

Defined in: three/src/textures/VideoFrameTexture.js:16

This class can be used as an alternative way to define video data. Instead of using
an instance of `HTMLVideoElement` like with `VideoTexture`, `VideoFrameTexture` expects each frame is
defined manually via [VideoFrameTexture#setFrame](#setframe). A typical use case for this module is when
video frames are decoded with the WebCodecs API.

```js
const texture = new THREE.VideoFrameTexture();
texture.setFrame( frame );
```

## Extends

- [`VideoTexture`](Three.VideoTexture.html)

## Constructors

### Constructor

> **new VideoFrameTexture**(`mapping`?, `wrapS`?, `wrapT`?, `magFilter`?, `minFilter`?, `format`?, `type`?, `anisotropy`?): `VideoFrameTexture`

Defined in: three/src/textures/VideoFrameTexture.js:30

Constructs a new video frame texture.

#### Parameters

##### mapping?

`number`

The texture mapping.

##### wrapS?

`number`

The wrapS value.

##### wrapT?

`number`

The wrapT value.

##### magFilter?

`number`

The mag filter value.

##### minFilter?

`number`

The min filter value.

##### format?

`number`

The texture format.

##### type?

`number`

The texture type.

##### anisotropy?

`number`

The anisotropy value.

#### Returns

`VideoFrameTexture`

#### Overrides

[`VideoTexture`](Three.VideoTexture.html).[`constructor`](Three.VideoTexture.html#constructor)

## Properties

### \_listeners

> **\_listeners**: `object`

Defined in: three/src/core/EventDispatcher.js:33

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`_listeners`](Three.VideoTexture.html#_listeners)

***

### anisotropy

> **anisotropy**: `number`

Defined in: three/src/textures/Texture.js:159

The number of samples taken along the axis through the pixel that has the
highest density of texels. By default, this value is `1`. A higher value
gives a less blurry result than a basic mipmap, at the cost of more
texture samples being used.

#### Default

```ts
0
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`anisotropy`](Three.VideoTexture.html#anisotropy)

***

### center

> **center**: [`Vector2`](Three.Vector2.html)

Defined in: three/src/textures/Texture.js:215

The point around which rotation occurs. A value of `(0.5, 0.5)` corresponds
to the center of the texture. Default is `(0, 0)`, the lower left.

#### Default

```ts
(0,0)
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`center`](Three.VideoTexture.html#center)

***

### channel

> **channel**: `number`

Defined in: three/src/textures/Texture.js:114

Lets you select the uv attribute to map the texture to. `0` for `uv`,
`1` for `uv1`, `2` for `uv2` and `3` for `uv3`.

#### Default

```ts
0
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`channel`](Three.VideoTexture.html#channel)

***

### colorSpace

> **colorSpace**: `string`

Defined in: three/src/textures/Texture.js:295

Textures containing color data should be annotated with `SRGBColorSpace` or `LinearSRGBColorSpace`.

#### Default

```ts
NoColorSpace
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`colorSpace`](Three.VideoTexture.html#colorspace)

***

### flipY

> **flipY**: `boolean`

Defined in: three/src/textures/Texture.js:277

If set to `true`, the texture is flipped along the vertical axis when
uploaded to the GPU.

Note that this property has no effect when using `ImageBitmap`. You need to
configure the flip on bitmap creation instead.

#### Default

```ts
true
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`flipY`](Three.VideoTexture.html#flipy)

***

### format

> **format**: `number`

Defined in: three/src/textures/Texture.js:167

The format of the texture.

#### Default

```ts
RGBAFormat
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`format`](Three.VideoTexture.html#format)

***

### generateMipmaps

> **generateMipmaps**: `boolean`

Defined in: three/src/textures/Texture.js:253

Whether to generate mipmaps (if possible) for a texture.

Set this to `false` if you are creating mipmaps manually.

#### Default

```ts
true
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`generateMipmaps`](Three.VideoTexture.html#generatemipmaps)

***

### internalFormat

> **internalFormat**: `string`

Defined in: three/src/textures/Texture.js:178

The default internal format is derived from [Texture#format](Three.Texture.html#format) and [Texture#type](Three.Texture.html#type) and
defines how the texture data is going to be stored on the GPU.

This property allows to overwrite the default format.

#### Default

```ts
null
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`internalFormat`](Three.VideoTexture.html#internalformat)

***

### isRenderTargetTexture

> `readonly` **isRenderTargetTexture**: `boolean`

Defined in: three/src/textures/Texture.js:338

Indicates whether a texture belongs to a render target or not.

#### Default

```ts
false
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`isRenderTargetTexture`](Three.VideoTexture.html#isrendertargettexture)

***

### isTexture

> `readonly` **isTexture**: `boolean`

Defined in: three/src/textures/Texture.js:55

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`isTexture`](Three.VideoTexture.html#istexture)

***

### isVideoFrameTexture

> `readonly` **isVideoFrameTexture**: `boolean`

Defined in: three/src/textures/VideoFrameTexture.js:41

This flag can be used for type testing.

#### Default

```ts
true
```

***

### isVideoTexture

> `readonly` **isVideoTexture**: `boolean`

Defined in: three/src/textures/VideoTexture.js:44

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`isVideoTexture`](Three.VideoTexture.html#isvideotexture)

***

### magFilter

> **magFilter**: `any`

Defined in: three/src/textures/Texture.js:140

How the texture is sampled when a texel covers more than one pixel.

#### Default

```ts
LinearFilter
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`magFilter`](Three.VideoTexture.html#magfilter)

***

### mapping

> **mapping**: `any`

Defined in: three/src/textures/Texture.js:105

How the texture is applied to the object. The value `UVMapping`
is the default, where texture or uv coordinates are used to apply the map.

#### Default

```ts
UVMapping
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`mapping`](Three.VideoTexture.html#mapping)

***

### matrix

> **matrix**: [`Matrix3`](Three.Matrix3.html)

Defined in: three/src/textures/Texture.js:243

The uv-transformation matrix of the texture.

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`matrix`](Three.VideoTexture.html#matrix)

***

### matrixAutoUpdate

> **matrixAutoUpdate**: `boolean`

Defined in: three/src/textures/Texture.js:236

Whether to update the texture's uv-transformation [Texture#matrix](Three.Texture.html#matrix)
from the properties [Texture#offset](Three.Texture.html#offset), [Texture#repeat](Three.Texture.html#repeat),
[Texture#rotation](Three.Texture.html#rotation), and [Texture#center](Three.Texture.html#center).

Set this to `false` if you are specifying the uv-transform matrix directly.

#### Default

```ts
true
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`matrixAutoUpdate`](Three.VideoTexture.html#matrixautoupdate)

***

### minFilter

> **minFilter**: `any`

Defined in: three/src/textures/Texture.js:148

How the texture is sampled when a texel covers less than one pixel.

#### Default

```ts
LinearMipmapLinearFilter
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`minFilter`](Three.VideoTexture.html#minfilter)

***

### mipmaps

> **mipmaps**: `any`[]

Defined in: three/src/textures/Texture.js:96

An array holding user-defined mipmaps.

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`mipmaps`](Three.VideoTexture.html#mipmaps)

***

### name

> **name**: `string`

Defined in: three/src/textures/Texture.js:79

The name of the material.

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`name`](Three.VideoTexture.html#name)

***

### offset

> **offset**: [`Vector2`](Three.Vector2.html)

Defined in: three/src/textures/Texture.js:195

How much a single repetition of the texture is offset from the beginning,
in each direction U and V. Typical range is `0.0` to `1.0`.

#### Default

```ts
(0,0)
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`offset`](Three.VideoTexture.html#offset)

***

### onUpdate

> **onUpdate**: `Function`

Defined in: three/src/textures/Texture.js:321

A callback function, called when the texture is updated (e.g., when
[Texture#needsUpdate](Three.Texture.html#needsupdate) has been set to true and then the texture is used).

#### Default

```ts
null
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`onUpdate`](Three.VideoTexture.html#onupdate)

***

### pmremVersion

> `readonly` **pmremVersion**: `number`

Defined in: three/src/textures/Texture.js:348

Indicates whether this texture should be processed by `PMREMGenerator` or not
(only relevant for render target textures).

#### Default

```ts
0
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`pmremVersion`](Three.VideoTexture.html#pmremversion)

***

### premultiplyAlpha

> **premultiplyAlpha**: `boolean`

Defined in: three/src/textures/Texture.js:265

If set to `true`, the alpha channel, if present, is multiplied into the
color channels when the texture is uploaded to the GPU.

Note that this property has no effect when using `ImageBitmap`. You need to
configure premultiply alpha on bitmap creation instead.

#### Default

```ts
false
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`premultiplyAlpha`](Three.VideoTexture.html#premultiplyalpha)

***

### renderTarget

> **renderTarget**: `any`

Defined in: three/src/textures/Texture.js:329

An optional back reference to the textures render target.

#### Default

```ts
null
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`renderTarget`](Three.VideoTexture.html#rendertarget)

***

### repeat

> **repeat**: [`Vector2`](Three.Vector2.html)

Defined in: three/src/textures/Texture.js:206

How many times the texture is repeated across the surface, in each
direction U and V. If repeat is set greater than `1` in either direction,
the corresponding wrap parameter should also be set to `RepeatWrapping`
or `MirroredRepeatWrapping` to achieve the desired tiling effect.

#### Default

```ts
(1,1)
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`repeat`](Three.VideoTexture.html#repeat)

***

### rotation

> **rotation**: `number`

Defined in: three/src/textures/Texture.js:224

How much the texture is rotated around the center point, in radians.
Positive values are counter-clockwise.

#### Default

```ts
0
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`rotation`](Three.VideoTexture.html#rotation)

***

### source

> **source**: [`Source`](Three.Source.html)

Defined in: three/src/textures/Texture.js:89

The data definition of a texture. A reference to the data source can be
shared across textures. This is often useful in context of spritesheets
where multiple textures render the same data but with different texture
transformations.

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`source`](Three.VideoTexture.html#source)

***

### type

> **type**: `number`

Defined in: three/src/textures/Texture.js:186

The data type of the texture.

#### Default

```ts
UnsignedByteType
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`type`](Three.VideoTexture.html#type)

***

### unpackAlignment

> **unpackAlignment**: `number`

Defined in: three/src/textures/Texture.js:287

Specifies the alignment requirements for the start of each pixel row in memory.
The allowable values are `1` (byte-alignment), `2` (rows aligned to even-numbered bytes),
`4` (word-alignment), and `8` (rows start on double-word boundaries).

#### Default

```ts
4
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`unpackAlignment`](Three.VideoTexture.html#unpackalignment)

***

### userData

> **userData**: `any`

Defined in: three/src/textures/Texture.js:303

An object that can be used to store custom data about the texture. It
should not hold references to functions as these will not be cloned.

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`userData`](Three.VideoTexture.html#userdata)

***

### uuid

> `readonly` **uuid**: `string`

Defined in: three/src/textures/Texture.js:72

The UUID of the material.

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`uuid`](Three.VideoTexture.html#uuid)

***

### version

> `readonly` **version**: `number`

Defined in: three/src/textures/Texture.js:312

This starts at `0` and counts how many times [Texture#needsUpdate](Three.Texture.html#needsupdate) is set to `true`.

#### Default

```ts
0
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`version`](Three.VideoTexture.html#version)

***

### wrapS

> **wrapS**: `number`

Defined in: three/src/textures/Texture.js:123

This defines how the texture is wrapped horizontally and corresponds to
*U* in UV mapping.

#### Default

```ts
ClampToEdgeWrapping
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`wrapS`](Three.VideoTexture.html#wraps)

***

### wrapT

> **wrapT**: `number`

Defined in: three/src/textures/Texture.js:132

This defines how the texture is wrapped horizontally and corresponds to
*V* in UV mapping.

#### Default

```ts
ClampToEdgeWrapping
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`wrapT`](Three.VideoTexture.html#wrapt)

***

### DEFAULT\_ANISOTROPY

> `static` **DEFAULT\_ANISOTROPY**: `number`

Defined in: three/src/textures/Texture.js:681

The default anisotropy value for all textures.

#### Static

#### Default

```ts
1
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`DEFAULT_ANISOTROPY`](Three.VideoTexture.html#default_anisotropy)

***

### DEFAULT\_IMAGE()

> `static` **DEFAULT\_IMAGE**: (`width`?, `height`?) => `HTMLImageElement`

Defined in: three/src/textures/Texture.js:663

The default image for all textures.

#### Parameters

##### width?

`number`

##### height?

`number`

#### Returns

`HTMLImageElement`

#### Static

#### Default

```ts
null
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`DEFAULT_IMAGE`](Three.VideoTexture.html#default_image)

***

### DEFAULT\_MAPPING

> `static` **DEFAULT\_MAPPING**: `number`

Defined in: three/src/textures/Texture.js:672

The default mapping for all textures.

#### Static

#### Default

```ts
UVMapping
```

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`DEFAULT_MAPPING`](Three.VideoTexture.html#default_mapping)

## Accessors

### image

#### Get Signature

> **get** **image**(): `any`

Defined in: three/src/textures/Texture.js:357

The image object holding the texture data.

##### Returns

`any`

#### Set Signature

> **set** **image**(`value`): `void`

Defined in: three/src/textures/Texture.js:363

##### Parameters

###### value

`any` = `null`

##### Returns

`void`

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`image`](Three.VideoTexture.html#image)

***

### needsPMREMUpdate

#### Set Signature

> **set** **needsPMREMUpdate**(`value`): `void`

Defined in: three/src/textures/Texture.js:644

Setting this property to `true` indicates the engine the PMREM
must be regenerated.

##### Default

```ts
false
```

##### Parameters

###### value

`boolean`

##### Returns

`void`

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`needsPMREMUpdate`](Three.VideoTexture.html#needspmremupdate)

***

### needsUpdate

#### Set Signature

> **set** **needsUpdate**(`value`): `void`

Defined in: three/src/textures/Texture.js:625

Setting this property to `true` indicates the engine the texture
must be updated in the next render. This triggers a texture upload
to the GPU and ensures correct texture parameter configuration.

##### Default

```ts
false
```

##### Parameters

###### value

`boolean`

##### Returns

`void`

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`needsUpdate`](Three.VideoTexture.html#needsupdate)

## Methods

### addEventListener()

> **addEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:31

Adds the given event listener to the given event type.

#### Parameters

##### type

`string`

The type of event to listen to.

##### listener

`Function`

The function that gets called when the event is fired.

#### Returns

`void`

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`addEventListener`](Three.VideoTexture.html#addeventlistener)

***

### clone()

> **clone**(): `any`

Defined in: three/src/textures/VideoFrameTexture.js:51

Returns a new texture with copied values from this instance.

#### Returns

`any`

A clone of this instance.

#### Overrides

[`VideoTexture`](Three.VideoTexture.html).[`clone`](Three.VideoTexture.html#clone)

***

### copy()

> **copy**(`source`): [`Texture`](Three.Texture.html)

Defined in: three/src/textures/Texture.js:396

Copies the values of the given texture to this instance.

#### Parameters

##### source

[`Texture`](Three.Texture.html)

The texture to copy.

#### Returns

[`Texture`](Three.Texture.html)

A reference to this instance.

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`copy`](Three.VideoTexture.html#copy)

***

### dispatchEvent()

> **dispatchEvent**(`event`): `void`

Defined in: three/src/core/EventDispatcher.js:101

Dispatches an event object.

#### Parameters

##### event

`any`

The event that gets fired.

#### Returns

`void`

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`dispatchEvent`](Three.VideoTexture.html#dispatchevent)

***

### dispose()

> **dispose**(): `void`

Defined in: three/src/textures/Texture.js:518

Frees the GPU-related resources allocated by this instance. Call this
method whenever this instance is no longer used in your app.

#### Returns

`void`

#### Fires

Texture#dispose

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`dispose`](Three.VideoTexture.html#dispose)

***

### hasEventListener()

> **hasEventListener**(`type`, `listener`): `boolean`

Defined in: three/src/core/EventDispatcher.js:58

Returns `true` if the given event listener has been added to the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to check.

#### Returns

`boolean`

Whether the given event listener has been added to the given event type.

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`hasEventListener`](Three.VideoTexture.html#haseventlistener)

***

### removeEventListener()

> **removeEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:74

Removes the given event listener from the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to remove.

#### Returns

`void`

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`removeEventListener`](Three.VideoTexture.html#removeeventlistener)

***

### setFrame()

> **setFrame**(`frame`): `void`

Defined in: three/src/textures/VideoFrameTexture.js:63

Sets the current frame of the video. This will automatically update the texture
so the data can be used for rendering.

#### Parameters

##### frame

`VideoFrame`

The video frame.

#### Returns

`void`

***

### toJSON()

> **toJSON**(`meta`): `any`

Defined in: three/src/textures/Texture.js:450

Serializes the texture into JSON.

#### Parameters

##### meta

`any`

An optional value holding meta information about the serialization.

#### Returns

`any`

A JSON object representing the serialized texture.

#### See

[ObjectLoader#parse](Three.ObjectLoader.html#parse)

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`toJSON`](Three.VideoTexture.html#tojson)

***

### transformUv()

> **transformUv**(`uv`): [`Vector2`](Three.Vector2.html)

Defined in: three/src/textures/Texture.js:536

Transforms the given uv vector with the textures uv transformation matrix.

#### Parameters

##### uv

[`Vector2`](Three.Vector2.html)

The uv vector.

#### Returns

[`Vector2`](Three.Vector2.html)

The transformed uv vector.

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`transformUv`](Three.VideoTexture.html#transformuv)

***

### update()

> **update**(): `void`

Defined in: three/src/textures/VideoFrameTexture.js:49

This method overwritten with an empty implementation since
this type of texture is updated via `setFrame()`.

#### Returns

`void`

#### Overrides

[`VideoTexture`](Three.VideoTexture.html).[`update`](Three.VideoTexture.html#update)

***

### updateMatrix()

> **updateMatrix**(): `void`

Defined in: three/src/textures/Texture.js:373

Updates the texture transformation matrix from the from the properties [Texture#offset](Three.Texture.html#offset),
[Texture#repeat](Three.Texture.html#repeat), [Texture#rotation](Three.Texture.html#rotation), and [Texture#center](Three.Texture.html#center).

#### Returns

`void`

#### Inherited from

[`VideoTexture`](Three.VideoTexture.html).[`updateMatrix`](Three.VideoTexture.html#updatematrix)
