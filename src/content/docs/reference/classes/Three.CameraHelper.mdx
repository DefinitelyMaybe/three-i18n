---
editUrl: false
next: false
prev: false
title: "CameraHelper"
---

Defined in: three/src/helpers/CameraHelper.js:29

This helps with visualizing what a camera contains in its frustum. It
visualizes the frustum of a camera using a line segments.

Based on frustum visualization in [lightgl.js shadowmap example][https://github.com/evanw/lightgl.js/blob/master/tests/shadowmap.html](https://github.com/evanw/lightgl.js/blob/master/tests/shadowmap.html).

`CameraHelper` must be a child of the scene.

```js
const camera = new THREE.PerspectiveCamera( 75, window.innerWidth / window.innerHeight, 0.1, 1000 );
const helper = new THREE.CameraHelper( camera );
scene.add( helper );
```

## Extends

- [`LineSegments`](Three.LineSegments.html)

## Constructors

### Constructor

> **new CameraHelper**(`camera`): `CameraHelper`

Defined in: three/src/helpers/CameraHelper.js:36

Constructs a new arrow helper.

#### Parameters

##### camera

[`Camera`](Three.Camera.html)

The camera to visualize.

#### Returns

`CameraHelper`

#### Overrides

[`LineSegments`](Three.LineSegments.html).[`constructor`](Three.LineSegments.html#constructor)

## Properties

### \_listeners

> **\_listeners**: `object`

Defined in: three/src/core/EventDispatcher.js:33

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`_listeners`](Three.LineSegments.html#_listeners)

***

### animations

> **animations**: `AnimationClip`[]

Defined in: three/src/core/Object3D.js:331

An array holding the animation clips of the 3D object.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`animations`](Three.LineSegments.html#animations)

***

### camera

> **camera**: [`Camera`](Three.Camera.html)

Defined in: three/src/helpers/CameraHelper.js:127

The camera being visualized.

***

### castShadow

> **castShadow**: `boolean`

Defined in: three/src/core/Object3D.js:296

When set to `true`, the 3D object gets rendered into shadow maps.

#### Default

```ts
false
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`castShadow`](Three.LineSegments.html#castshadow)

***

### children

> **children**: [`Object3D`](Three.Object3D.html)[]

Defined in: three/src/core/Object3D.js:127

An array holding the child 3D objects of this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`children`](Three.LineSegments.html#children)

***

### customDepthMaterial

> **customDepthMaterial**: `any`

Defined in: three/src/core/Object3D.js:344

Custom depth material to be used when rendering to the depth map. Can only be used
in context of meshes. When shadow-casting with a [DirectionalLight](Three.DirectionalLight.html) or [SpotLight](Three.SpotLight.html),
if you are modifying vertex positions in the vertex shader you must specify a custom depth
material for proper shadows.

Only relevant in context of [WebGLRenderer](Three.WebGLRenderer.html).

#### Default

```ts
undefined
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`customDepthMaterial`](Three.LineSegments.html#customdepthmaterial)

***

### customDistanceMaterial

> **customDistanceMaterial**: `any`

Defined in: three/src/core/Object3D.js:354

Same as [Object3D#customDepthMaterial](Three.Object3D.html#customdepthmaterial), but used with [PointLight](Three.PointLight.html).

Only relevant in context of [WebGLRenderer](Three.WebGLRenderer.html).

#### Default

```ts
undefined
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`customDistanceMaterial`](Three.LineSegments.html#customdistancematerial)

***

### frustumCulled

> **frustumCulled**: `boolean`

Defined in: three/src/core/Object3D.js:312

When set to `true`, the 3D object is honored by view frustum culling.

#### Default

```ts
true
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`frustumCulled`](Three.LineSegments.html#frustumculled)

***

### geometry

> **geometry**: [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/objects/Line.js:68

The line geometry.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`geometry`](Three.LineSegments.html#geometry)

***

### isLine

> `readonly` **isLine**: `boolean`

Defined in: three/src/objects/Line.js:59

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`isLine`](Three.LineSegments.html#isline)

***

### isLineSegments

> `readonly` **isLineSegments**: `boolean`

Defined in: three/src/objects/LineSegments.js:32

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`isLineSegments`](Three.LineSegments.html#islinesegments)

***

### isObject3D

> `readonly` **isObject3D**: `boolean`

Defined in: three/src/core/Object3D.js:79

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`isObject3D`](Three.LineSegments.html#isobject3d)

***

### layers

> **layers**: [`Layers`](Three.Layers.html)

Defined in: three/src/core/Object3D.js:280

The layer membership of the 3D object. The 3D object is only visible if it has
at least one layer in common with the camera in use. This property can also be
used to filter out unwanted objects in ray-intersection tests when using [Raycaster](Three.Raycaster.html).

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`layers`](Three.LineSegments.html#layers)

***

### material

> **material**: `any`

Defined in: three/src/objects/Line.js:76

The line material.

#### Default

```ts
LineBasicMaterial
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`material`](Three.LineSegments.html#material)

***

### matrix

> **matrix**: [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/helpers/CameraHelper.js:130

Represents the object's transformation matrix in local space.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`matrix`](Three.LineSegments.html#matrix)

***

### matrixAutoUpdate

> **matrixAutoUpdate**: `boolean`

Defined in: three/src/helpers/CameraHelper.js:131

When set to `true`, the engine automatically computes the local matrix from position,
rotation and scale every frame.

The default values for all 3D objects is defined by `Object3D.DEFAULT_MATRIX_AUTO_UPDATE`.

#### Default

```ts
true
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`matrixAutoUpdate`](Three.LineSegments.html#matrixautoupdate)

***

### matrixWorld

> **matrixWorld**: [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/core/Object3D.js:240

Represents the object's transformation matrix in world space.
If the 3D object has no parent, then it's identical to the local transformation matrix

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`matrixWorld`](Three.LineSegments.html#matrixworld)

***

### matrixWorldAutoUpdate

> **matrixWorldAutoUpdate**: `boolean`

Defined in: three/src/core/Object3D.js:262

When set to `true`, the engine automatically computes the world matrix from the current local
matrix and the object's transformation hierarchy.

The default values for all 3D objects is defined by `Object3D.DEFAULT_MATRIX_WORLD_AUTO_UPDATE`.

#### Default

```ts
true
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`matrixWorldAutoUpdate`](Three.LineSegments.html#matrixworldautoupdate)

***

### matrixWorldNeedsUpdate

> **matrixWorldNeedsUpdate**: `boolean`

Defined in: three/src/core/Object3D.js:271

When set to `true`, it calculates the world matrix in that frame and resets this property
to `false`.

#### Default

```ts
false
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`matrixWorldNeedsUpdate`](Three.LineSegments.html#matrixworldneedsupdate)

***

### morphTargetDictionary

> **morphTargetDictionary**: `any`

Defined in: three/src/objects/Line.js:86

A dictionary representing the morph targets in the geometry. The key is the
morph targets name, the value its attribute index. This member is `undefined`
by default and only set when morph targets are detected in the geometry.

#### Default

```ts
undefined
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`morphTargetDictionary`](Three.LineSegments.html#morphtargetdictionary)

***

### morphTargetInfluences

> **morphTargetInfluences**: `number`[]

Defined in: three/src/objects/Line.js:96

An array of weights typically in the range `[0,1]` that specify how much of the morph
is applied. This member is `undefined` by default and only set when morph targets are
detected in the geometry.

#### Default

```ts
undefined
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`morphTargetInfluences`](Three.LineSegments.html#morphtargetinfluences)

***

### name

> **name**: `string`

Defined in: three/src/core/Object3D.js:103

The name of the 3D object.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`name`](Three.LineSegments.html#name)

***

### parent

> **parent**: [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:120

A reference to the parent object.

#### Default

```ts
null
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`parent`](Three.LineSegments.html#parent)

***

### pointMap

> **pointMap**: `object`

Defined in: three/src/helpers/CameraHelper.js:138

This contains the points used to visualize the camera.

***

### receiveShadow

> **receiveShadow**: `boolean`

Defined in: three/src/core/Object3D.js:304

When set to `true`, the 3D object is affected by shadows in the scene.

#### Default

```ts
false
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`receiveShadow`](Three.LineSegments.html#receiveshadow)

***

### renderOrder

> **renderOrder**: `number`

Defined in: three/src/core/Object3D.js:324

This value allows the default rendering order of scene graph objects to be
overridden although opaque and transparent objects remain sorted independently.
When this property is set for an instance of [Group](Three.Group.html),all descendants
objects will be sorted and rendered together. Sorting is from lowest to highest
render order.

#### Default

```ts
0
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`renderOrder`](Three.LineSegments.html#renderorder)

***

### type

> **type**: `string`

Defined in: three/src/helpers/CameraHelper.js:120

The type property is used for detecting the object type
in context of serialization/deserialization.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`type`](Three.LineSegments.html#type)

***

### up

> **up**: [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:137

Defines the `up` direction of the 3D object which influences
the orientation via methods like [Object3D#lookAt](Three.Object3D.html#lookat).

The default values for all 3D objects is defined by `Object3D.DEFAULT_UP`.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`up`](Three.LineSegments.html#up)

***

### userData

> **userData**: `any`

Defined in: three/src/core/Object3D.js:362

An object that can be used to store custom data about the 3D object. It
should not hold references to functions as these will not be cloned.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`userData`](Three.LineSegments.html#userdata)

***

### uuid

> `readonly` **uuid**: `string`

Defined in: three/src/core/Object3D.js:96

The UUID of the 3D object.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`uuid`](Three.LineSegments.html#uuid)

***

### visible

> **visible**: `boolean`

Defined in: three/src/core/Object3D.js:288

When set to `true`, the 3D object gets rendered.

#### Default

```ts
true
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`visible`](Three.LineSegments.html#visible)

***

### DEFAULT\_MATRIX\_AUTO\_UPDATE

> `static` **DEFAULT\_MATRIX\_AUTO\_UPDATE**: `boolean`

Defined in: three/src/core/Object3D.js:1605

The default setting for [Object3D#matrixAutoUpdate](Three.Object3D.html#matrixautoupdate) for
newly created 3D objects.

#### Static

#### Default

```ts
true
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`DEFAULT_MATRIX_AUTO_UPDATE`](Three.LineSegments.html#default_matrix_auto_update)

***

### DEFAULT\_MATRIX\_WORLD\_AUTO\_UPDATE

> `static` **DEFAULT\_MATRIX\_WORLD\_AUTO\_UPDATE**: `boolean`

Defined in: three/src/core/Object3D.js:1615

The default setting for [Object3D#matrixWorldAutoUpdate](Three.Object3D.html#matrixworldautoupdate) for
newly created 3D objects.

#### Static

#### Default

```ts
true
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`DEFAULT_MATRIX_WORLD_AUTO_UPDATE`](Three.LineSegments.html#default_matrix_world_auto_update)

***

### DEFAULT\_UP

> `static` **DEFAULT\_UP**: [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:1595

The default up direction for objects, also used as the default
position for [DirectionalLight](Three.DirectionalLight.html) and [HemisphereLight](Three.HemisphereLight.html).

#### Static

#### Default

```ts
(0,1,0)
```

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`DEFAULT_UP`](Three.LineSegments.html#default_up)

## Methods

### add()

> **add**(`object`, ...`args`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:720

Adds the given 3D object as a child to this 3D object. An arbitrary number of
objects may be added. Any current parent on an object passed in here will be
removed, since an object can have at most one parent.

#### Parameters

##### object

[`Object3D`](Three.Object3D.html)

The 3D object to add.

##### args

...`any`

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#added

#### Fires

Object3D#childadded

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`add`](Three.LineSegments.html#add)

***

### addEventListener()

> **addEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:31

Adds the given event listener to the given event type.

#### Parameters

##### type

`string`

The type of event to listen to.

##### listener

`Function`

The function that gets called when the event is fired.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`addEventListener`](Three.LineSegments.html#addeventlistener)

***

### applyMatrix4()

> **applyMatrix4**(`matrix`): `void`

Defined in: three/src/core/Object3D.js:422

Applies the given transformation matrix to the object and updates the object's position,
rotation and scale.

#### Parameters

##### matrix

[`Matrix4`](Three.Matrix4.html)

The transformation matrix.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`applyMatrix4`](Three.LineSegments.html#applymatrix4)

***

### applyQuaternion()

> **applyQuaternion**(`q`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:438

Applies a rotation represented by given the quaternion to the 3D object.

#### Parameters

##### q

[`Quaternion`](Three.Quaternion.html)

The quaternion.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`applyQuaternion`](Three.LineSegments.html#applyquaternion)

***

### attach()

> **attach**(`object`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:848

Adds the given 3D object as a child of this 3D object, while maintaining the object's world
transform. This method does not support scene graphs having non-uniformly-scaled nodes(s).

#### Parameters

##### object

[`Object3D`](Three.Object3D.html)

The 3D object to attach.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#added

#### Fires

Object3D#childadded

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`attach`](Three.LineSegments.html#attach)

***

### clear()

> **clear**(): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:833

Removes all child objects.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#removed

#### Fires

Object3D#childremoved

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`clear`](Three.LineSegments.html#clear)

***

### clone()

> **clone**(`recursive`?): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:1525

Returns a new 3D object with copied values from this instance.

#### Parameters

##### recursive?

`boolean`

When set to `true`, descendants of the 3D object are also cloned.

#### Returns

[`Object3D`](Three.Object3D.html)

A clone of this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`clone`](Three.LineSegments.html#clone)

***

### computeLineDistances()

> **computeLineDistances**(): `CameraHelper`

Defined in: three/src/objects/LineSegments.js:38

Computes an array of distance values which are necessary for rendering dashed lines.
For each vertex in the geometry, the method calculates the cumulative length from the
current point to the very beginning of the line.

#### Returns

`CameraHelper`

A reference to this line.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`computeLineDistances`](Three.LineSegments.html#computelinedistances)

***

### copy()

> **copy**(`source`, `recursive`): `CameraHelper`

Defined in: three/src/objects/Line.js:102

Copies the values of the given 3D object to this instance.

#### Parameters

##### source

`any`

The 3D object to copy.

##### recursive

`any`

When set to `true`, descendants of the 3D object are cloned.

#### Returns

`CameraHelper`

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`copy`](Three.LineSegments.html#copy)

***

### dispatchEvent()

> **dispatchEvent**(`event`): `void`

Defined in: three/src/core/EventDispatcher.js:101

Dispatches an event object.

#### Parameters

##### event

`any`

The event that gets fired.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`dispatchEvent`](Three.LineSegments.html#dispatchevent)

***

### dispose()

> **dispose**(): `void`

Defined in: three/src/helpers/CameraHelper.js:282

Frees the GPU-related resources allocated by this instance. Call this
method whenever this instance is no longer used in your app.

#### Returns

`void`

***

### getObjectById()

> **getObjectById**(`id`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:891

Searches through the 3D object and its children, starting with the 3D object
itself, and returns the first with a matching ID.

#### Parameters

##### id

`number`

The id.

#### Returns

[`Object3D`](Three.Object3D.html)

The found 3D object. Returns `undefined` if no 3D object has been found.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`getObjectById`](Three.LineSegments.html#getobjectbyid)

***

### getObjectByName()

> **getObjectByName**(`name`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:904

Searches through the 3D object and its children, starting with the 3D object
itself, and returns the first with a matching name.

#### Parameters

##### name

`string`

The name.

#### Returns

[`Object3D`](Three.Object3D.html)

The found 3D object. Returns `undefined` if no 3D object has been found.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`getObjectByName`](Three.LineSegments.html#getobjectbyname)

***

### getObjectByProperty()

> **getObjectByProperty**(`name`, `value`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:918

Searches through the 3D object and its children, starting with the 3D object
itself, and returns the first with a matching property value.

#### Parameters

##### name

`string`

The name of the property.

##### value

`any`

The value.

#### Returns

[`Object3D`](Three.Object3D.html)

The found 3D object. Returns `undefined` if no 3D object has been found.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`getObjectByProperty`](Three.LineSegments.html#getobjectbyproperty)

***

### getObjectsByProperty()

> **getObjectsByProperty**(`name`, `value`, `result`): [`Object3D`](Three.Object3D.html)[]

Defined in: three/src/core/Object3D.js:948

Searches through the 3D object and its children, starting with the 3D object
itself, and returns all 3D objects with a matching property value.

#### Parameters

##### name

`string`

The name of the property.

##### value

`any`

The value.

##### result

[`Object3D`](Three.Object3D.html)[] = `[]`

The method stores the result in this array.

#### Returns

[`Object3D`](Three.Object3D.html)[]

The found 3D objects.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`getObjectsByProperty`](Three.LineSegments.html#getobjectsbyproperty)

***

### getWorldDirection()

> **getWorldDirection**(`target`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:1016

Returns a vector representing the ("look") direction of the 3D object in world space.

#### Parameters

##### target

[`Vector3`](Three.Vector3.html)

The target vector the result is stored to.

#### Returns

[`Vector3`](Three.Vector3.html)

The 3D object's direction in world space.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`getWorldDirection`](Three.LineSegments.html#getworlddirection)

***

### getWorldPosition()

> **getWorldPosition**(`target`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:970

Returns a vector representing the position of the 3D object in world space.

#### Parameters

##### target

[`Vector3`](Three.Vector3.html)

The target vector the result is stored to.

#### Returns

[`Vector3`](Three.Vector3.html)

The 3D object's position in world space.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`getWorldPosition`](Three.LineSegments.html#getworldposition)

***

### getWorldQuaternion()

> **getWorldQuaternion**(`target`): [`Quaternion`](Three.Quaternion.html)

Defined in: three/src/core/Object3D.js:984

Returns a Quaternion representing the position of the 3D object in world space.

#### Parameters

##### target

[`Quaternion`](Three.Quaternion.html)

The target Quaternion the result is stored to.

#### Returns

[`Quaternion`](Three.Quaternion.html)

The 3D object's rotation in world space.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`getWorldQuaternion`](Three.LineSegments.html#getworldquaternion)

***

### getWorldScale()

> **getWorldScale**(`target`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:1000

Returns a vector representing the scale of the 3D object in world space.

#### Parameters

##### target

[`Vector3`](Three.Vector3.html)

The target vector the result is stored to.

#### Returns

[`Vector3`](Three.Vector3.html)

The 3D object's scale in world space.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`getWorldScale`](Three.LineSegments.html#getworldscale)

***

### hasEventListener()

> **hasEventListener**(`type`, `listener`): `boolean`

Defined in: three/src/core/EventDispatcher.js:58

Returns `true` if the given event listener has been added to the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to check.

#### Returns

`boolean`

Whether the given event listener has been added to the given event type.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`hasEventListener`](Three.LineSegments.html#haseventlistener)

***

### localToWorld()

> **localToWorld**(`vector`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:637

Converts the given vector from this 3D object's local space to world space.

#### Parameters

##### vector

[`Vector3`](Three.Vector3.html)

The vector to convert.

#### Returns

[`Vector3`](Three.Vector3.html)

The converted vector.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`localToWorld`](Three.LineSegments.html#localtoworld)

***

### lookAt()

> **lookAt**(`x`, `y`?, `z`?): `void`

Defined in: three/src/core/Object3D.js:668

Rotates the object to face a point in world space.

This method does not support objects having non-uniformly-scaled parent(s).

#### Parameters

##### x

The x coordinate in world space. Alternatively, a vector representing a position in world space

`number` | [`Vector3`](Three.Vector3.html)

##### y?

`number`

The y coordinate in world space.

##### z?

`number`

The z coordinate in world space.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`lookAt`](Three.LineSegments.html#lookat)

***

### onAfterRender()

> **onAfterRender**(): `void`

Defined in: three/src/core/Object3D.js:414

A callback that is executed immediately after a 3D object is rendered.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`onAfterRender`](Three.LineSegments.html#onafterrender)

***

### onAfterShadow()

> **onAfterShadow**(): `void`

Defined in: three/src/core/Object3D.js:390

A callback that is executed immediately after a 3D object is rendered to a shadow map.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`onAfterShadow`](Three.LineSegments.html#onaftershadow)

***

### onBeforeRender()

> **onBeforeRender**(): `void`

Defined in: three/src/core/Object3D.js:402

A callback that is executed immediately before a 3D object is rendered.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`onBeforeRender`](Three.LineSegments.html#onbeforerender)

***

### onBeforeShadow()

> **onBeforeShadow**(): `void`

Defined in: three/src/core/Object3D.js:377

A callback that is executed immediately before a 3D object is rendered to a shadow map.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`onBeforeShadow`](Three.LineSegments.html#onbeforeshadow)

***

### raycast()

> **raycast**(`raycaster`, `intersects`): `void`

Defined in: three/src/objects/Line.js:159

Computes intersection points between a casted ray and this line.

#### Parameters

##### raycaster

`Raycaster`

The raycaster.

##### intersects

`any`[]

The target array that holds the intersection points.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`raycast`](Three.LineSegments.html#raycast)

***

### remove()

> **remove**(`object`, ...`args`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:772

Removes the given 3D object as child from this 3D object.
An arbitrary number of objects may be removed.

#### Parameters

##### object

[`Object3D`](Three.Object3D.html)

The 3D object to remove.

##### args

...`any`

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#removed

#### Fires

Object3D#childremoved

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`remove`](Three.LineSegments.html#remove)

***

### removeEventListener()

> **removeEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:74

Removes the given event listener from the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to remove.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`removeEventListener`](Three.LineSegments.html#removeeventlistener)

***

### removeFromParent()

> **removeFromParent**(): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:812

Removes this 3D object from its current parent.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#removed

#### Fires

Object3D#childremoved

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`removeFromParent`](Three.LineSegments.html#removefromparent)

***

### rotateOnAxis()

> **rotateOnAxis**(`axis`, `angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:505

Rotates the 3D object along an axis in local space.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`rotateOnAxis`](Three.LineSegments.html#rotateonaxis)

***

### rotateOnWorldAxis()

> **rotateOnWorldAxis**(`axis`, `angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:525

Rotates the 3D object along an axis in world space.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`rotateOnWorldAxis`](Three.LineSegments.html#rotateonworldaxis)

***

### rotateX()

> **rotateX**(`angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:545

Rotates the 3D object around its X axis in local space.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`rotateX`](Three.LineSegments.html#rotatex)

***

### rotateY()

> **rotateY**(`angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:557

Rotates the 3D object around its Y axis in local space.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`rotateY`](Three.LineSegments.html#rotatey)

***

### rotateZ()

> **rotateZ**(`angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:569

Rotates the 3D object around its Z axis in local space.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`rotateZ`](Three.LineSegments.html#rotatez)

***

### setColors()

> **setColors**(`frustum`, `cone`, `up`, `target`, `cross`): `void`

Defined in: three/src/helpers/CameraHelper.js:163

Defines the colors of the helper.

#### Parameters

##### frustum

[`Color`](Three.Color.html)

The frustum line color.

##### cone

[`Color`](Three.Color.html)

The cone line color.

##### up

[`Color`](Three.Color.html)

The up line color.

##### target

[`Color`](Three.Color.html)

The target line color.

##### cross

[`Color`](Three.Color.html)

The cross line color.

#### Returns

`void`

***

### setRotationFromAxisAngle()

> **setRotationFromAxisAngle**(`axis`, `angle`): `void`

Defined in: three/src/core/Object3D.js:452

Sets the given rotation represented as an axis/angle couple to the 3D object.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### angle

`number`

The angle in radians.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`setRotationFromAxisAngle`](Three.LineSegments.html#setrotationfromaxisangle)

***

### setRotationFromEuler()

> **setRotationFromEuler**(`euler`): `void`

Defined in: three/src/core/Object3D.js:465

Sets the given rotation represented as Euler angles to the 3D object.

#### Parameters

##### euler

[`Euler`](Three.Euler.html)

The Euler angles.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`setRotationFromEuler`](Three.LineSegments.html#setrotationfromeuler)

***

### setRotationFromMatrix()

> **setRotationFromMatrix**(`m`): `void`

Defined in: three/src/core/Object3D.js:477

Sets the given rotation represented as rotation matrix to the 3D object.

#### Parameters

##### m

[`Matrix4`](Three.Matrix4.html)

Although a 4x4 matrix is expected, the upper 3x3 portion must be
a pure rotation matrix (i.e, unscaled).

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`setRotationFromMatrix`](Three.LineSegments.html#setrotationfrommatrix)

***

### setRotationFromQuaternion()

> **setRotationFromQuaternion**(`q`): `void`

Defined in: three/src/core/Object3D.js:490

Sets the given rotation represented as a Quaternion to the 3D object.

#### Parameters

##### q

[`Quaternion`](Three.Quaternion.html)

The Quaternion

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`setRotationFromQuaternion`](Three.LineSegments.html#setrotationfromquaternion)

***

### toJSON()

> **toJSON**(`meta`): `any`

Defined in: three/src/core/Object3D.js:1224

Serializes the 3D object into JSON.

#### Parameters

##### meta

`any`

An optional value holding meta information about the serialization.

#### Returns

`any`

A JSON object representing the serialized 3D object.

#### See

[ObjectLoader#parse](Three.ObjectLoader.html#parse)

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`toJSON`](Three.LineSegments.html#tojson)

***

### translateOnAxis()

> **translateOnAxis**(`axis`, `distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:582

Translate the 3D object by a distance along the given axis in local space.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`translateOnAxis`](Three.LineSegments.html#translateonaxis)

***

### translateX()

> **translateX**(`distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:601

Translate the 3D object by a distance along its X-axis in local space.

#### Parameters

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`translateX`](Three.LineSegments.html#translatex)

***

### translateY()

> **translateY**(`distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:613

Translate the 3D object by a distance along its Y-axis in local space.

#### Parameters

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`translateY`](Three.LineSegments.html#translatey)

***

### translateZ()

> **translateZ**(`distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:625

Translate the 3D object by a distance along its Z-axis in local space.

#### Parameters

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`translateZ`](Three.LineSegments.html#translatez)

***

### traverse()

> **traverse**(`callback`): `void`

Defined in: three/src/core/Object3D.js:1044

Executes the callback on this 3D object and all descendants.

Note: Modifying the scene graph inside the callback is discouraged.

#### Parameters

##### callback

`Function`

A callback function that allows to process the current 3D object.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`traverse`](Three.LineSegments.html#traverse)

***

### traverseAncestors()

> **traverseAncestors**(`callback`): `void`

Defined in: three/src/core/Object3D.js:1089

Like [Object3D#traverse](Three.Object3D.html#traverse), but the callback will only be executed for all ancestors.

Note: Modifying the scene graph inside the callback is discouraged.

#### Parameters

##### callback

`Function`

A callback function that allows to process the current 3D object.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`traverseAncestors`](Three.LineSegments.html#traverseancestors)

***

### traverseVisible()

> **traverseVisible**(`callback`): `void`

Defined in: three/src/core/Object3D.js:1066

Like [Object3D#traverse](Three.Object3D.html#traverse), but the callback will only be executed for visible 3D objects.
Descendants of invisible 3D objects are not traversed.

Note: Modifying the scene graph inside the callback is discouraged.

#### Parameters

##### callback

`Function`

A callback function that allows to process the current 3D object.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`traverseVisible`](Three.LineSegments.html#traversevisible)

***

### update()

> **update**(): `void`

Defined in: three/src/helpers/CameraHelper.js:223

Updates the helper based on the projection matrix of the camera.

#### Returns

`void`

***

### updateMatrix()

> **updateMatrix**(): `void`

Defined in: three/src/core/Object3D.js:1107

Updates the transformation matrix in local space by computing it from the current
position, rotation and scale values.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`updateMatrix`](Three.LineSegments.html#updatematrix)

***

### updateMatrixWorld()

> **updateMatrixWorld**(`force`?): `void`

Defined in: three/src/core/Object3D.js:1126

Updates the transformation matrix in world space of this 3D objects and its descendants.

To ensure correct results, this method also recomputes the 3D object's transformation matrix in
local space. The computation of the local and world matrix can be controlled with the
[Object3D#matrixAutoUpdate](Three.Object3D.html#matrixautoupdate) and [Object3D#matrixWorldAutoUpdate](Three.Object3D.html#matrixworldautoupdate) flags which are both
`true` by default.  Set these flags to `false` if you need more control over the update matrix process.

#### Parameters

##### force?

`boolean`

When set to `true`, a recomputation of world matrices is forced even
when [Object3D#matrixWorldAutoUpdate](Three.Object3D.html#matrixworldautoupdate) is set to `false`.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`updateMatrixWorld`](Three.LineSegments.html#updatematrixworld)

***

### updateMorphTargets()

> **updateMorphTargets**(): `void`

Defined in: three/src/objects/Line.js:262

Sets the values of [Line#morphTargetDictionary](Three.Line.html#morphtargetdictionary) and [Line#morphTargetInfluences](Three.Line.html#morphtargetinfluences)
to make sure existing morph targets can influence this 3D object.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`updateMorphTargets`](Three.LineSegments.html#updatemorphtargets)

***

### updateWorldMatrix()

> **updateWorldMatrix**(`updateParents`?, `updateChildren`?): `void`

Defined in: three/src/core/Object3D.js:1173

An alternative version of [Object3D#updateMatrixWorld](Three.Object3D.html#updatematrixworld) with more control over the
update of ancestor and descendant nodes.

#### Parameters

##### updateParents?

`boolean`

Whether ancestor nodes should be updated or not.

##### updateChildren?

`boolean`

Whether descendant nodes should be updated or not.

#### Returns

`void`

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`updateWorldMatrix`](Three.LineSegments.html#updateworldmatrix)

***

### worldToLocal()

> **worldToLocal**(`vector`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:651

Converts the given vector from this 3D object's word space to local space.

#### Parameters

##### vector

[`Vector3`](Three.Vector3.html)

The vector to convert.

#### Returns

[`Vector3`](Three.Vector3.html)

The converted vector.

#### Inherited from

[`LineSegments`](Three.LineSegments.html).[`worldToLocal`](Three.LineSegments.html#worldtolocal)
