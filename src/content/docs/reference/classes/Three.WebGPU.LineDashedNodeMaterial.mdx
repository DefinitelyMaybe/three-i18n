---
editUrl: false
next: false
prev: false
title: "LineDashedNodeMaterial"
---

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:16

Node material version of  [LineDashedMaterial](Three.LineDashedMaterial.html).

## Extends

- [`NodeMaterial`](Three.WebGPU.NodeMaterial.html)

## Constructors

### Constructor

> **new LineDashedNodeMaterial**(`parameters`?): `LineDashedNodeMaterial`

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:29

Constructs a new line dashed node material.

#### Parameters

##### parameters?

`any`

The configuration parameter.

#### Returns

`LineDashedNodeMaterial`

#### Overrides

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`constructor`](Three.WebGPU.NodeMaterial.html#constructor)

## Properties

### \_alphaTest

> **\_alphaTest**: `number`

Defined in: three/src/materials/Material.js:474

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`_alphaTest`](Three.WebGPU.NodeMaterial.html#_alphatest)

***

### \_listeners

> **\_listeners**: `object`

Defined in: three/src/core/EventDispatcher.js:33

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`_listeners`](Three.WebGPU.NodeMaterial.html#_listeners)

***

### allowOverride

> **allowOverride**: `boolean`

Defined in: three/src/materials/Material.js:436

Whether it's possible to override the material with [Scene#overrideMaterial](Three.Scene.html#overridematerial) or not.

#### Default

```ts
true
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`allowOverride`](Three.WebGPU.NodeMaterial.html#allowoverride)

***

### alphaHash

> **alphaHash**: `boolean`

Defined in: three/src/materials/Material.js:132

Enables alpha hashed transparency, an alternative to [Material#transparent](Three.Material.html#transparent) or
[Material#alphaTest](Three.Material.html#alphatest). The material will not be rendered if opacity is lower than
a random threshold. Randomization introduces some grain or noise, but approximates alpha
blending without the associated problems of sorting. Using TAA can reduce the resulting noise.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`alphaHash`](Three.WebGPU.NodeMaterial.html#alphahash)

***

### alphaTestNode

> **alphaTestNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:230

The alpha test of node materials is by default inferred from the `alphaTest`
property. This node property allows to overwrite the default and define the
alpha test with a node instead.

If you don't want to overwrite the alpha test but modify the existing
value instead, use [materialAlphaTest](../variables/Three.WebGPU.TSL.materialAlphaTest.html).

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`alphaTestNode`](Three.WebGPU.NodeMaterial.html#alphatestnode)

***

### alphaToCoverage

> **alphaToCoverage**: `boolean`

Defined in: three/src/materials/Material.js:406

Whether alpha to coverage should be enabled or not. Can only be used with MSAA-enabled contexts
(meaning when the renderer was created with *antialias* parameter set to `true`). Enabling this
will smooth aliasing on clip plane edges and alphaTest-clipped edges.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`alphaToCoverage`](Three.WebGPU.NodeMaterial.html#alphatocoverage)

***

### aoNode

> **aoNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:141

The lighting of node materials might be influenced by ambient occlusion.
The default AO is inferred from an ambient occlusion map assigned to `aoMap`
and the respective `aoMapIntensity`. This node property allows to overwrite
the default and define the ambient occlusion with a custom node instead.

If you don't want to overwrite the diffuse color but modify the existing
values instead, use [materialAO](../variables/Three.WebGPU.TSL.materialAO.html).

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`aoNode`](Three.WebGPU.NodeMaterial.html#aonode)

***

### backdropAlphaNode

> **backdropAlphaNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:217

This node allows to modulate the influence of `backdropNode` to the outgoing light.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`backdropAlphaNode`](Three.WebGPU.NodeMaterial.html#backdropalphanode)

***

### backdropNode

> **backdropNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:209

This node can be used to to implement a variety of filter-like effects. The idea is
to store the current rendering into a texture e.g. via `viewportSharedTexture()`, use it
to create an arbitrary effect and then assign the node composition to this property.
Everything behind the object using this material will now be affected by a filter.

```js
const material = new NodeMaterial()
material.transparent = true;

// everything behind the object will be monochromatic
material.backdropNode = saturation( viewportSharedTexture().rgb, 0 );
```

Backdrop computations are part of the lighting so only lit materials can use this property.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`backdropNode`](Three.WebGPU.NodeMaterial.html#backdropnode)

***

### blendAlpha

> **blendAlpha**: `number`

Defined in: three/src/materials/Material.js:200

Represents the alpha value of the constant blend color.

This property has only an effect when using custom blending with `ConstantAlpha` or `OneMinusConstantAlpha`.

#### Default

```ts
0
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`blendAlpha`](Three.WebGPU.NodeMaterial.html#blendalpha)

***

### blendColor

> **blendColor**: [`Color`](Three.Color.html)

Defined in: three/src/materials/Material.js:190

Represents the RGB values of the constant blend color.

This property has only an effect when using custom blending with `ConstantColor` or `OneMinusConstantColor`.

#### Default

```ts
(0,0,0)
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`blendColor`](Three.WebGPU.NodeMaterial.html#blendcolor)

***

### blendDst

> **blendDst**: `any`

Defined in: three/src/materials/Material.js:148

Defines the blending destination factor.

#### Default

```ts
OneMinusSrcAlphaFactor
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`blendDst`](Three.WebGPU.NodeMaterial.html#blenddst)

***

### blendDstAlpha

> **blendDstAlpha**: `any`

Defined in: three/src/materials/Material.js:172

Defines the blending destination alpha factor.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`blendDstAlpha`](Three.WebGPU.NodeMaterial.html#blenddstalpha)

***

### blendEquation

> **blendEquation**: `any`

Defined in: three/src/materials/Material.js:156

Defines the blending equation.

#### Default

```ts
OneMinusSrcAlphaFactor
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`blendEquation`](Three.WebGPU.NodeMaterial.html#blendequation)

***

### blendEquationAlpha

> **blendEquationAlpha**: `any`

Defined in: three/src/materials/Material.js:180

Defines the blending equation of the alpha channel.

#### Default

```ts
OneMinusSrcAlphaFactor
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`blendEquationAlpha`](Three.WebGPU.NodeMaterial.html#blendequationalpha)

***

### blending

> **blending**: `any`

Defined in: three/src/materials/Material.js:77

Defines the blending type of the material.

It must be set to `CustomBlending` if custom blending properties like
[Material#blendSrc](Three.Material.html#blendsrc), [Material#blendDst](Three.Material.html#blenddst) or [Material#blendEquation](Three.Material.html#blendequation)
should have any effect.

#### Default

```ts
NormalBlending
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`blending`](Three.WebGPU.NodeMaterial.html#blending)

***

### blendSrc

> **blendSrc**: `any`

Defined in: three/src/materials/Material.js:140

Defines the blending source factor.

#### Default

```ts
SrcAlphaFactor
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`blendSrc`](Three.WebGPU.NodeMaterial.html#blendsrc)

***

### blendSrcAlpha

> **blendSrcAlpha**: `any`

Defined in: three/src/materials/Material.js:164

Defines the blending source alpha factor.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`blendSrcAlpha`](Three.WebGPU.NodeMaterial.html#blendsrcalpha)

***

### castShadowNode

> **castShadowNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:322

This node can be used to influence how an object using this node material
casts shadows. To apply a color to shadows, you can simply do:

```js
material.castShadowNode = vec4( 1, 0, 0, 1 );
```

Which can be nice to fake colored shadows of semi-transparent objects. It
is also common to use the property with `Fn` function so checks are performed
per fragment.

```js
materialCustomShadow.castShadowNode = Fn( () => {
	hash( vertexIndex ).greaterThan( 0.5 ).discard();
	return materialColor;
} )();
 ```

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`castShadowNode`](Three.WebGPU.NodeMaterial.html#castshadownode)

***

### clipIntersection

> **clipIntersection**: `boolean`

Defined in: three/src/materials/Material.js:317

Changes the behavior of clipping planes so that only their intersection is
clipped, rather than their union.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`clipIntersection`](Three.WebGPU.NodeMaterial.html#clipintersection)

***

### clippingPlanes

> **clippingPlanes**: `Plane`[]

Defined in: three/src/materials/Material.js:308

User-defined clipping planes specified as THREE.Plane objects in world
space. These planes apply to the objects this material is attached to.
Points in space whose signed distance to the plane is negative are clipped
(not rendered). This requires WebGLRenderer#localClippingEnabled to
be `true`.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`clippingPlanes`](Three.WebGPU.NodeMaterial.html#clippingplanes)

***

### clipShadows

> **clipShadows**: `boolean`

Defined in: three/src/materials/Material.js:326

Defines whether to clip shadows according to the clipping planes specified
on this material.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`clipShadows`](Three.WebGPU.NodeMaterial.html#clipshadows)

***

### colorNode

> **colorNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:162

The diffuse color of node materials is by default inferred from the
`color` and `map` properties. This node property allows to overwrite the default
and define the diffuse color with a node instead.

```js
material.colorNode = color( 0xff0000 ); // define red color
```

If you don't want to overwrite the diffuse color but modify the existing
values instead, use [materialColor](../variables/Three.WebGPU.TSL.materialColor.html).

```js
material.colorNode = materialColor.mul( color( 0xff0000 ) ); // give diffuse colors a red tint
```

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`colorNode`](Three.WebGPU.NodeMaterial.html#colornode)

***

### colorWrite

> **colorWrite**: `boolean`

Defined in: three/src/materials/Material.js:350

Whether to render the material's color.

This can be used in conjunction with Object3D#renderOder to create invisible
objects that occlude other objects.

#### Default

```ts
true
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`colorWrite`](Three.WebGPU.NodeMaterial.html#colorwrite)

***

### dashOffset

> **dashOffset**: `number`

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:50

The dash offset.

#### Default

```ts
0
```

***

### dashScaleNode

> **dashScaleNode**: `any`

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:76

The scale of dash materials is by default inferred from the `scale`
property. This node property allows to overwrite the default
and define the scale with a node instead.

If you don't want to overwrite the scale but modify the existing
value instead, use [materialLineScale](../variables/Three.WebGPU.TSL.materialLineScale.html).

#### Default

```ts
null
```

***

### dashSizeNode

> **dashSizeNode**: `any`

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:89

The dash size of dash materials is by default inferred from the `dashSize`
property. This node property allows to overwrite the default
and define the dash size with a node instead.

If you don't want to overwrite the dash size but modify the existing
value instead, use [materialLineDashSize](../variables/Three.WebGPU.TSL.materialLineDashSize.html).

#### Default

```ts
null
```

***

### depthFunc

> **depthFunc**: `any`

Defined in: three/src/materials/Material.js:208

Defines the depth function.

#### Default

```ts
LessEqualDepth
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`depthFunc`](Three.WebGPU.NodeMaterial.html#depthfunc)

***

### depthNode

> **depthNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:271

Allows to overwrite depth values in the fragment shader.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`depthNode`](Three.WebGPU.NodeMaterial.html#depthnode)

***

### depthTest

> **depthTest**: `boolean`

Defined in: three/src/materials/Material.js:217

Whether to have depth test enabled when rendering this material.
When the depth test is disabled, the depth write will also be implicitly disabled.

#### Default

```ts
true
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`depthTest`](Three.WebGPU.NodeMaterial.html#depthtest)

***

### depthWrite

> **depthWrite**: `boolean`

Defined in: three/src/materials/Material.js:228

Whether rendering this material has any effect on the depth buffer.

When drawing 2D overlays it can be useful to disable the depth writing in
order to layer several things together without creating z-index artifacts.

#### Default

```ts
true
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`depthWrite`](Three.WebGPU.NodeMaterial.html#depthwrite)

***

### dithering

> **dithering**: `boolean`

Defined in: three/src/materials/Material.js:396

Whether to apply dithering to the color to remove the appearance of banding.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`dithering`](Three.WebGPU.NodeMaterial.html#dithering)

***

### envNode

> **envNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:127

The environment of node materials can be defined by an environment
map assigned to the `envMap` property or by `Scene.environment`
if the node material is a PBR material. This node property allows to overwrite
the default behavior and define the environment with a custom node.

```js
material.envNode = pmremTexture( renderTarget.texture );
```

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`envNode`](Three.WebGPU.NodeMaterial.html#envnode)

***

### fog

> **fog**: `boolean`

Defined in: three/src/materials/nodes/NodeMaterial.js:76

Whether this material is affected by fog or not.

#### Default

```ts
true
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`fog`](Three.WebGPU.NodeMaterial.html#fog)

***

### forceSinglePass

> **forceSinglePass**: `boolean`

Defined in: three/src/materials/Material.js:428

Whether double-sided, transparent objects should be rendered with a single pass or not.

The engine renders double-sided, transparent objects with two draw calls (back faces first,
then front faces) to mitigate transparency artifacts. There are scenarios however where this
approach produces no quality gains but still doubles draw calls e.g. when rendering flat
vegetation like grass sprites. In these cases, set the `forceSinglePass` flag to `true` to
disable the two pass rendering to avoid performance issues.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`forceSinglePass`](Three.WebGPU.NodeMaterial.html#forcesinglepass)

***

### fragmentNode

> **fragmentNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:353

This node property can be used if you need complete freedom in implementing
the fragment shader. Assigning a node will replace the built-in material
logic used in the fragment stage.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`fragmentNode`](Three.WebGPU.NodeMaterial.html#fragmentnode)

***

### gapSizeNode

> **gapSizeNode**: `any`

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:102

The gap size of dash materials is by default inferred from the `gapSize`
property. This node property allows to overwrite the default
and define the gap size with a node instead.

If you don't want to overwrite the gap size but modify the existing
value instead, use [materialLineGapSize](../variables/Three.WebGPU.TSL.materialLineGapSize.html).

#### Default

```ts
null
```

***

### geometryNode

> **geometryNode**: `Function`

Defined in: three/src/materials/nodes/NodeMaterial.js:263

This node property is intended for logic which modifies geometry data once or per animation step.
Apps usually place such logic randomly in initialization routines or in the animation loop.
`geometryNode` is intended as a dedicated API so there is an intended spot where geometry modifications
can be implemented.

The idea is to assign a `Fn` definition that holds the geometry modification logic. A typical example
would be a GPU based particle system that provides a node material for usage on app level. The particle
simulation would be implemented as compute shaders and managed inside a `Fn` function. This function is
eventually assigned to `geometryNode`.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`geometryNode`](Three.WebGPU.NodeMaterial.html#geometrynode)

***

### hardwareClipping

> **hardwareClipping**: `boolean`

Defined in: three/src/materials/nodes/NodeMaterial.js:94

Whether this material uses hardware clipping or not.
This property is managed by the engine and should not be
modified by apps.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`hardwareClipping`](Three.WebGPU.NodeMaterial.html#hardwareclipping)

***

### isLineDashedNodeMaterial

> `readonly` **isLineDashedNodeMaterial**: `boolean`

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:40

This flag can be used for type testing.

#### Default

```ts
true
```

***

### isMaterial

> `readonly` **isMaterial**: `boolean`

Defined in: three/src/materials/Material.js:32

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`isMaterial`](Three.WebGPU.NodeMaterial.html#ismaterial)

***

### isNodeMaterial

> `readonly` **isNodeMaterial**: `boolean`

Defined in: three/src/materials/nodes/NodeMaterial.js:68

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`isNodeMaterial`](Three.WebGPU.NodeMaterial.html#isnodematerial)

***

### lights

> **lights**: `boolean`

Defined in: three/src/materials/nodes/NodeMaterial.js:84

Whether this material is affected by lights or not.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`lights`](Three.WebGPU.NodeMaterial.html#lights)

***

### lightsNode

> **lightsNode**: `LightsNode`

Defined in: three/src/materials/nodes/NodeMaterial.js:112

Node materials which set their `lights` property to `true`
are affected by all lights of the scene. Sometimes selective
lighting is wanted which means only _some_ lights in the scene
affect a material. This can be achieved by creating an instance
of [LightsNode](Three.WebGPU.LightsNode.html) with a list of selective
lights and assign the node to this property.

```js
const customLightsNode = lights( [ light1, light2 ] );
material.lightsNode = customLightsNode;
```

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`lightsNode`](Three.WebGPU.NodeMaterial.html#lightsnode)

***

### mrtNode

> **mrtNode**: `MRTNode`

Defined in: three/src/materials/nodes/NodeMaterial.js:343

MRT configuration is done on renderer or pass level. This node allows to
overwrite what values are written into MRT targets on material level. This
can be useful for implementing selective FX features that should only affect
specific objects.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`mrtNode`](Three.WebGPU.NodeMaterial.html#mrtnode)

***

### name

> **name**: `string`

Defined in: three/src/materials/Material.js:56

The name of the material.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`name`](Three.WebGPU.NodeMaterial.html#name)

***

### normalNode

> **normalNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:175

The normals of node materials are by default inferred from the `normalMap`/`normalScale`
or `bumpMap`/`bumpScale` properties. This node property allows to overwrite the default
and define the normals with a node instead.

If you don't want to overwrite the normals but modify the existing values instead,
use [materialNormal](../variables/Three.WebGPU.TSL.materialNormal.html).

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`normalNode`](Three.WebGPU.NodeMaterial.html#normalnode)

***

### offsetNode

> **offsetNode**: `any`

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:63

The offset of dash materials is by default inferred from the `dashOffset`
property. This node property allows to overwrite the default
and define the offset with a node instead.

If you don't want to overwrite the offset but modify the existing
value instead, use [materialLineDashOffset](../variables/Three.WebGPU.TSL.materialLineDashOffset.html).

#### Default

```ts
null
```

***

### opacity

> **opacity**: `number`

Defined in: three/src/materials/Material.js:108

Defines how transparent the material is.
A value of `0.0` indicates fully transparent, `1.0` is fully opaque.

If the [Material#transparent](Three.Material.html#transparent) is not set to `true`,
the material will remain fully opaque and this value will only affect its color.

#### Default

```ts
1
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`opacity`](Three.WebGPU.NodeMaterial.html#opacity)

***

### opacityNode

> **opacityNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:188

The opacity of node materials is by default inferred from the `opacity`
and `alphaMap` properties. This node property allows to overwrite the default
and define the opacity with a node instead.

If you don't want to overwrite the normals but modify the existing
value instead, use [materialOpacity](../variables/Three.WebGPU.TSL.materialOpacity.html).

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`opacityNode`](Three.WebGPU.NodeMaterial.html#opacitynode)

***

### outputNode

> **outputNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:332

This node can be used to define the final output of the material.

TODO: Explain the differences to `fragmentNode`.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`outputNode`](Three.WebGPU.NodeMaterial.html#outputnode)

***

### polygonOffset

> **polygonOffset**: `boolean`

Defined in: three/src/materials/Material.js:372

Whether to use polygon offset or not. When enabled, each fragment's depth value will
be offset after it is interpolated from the depth values of the appropriate vertices.
The offset is added before the depth test is performed and before the value is written
into the depth buffer.

Can be useful for rendering hidden-line images, for applying decals to surfaces, and for
rendering solids with highlighted edges.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`polygonOffset`](Three.WebGPU.NodeMaterial.html#polygonoffset)

***

### polygonOffsetFactor

> **polygonOffsetFactor**: `number`

Defined in: three/src/materials/Material.js:380

Specifies a scale factor that is used to create a variable depth offset for each polygon.

#### Default

```ts
0
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`polygonOffsetFactor`](Three.WebGPU.NodeMaterial.html#polygonoffsetfactor)

***

### polygonOffsetUnits

> **polygonOffsetUnits**: `number`

Defined in: three/src/materials/Material.js:388

Is multiplied by an implementation-specific value to create a constant depth offset.

#### Default

```ts
0
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`polygonOffsetUnits`](Three.WebGPU.NodeMaterial.html#polygonoffsetunits)

***

### positionNode

> **positionNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:247

The local vertex positions are computed based on multiple factors like the
attribute data, morphing or skinning. This node property allows to overwrite
the default and define local vertex positions with nodes instead.

If you don't want to overwrite the vertex positions but modify the existing
values instead, use [positionLocal](../variables/Three.WebGPU.TSL.positionLocal.html).

```js
material.positionNode = positionLocal.add( displace );
```

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`positionNode`](Three.WebGPU.NodeMaterial.html#positionnode)

***

### precision

> **precision**: `"highp"` \| `"mediump"` \| `"lowp"`

Defined in: three/src/materials/Material.js:358

Override the renderer's default precision for this material.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`precision`](Three.WebGPU.NodeMaterial.html#precision)

***

### premultipliedAlpha

> **premultipliedAlpha**: `boolean`

Defined in: three/src/materials/Material.js:414

Whether to premultiply the alpha (transparency) value.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`premultipliedAlpha`](Three.WebGPU.NodeMaterial.html#premultipliedalpha)

***

### receivedShadowNode

> **receivedShadowNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:298

This node can be used to influence how an object using this node material
receive shadows.

```js
const totalShadows = float( 1 ).toVar();
material.receivedShadowNode = Fn( ( [ shadow ] ) => {
	totalShadows.mulAssign( shadow );
	//return float( 1 ); // bypass received shadows
	return shadow.mix( color( 0xff0000 ), 1 ); // modify shadow color
} );

@type {?(Function|FunctionNode<vec4>)}
@default null

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`receivedShadowNode`](Three.WebGPU.NodeMaterial.html#receivedshadownode)

***

### shadowPositionNode

> **shadowPositionNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:281

Allows to overwrite the position used for shadow map rendering which
is by default positionWorld, the vertex position
in world space.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`shadowPositionNode`](Three.WebGPU.NodeMaterial.html#shadowpositionnode)

***

### shadowSide

> **shadowSide**: `any`

Defined in: three/src/materials/Material.js:339

Defines which side of faces cast shadows. If `null`, the side casting shadows
is determined as follows:

- When [Material#side](Three.Material.html#side) is set to `FrontSide`, the back side cast shadows.
- When [Material#side](Three.Material.html#side) is set to `BackSide`, the front side cast shadows.
- When [Material#side](Three.Material.html#side) is set to `DoubleSide`, both sides cast shadows.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`shadowSide`](Three.WebGPU.NodeMaterial.html#shadowside)

***

### side

> **side**: `any`

Defined in: three/src/materials/Material.js:85

Defines which side of faces will be rendered - front, back or both.

#### Default

```ts
FrontSide
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`side`](Three.WebGPU.NodeMaterial.html#side)

***

### stencilFail

> **stencilFail**: `any`

Defined in: three/src/materials/Material.js:268

Which stencil operation to perform when the comparison function returns `false`.

#### Default

```ts
KeepStencilOp
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`stencilFail`](Three.WebGPU.NodeMaterial.html#stencilfail)

***

### stencilFunc

> **stencilFunc**: `any`

Defined in: three/src/materials/Material.js:244

The stencil comparison function to use.

#### Default

```ts
AlwaysStencilFunc
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`stencilFunc`](Three.WebGPU.NodeMaterial.html#stencilfunc)

***

### stencilFuncMask

> **stencilFuncMask**: `number`

Defined in: three/src/materials/Material.js:260

The bit mask to use when comparing against the stencil buffer.

#### Default

```ts
0xff
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`stencilFuncMask`](Three.WebGPU.NodeMaterial.html#stencilfuncmask)

***

### stencilRef

> **stencilRef**: `number`

Defined in: three/src/materials/Material.js:252

The value to use when performing stencil comparisons or stencil operations.

#### Default

```ts
0
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`stencilRef`](Three.WebGPU.NodeMaterial.html#stencilref)

***

### stencilWrite

> **stencilWrite**: `boolean`

Defined in: three/src/materials/Material.js:296

Whether stencil operations are performed against the stencil buffer. In
order to perform writes or comparisons against the stencil buffer this
value must be `true`.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`stencilWrite`](Three.WebGPU.NodeMaterial.html#stencilwrite)

***

### stencilWriteMask

> **stencilWriteMask**: `number`

Defined in: three/src/materials/Material.js:236

The bit mask to use when writing to the stencil buffer.

#### Default

```ts
0xff
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`stencilWriteMask`](Three.WebGPU.NodeMaterial.html#stencilwritemask)

***

### stencilZFail

> **stencilZFail**: `any`

Defined in: three/src/materials/Material.js:277

Which stencil operation to perform when the comparison function returns
`true` but the depth test fails.

#### Default

```ts
KeepStencilOp
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`stencilZFail`](Three.WebGPU.NodeMaterial.html#stencilzfail)

***

### stencilZPass

> **stencilZPass**: `any`

Defined in: three/src/materials/Material.js:286

Which stencil operation to perform when the comparison function returns
`true` and the depth test passes.

#### Default

```ts
KeepStencilOp
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`stencilZPass`](Three.WebGPU.NodeMaterial.html#stencilzpass)

***

### toneMapped

> **toneMapped**: `boolean`

Defined in: three/src/materials/Material.js:455

Defines whether this material is tone mapped according to the renderer's tone mapping setting.

It is ignored when rendering to a render target or using post processing or when using
`WebGPURenderer`. In all these cases, all materials are honored by tone mapping.

#### Default

```ts
true
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`toneMapped`](Three.WebGPU.NodeMaterial.html#tonemapped)

***

### transparent

> **transparent**: `boolean`

Defined in: three/src/materials/Material.js:121

Defines whether this material is transparent. This has an effect on
rendering as transparent objects need special treatment and are rendered
after non-transparent objects.

When set to true, the extent to which the material is transparent is
controlled by [Material#opacity](Three.Material.html#opacity).

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`transparent`](Three.WebGPU.NodeMaterial.html#transparent)

***

### userData

> **userData**: `any`

Defined in: three/src/materials/Material.js:463

An object that can be used to store custom data about the Material. It
should not hold references to functions as these will not be cloned.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`userData`](Three.WebGPU.NodeMaterial.html#userdata)

***

### uuid

> `readonly` **uuid**: `string`

Defined in: three/src/materials/Material.js:49

The UUID of the material.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`uuid`](Three.WebGPU.NodeMaterial.html#uuid)

***

### version

> `readonly` **version**: `number`

Defined in: three/src/materials/Material.js:472

This starts at `0` and counts how many times [Material#needsUpdate](Three.Material.html#needsupdate) is set to `true`.

#### Default

```ts
0
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`version`](Three.WebGPU.NodeMaterial.html#version)

***

### vertexColors

> **vertexColors**: `boolean`

Defined in: three/src/materials/Material.js:96

If set to `true`, vertex colors should be used.

The engine supports RGB and RGBA vertex colors depending on whether a three (RGB) or
four (RGBA) component color buffer attribute is used.

#### Default

```ts
false
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`vertexColors`](Three.WebGPU.NodeMaterial.html#vertexcolors)

***

### vertexNode

> **vertexNode**: `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:363

This node property can be used if you need complete freedom in implementing
the vertex shader. Assigning a node will replace the built-in material logic
used in the vertex stage.

#### Default

```ts
null
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`vertexNode`](Three.WebGPU.NodeMaterial.html#vertexnode)

***

### visible

> **visible**: `boolean`

Defined in: three/src/materials/Material.js:444

Defines whether 3D objects using this material are visible.

#### Default

```ts
true
```

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`visible`](Three.WebGPU.NodeMaterial.html#visible)

## Accessors

### alphaTest

#### Get Signature

> **get** **alphaTest**(): `number`

Defined in: three/src/materials/Material.js:486

Sets the alpha value to be used when running an alpha test. The material
will not be rendered if the opacity is lower than this value.

##### Default

```ts
0
```

##### Returns

`number`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`alphaTest`](Three.WebGPU.NodeMaterial.html#alphatest)

***

### needsUpdate

#### Set Signature

> **set** **needsUpdate**(`value`): `void`

Defined in: three/src/materials/Material.js:994

Setting this property to `true` indicates the engine the material
needs to be recompiled.

##### Default

```ts
false
```

##### Parameters

###### value

`boolean`

##### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`needsUpdate`](Three.WebGPU.NodeMaterial.html#needsupdate)

***

### type

#### Get Signature

> **get** **type**(): `string`

Defined in: three/src/materials/nodes/NodeMaterial.js:46

Represents the type of the node material.

##### Returns

`string`

#### Set Signature

> **set** **type**(`_value`): `void`

Defined in: three/src/materials/nodes/NodeMaterial.js:52

The type property is used for detecting the object type
in context of serialization/deserialization.

##### Parameters

###### \_value

`string`

##### Returns

`void`

The type property is used for detecting the object type
in context of serialization/deserialization.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`type`](Three.WebGPU.NodeMaterial.html#type)

***

### type

#### Get Signature

> **get** `static` **type**(): `string`

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:18

##### Returns

`string`

#### Overrides

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`type`](Three.WebGPU.NodeMaterial.html#type-1)

## Methods

### addEventListener()

> **addEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:31

Adds the given event listener to the given event type.

#### Parameters

##### type

`string`

The type of event to listen to.

##### listener

`Function`

The function that gets called when the event is fired.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`addEventListener`](Three.WebGPU.NodeMaterial.html#addeventlistener)

***

### build()

> **build**(`builder`): `void`

Defined in: three/src/materials/nodes/NodeMaterial.js:384

Builds this material with the given node builder.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`build`](Three.WebGPU.NodeMaterial.html#build)

***

### clone()

> **clone**(): [`Material`](Three.Material.html)

Defined in: three/src/materials/Material.js:875

Returns a new material with copied values from this instance.

#### Returns

[`Material`](Three.Material.html)

A clone of this instance.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`clone`](Three.WebGPU.NodeMaterial.html#clone)

***

### copy()

> **copy**(`source`): [`NodeMaterial`](Three.WebGPU.NodeMaterial.html)

Defined in: three/src/materials/nodes/NodeMaterial.js:1152

Copies the properties of the given node material to this instance.

#### Parameters

##### source

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html)

The material to copy.

#### Returns

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html)

A reference to this node material.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`copy`](Three.WebGPU.NodeMaterial.html#copy)

***

### customProgramCacheKey()

> **customProgramCacheKey**(): `string`

Defined in: three/src/materials/nodes/NodeMaterial.js:373

Allows to define a custom cache key that influence the material key computation
for render objects.

#### Returns

`string`

The custom cache key.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`customProgramCacheKey`](Three.WebGPU.NodeMaterial.html#customprogramcachekey)

***

### dispatchEvent()

> **dispatchEvent**(`event`): `void`

Defined in: three/src/core/EventDispatcher.js:101

Dispatches an event object.

#### Parameters

##### event

`any`

The event that gets fired.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`dispatchEvent`](Three.WebGPU.NodeMaterial.html#dispatchevent)

***

### dispose()

> **dispose**(): `void`

Defined in: three/src/materials/Material.js:974

Frees the GPU-related resources allocated by this instance. Call this
method whenever this instance is no longer used in your app.

#### Returns

`void`

#### Fires

Material#dispose

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`dispose`](Three.WebGPU.NodeMaterial.html#dispose)

***

### hasEventListener()

> **hasEventListener**(`type`, `listener`): `boolean`

Defined in: three/src/core/EventDispatcher.js:58

Returns `true` if the given event listener has been added to the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to check.

#### Returns

`boolean`

Whether the given event listener has been added to the given event type.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`hasEventListener`](Three.WebGPU.NodeMaterial.html#haseventlistener)

***

### onBeforeCompile()

> **onBeforeCompile**(): `void`

Defined in: three/src/materials/Material.js:530

An optional callback that is executed immediately before the shader
program is compiled. This function is called with the shader source code
as a parameter. Useful for the modification of built-in materials.

This method can only be used when rendering with WebGLRenderer. The
recommended approach when customizing materials is to use `WebGPURenderer` with the new
Node Material system and [TSL][https://github.com/mrdoob/three.js/wiki/Three.js-Shading-Language](https://github.com/mrdoob/three.js/wiki/Three.js-Shading-Language).

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`onBeforeCompile`](Three.WebGPU.NodeMaterial.html#onbeforecompile)

***

### onBeforeRender()

> **onBeforeRender**(): `void`

Defined in: three/src/materials/Material.js:516

An optional callback that is executed immediately before the material is used to render a 3D object.

This method can only be used when rendering with WebGLRenderer.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`onBeforeRender`](Three.WebGPU.NodeMaterial.html#onbeforerender)

***

### onBuild()

> **onBuild**(): `void`

Defined in: three/src/materials/Material.js:1000

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`onBuild`](Three.WebGPU.NodeMaterial.html#onbuild)

***

### removeEventListener()

> **removeEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:74

Removes the given event listener from the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to remove.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`removeEventListener`](Three.WebGPU.NodeMaterial.html#removeeventlistener)

***

### setDefaultValues()

> **setDefaultValues**(`material`): `void`

Defined in: three/src/materials/nodes/NodeMaterial.js:1047

Most classic material types have a node pendant e.g. for `MeshBasicMaterial`
there is `MeshBasicNodeMaterial`. This utility method is intended for
defining all material properties of the classic type in the node type.

#### Parameters

##### material

[`Material`](Three.Material.html)

The material to copy properties with their values to this node material.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setDefaultValues`](Three.WebGPU.NodeMaterial.html#setdefaultvalues)

***

### setup()

> **setup**(`builder`): `void`

Defined in: three/src/materials/nodes/NodeMaterial.js:407

Setups the vertex and fragment stage of this node material.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setup`](Three.WebGPU.NodeMaterial.html#setup)

***

### setupClipping()

> **setupClipping**(`builder`): `ClippingNode`

Defined in: three/src/materials/nodes/NodeMaterial.js:540

Setups the clipping node.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`ClippingNode`

The clipping node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupClipping`](Three.WebGPU.NodeMaterial.html#setupclipping)

***

### setupDepth()

> **setupDepth**(`builder`): `void`

Defined in: three/src/materials/nodes/NodeMaterial.js:601

Setups the depth of this material.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupDepth`](Three.WebGPU.NodeMaterial.html#setupdepth)

***

### setupDiffuseColor()

> **setupDiffuseColor**(`__namedParameters`): `void`

Defined in: three/src/materials/nodes/NodeMaterial.js:744

Setups the computation of the material's diffuse color.

#### Parameters

##### \_\_namedParameters

`NodeBuilder`

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupDiffuseColor`](Three.WebGPU.NodeMaterial.html#setupdiffusecolor)

***

### setupEnvironment()

> **setupEnvironment**(): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:851

Setups the environment node from the material.

#### Returns

`any`

The environment node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupEnvironment`](Three.WebGPU.NodeMaterial.html#setupenvironment)

***

### setupFog()

> **setupFog**(`builder`, `outputNode`): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:1003

Setup the fog.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

##### outputNode

`any`

The existing output node.

#### Returns

`any`

The output node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupFog`](Three.WebGPU.NodeMaterial.html#setupfog)

***

### setupHardwareClipping()

> **setupHardwareClipping**(`builder`): `void`

Defined in: three/src/materials/nodes/NodeMaterial.js:574

Setups the hardware clipping if available on the current device.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupHardwareClipping`](Three.WebGPU.NodeMaterial.html#setuphardwareclipping)

***

### setupLighting()

> **setupLighting**(`builder`): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:957

Setups the outgoing light node.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`any`

The outgoing light node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupLighting`](Three.WebGPU.NodeMaterial.html#setuplighting)

***

### setupLightingModel()

> `abstract` **setupLightingModel**(): `LightingModel`

Defined in: three/src/materials/nodes/NodeMaterial.js:945

This method should be implemented by most derived materials
since it defines the material's lighting model.

#### Returns

`LightingModel`

The lighting model.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupLightingModel`](Three.WebGPU.NodeMaterial.html#setuplightingmodel)

***

### setupLightMap()

> **setupLightMap**(`builder`): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:875

Setups the light map node from the material.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`any`

The light map node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupLightMap`](Three.WebGPU.NodeMaterial.html#setuplightmap)

***

### setupLights()

> **setupLights**(`builder`): `LightsNode`

Defined in: three/src/materials/nodes/NodeMaterial.js:895

Setups the lights node based on the scene, environment and material.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`LightsNode`

The lights node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupLights`](Three.WebGPU.NodeMaterial.html#setuplights)

***

### setupModelViewProjection()

> **setupModelViewProjection**(): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:660

Setups the position in clip space.

#### Returns

`any`

The position in view space.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupModelViewProjection`](Three.WebGPU.NodeMaterial.html#setupmodelviewprojection)

***

### setupNormal()

> **setupNormal**(): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:839

Setups the normal node from the material.

#### Returns

`any`

The normal node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupNormal`](Three.WebGPU.NodeMaterial.html#setupnormal)

***

### setupObserver()

> **setupObserver**(`builder`): [`NodeMaterialObserver`](Three.WebGPU.NodeMaterialObserver.html)

Defined in: three/src/materials/nodes/NodeMaterial.js:396

Setups a node material observer with the given builder.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

[`NodeMaterialObserver`](Three.WebGPU.NodeMaterialObserver.html)

The node material observer.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupObserver`](Three.WebGPU.NodeMaterial.html#setupobserver)

***

### setupOutgoingLight()

> **setupOutgoingLight**(): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:828

Setups the outgoing light node variable

#### Returns

`any`

The outgoing light node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupOutgoingLight`](Three.WebGPU.NodeMaterial.html#setupoutgoinglight)

***

### setupOutput()

> **setupOutput**(`builder`, `outputNode`): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:1026

Setups the output node.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

##### outputNode

`any`

The existing output node.

#### Returns

`any`

The output node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupOutput`](Three.WebGPU.NodeMaterial.html#setupoutput)

***

### setupPosition()

> **setupPosition**(`builder`): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:690

Setups the computation of the position in local space.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`any`

The position in local space.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupPosition`](Three.WebGPU.NodeMaterial.html#setupposition)

***

### setupPositionView()

> **setupPositionView**(): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:648

Setups the position node in view space. This method exists
so derived node materials can modify the implementation e.g. sprite materials.

#### Returns

`any`

The position in view space.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupPositionView`](Three.WebGPU.NodeMaterial.html#setuppositionview)

***

### setupVariants()

> **setupVariants**(): `void`

Defined in: three/src/materials/nodes/LineDashedNodeMaterial.js:113

Setups the dash specific node variables.

#### Returns

`void`

#### Overrides

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupVariants`](Three.WebGPU.NodeMaterial.html#setupvariants)

***

### setupVertex()

> **setupVertex**(`builder`): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:672

Setups the logic for the vertex stage.

#### Parameters

##### builder

`NodeBuilder`

The current node builder.

#### Returns

`any`

The position in clip space.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setupVertex`](Three.WebGPU.NodeMaterial.html#setupvertex)

***

### setValues()

> **setValues**(`values`?): `void`

Defined in: three/src/materials/Material.js:554

This method can be used to set default values from parameter objects.
It is a generic implementation so it can be used with different types
of materials.

#### Parameters

##### values?

`any`

The material values to set.

#### Returns

`void`

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`setValues`](Three.WebGPU.NodeMaterial.html#setvalues)

***

### toJSON()

> **toJSON**(`meta`): `any`

Defined in: three/src/materials/nodes/NodeMaterial.js:1087

Serializes this material to JSON.

#### Parameters

##### meta

`any`

The meta information for serialization.

#### Returns

`any`

The serialized node.

#### Inherited from

[`NodeMaterial`](Three.WebGPU.NodeMaterial.html).[`toJSON`](Three.WebGPU.NodeMaterial.html#tojson)
