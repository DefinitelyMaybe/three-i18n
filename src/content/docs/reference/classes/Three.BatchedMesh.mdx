---
editUrl: false
next: false
prev: false
title: "BatchedMesh"
---

Defined in: three/src/objects/BatchedMesh.js:180

A special version of a mesh with multi draw batch rendering support. Use
this class if you have to render a large number of objects with the same
material but with different geometries or world transformations. The usage of
`BatchedMesh` will help you to reduce the number of draw calls and thus improve the overall
rendering performance in your application.

```js
const box = new THREE.BoxGeometry( 1, 1, 1 );
const sphere = new THREE.SphereGeometry( 1, 12, 12 );
const material = new THREE.MeshBasicMaterial( { color: 0x00ff00 } );

// initialize and add geometries into the batched mesh
const batchedMesh = new BatchedMesh( 10, 5000, 10000, material );
const boxGeometryId = batchedMesh.addGeometry( box );
const sphereGeometryId = batchedMesh.addGeometry( sphere );

// create instances of those geometries
const boxInstancedId1 = batchedMesh.addInstance( boxGeometryId );
const boxInstancedId2 = batchedMesh.addInstance( boxGeometryId );

const sphereInstancedId1 = batchedMesh.addInstance( sphereGeometryId );
const sphereInstancedId2 = batchedMesh.addInstance( sphereGeometryId );

// position the geometries
batchedMesh.setMatrixAt( boxInstancedId1, boxMatrix1 );
batchedMesh.setMatrixAt( boxInstancedId2, boxMatrix2 );

batchedMesh.setMatrixAt( sphereInstancedId1, sphereMatrix1 );
batchedMesh.setMatrixAt( sphereInstancedId2, sphereMatrix2 );

scene.add( batchedMesh );
```

## Extends

- [`Mesh`](Three.Mesh.html)

## Constructors

### Constructor

> **new BatchedMesh**(`maxInstanceCount`, `maxVertexCount`, `maxIndexCount`?, `material`?): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:190

Constructs a new batched mesh.

#### Parameters

##### maxInstanceCount

`number`

The maximum number of individual instances planned to be added and rendered.

##### maxVertexCount

`number`

The maximum number of vertices to be used by all unique geometries.

##### maxIndexCount?

`number` = `...`

The maximum number of indices to be used by all unique geometries

##### material?

`any`

The mesh material.

#### Returns

`BatchedMesh`

#### Overrides

[`Mesh`](Three.Mesh.html).[`constructor`](Three.Mesh.html#constructor)

## Properties

### \_availableGeometryIds

> **\_availableGeometryIds**: `any`[]

Defined in: three/src/objects/BatchedMesh.js:253

***

### \_availableInstanceIds

> **\_availableInstanceIds**: `any`[]

Defined in: three/src/objects/BatchedMesh.js:252

***

### \_colorsTexture

> **\_colorsTexture**: `any`

Defined in: three/src/objects/BatchedMesh.js:278

***

### \_geometryCount

> **\_geometryCount**: `number`

Defined in: three/src/objects/BatchedMesh.js:258

***

### \_geometryInfo

> **\_geometryInfo**: `any`[]

Defined in: three/src/objects/BatchedMesh.js:249

***

### \_geometryInitialized

> **\_geometryInitialized**: `boolean`

Defined in: three/src/objects/BatchedMesh.js:262

***

### \_indirectTexture

> **\_indirectTexture**: [`DataTexture`](Three.DataTexture.html)

Defined in: three/src/objects/BatchedMesh.js:277

***

### \_instanceInfo

> **\_instanceInfo**: `any`[]

Defined in: three/src/objects/BatchedMesh.js:248

***

### \_listeners

> **\_listeners**: `object`

Defined in: three/src/core/EventDispatcher.js:33

#### Inherited from

[`Mesh`](Three.Mesh.html).[`_listeners`](Three.Mesh.html#_listeners)

***

### \_matricesTexture

> **\_matricesTexture**: `any`

Defined in: three/src/objects/BatchedMesh.js:276

***

### \_maxIndexCount

> **\_maxIndexCount**: `number`

Defined in: three/src/objects/BatchedMesh.js:267

***

### \_maxInstanceCount

> **\_maxInstanceCount**: `number`

Defined in: three/src/objects/BatchedMesh.js:265

***

### \_maxVertexCount

> **\_maxVertexCount**: `number`

Defined in: three/src/objects/BatchedMesh.js:266

***

### \_multiDrawCount

> **\_multiDrawCount**: `number`

Defined in: three/src/objects/BatchedMesh.js:272

***

### \_multiDrawCounts

> **\_multiDrawCounts**: `Int32Array`\<`ArrayBuffer`\>

Defined in: three/src/objects/BatchedMesh.js:270

***

### \_multiDrawInstances

> **\_multiDrawInstances**: `any`

Defined in: three/src/objects/BatchedMesh.js:273

***

### \_multiDrawStarts

> **\_multiDrawStarts**: `Int32Array`\<`ArrayBuffer`\>

Defined in: three/src/objects/BatchedMesh.js:271

***

### \_nextIndexStart

> **\_nextIndexStart**: `number`

Defined in: three/src/objects/BatchedMesh.js:256

***

### \_nextVertexStart

> **\_nextVertexStart**: `number`

Defined in: three/src/objects/BatchedMesh.js:257

***

### \_visibilityChanged

> **\_visibilityChanged**: `boolean`

Defined in: three/src/objects/BatchedMesh.js:261

***

### animations

> **animations**: `AnimationClip`[]

Defined in: three/src/core/Object3D.js:331

An array holding the animation clips of the 3D object.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`animations`](Three.Mesh.html#animations)

***

### boundingBox

> **boundingBox**: [`Box3`](Three.Box3.html)

Defined in: three/src/objects/BatchedMesh.js:227

The bounding box of the batched mesh. Can be computed via [BatchedMesh#computeBoundingBox](#computeboundingbox).

#### Default

```ts
null
```

***

### boundingSphere

> **boundingSphere**: [`Sphere`](Three.Sphere.html)

Defined in: three/src/objects/BatchedMesh.js:235

The bounding sphere of the batched mesh. Can be computed via [BatchedMesh#computeBoundingSphere](#computeboundingsphere).

#### Default

```ts
null
```

***

### castShadow

> **castShadow**: `boolean`

Defined in: three/src/core/Object3D.js:296

When set to `true`, the 3D object gets rendered into shadow maps.

#### Default

```ts
false
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`castShadow`](Three.Mesh.html#castshadow)

***

### children

> **children**: [`Object3D`](Three.Object3D.html)[]

Defined in: three/src/core/Object3D.js:127

An array holding the child 3D objects of this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`children`](Three.Mesh.html#children)

***

### customDepthMaterial

> **customDepthMaterial**: `any`

Defined in: three/src/core/Object3D.js:344

Custom depth material to be used when rendering to the depth map. Can only be used
in context of meshes. When shadow-casting with a [DirectionalLight](Three.DirectionalLight.html) or [SpotLight](Three.SpotLight.html),
if you are modifying vertex positions in the vertex shader you must specify a custom depth
material for proper shadows.

Only relevant in context of [WebGLRenderer](Three.WebGLRenderer.html).

#### Default

```ts
undefined
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`customDepthMaterial`](Three.Mesh.html#customdepthmaterial)

***

### customDistanceMaterial

> **customDistanceMaterial**: `any`

Defined in: three/src/core/Object3D.js:354

Same as [Object3D#customDepthMaterial](Three.Object3D.html#customdepthmaterial), but used with [PointLight](Three.PointLight.html).

Only relevant in context of [WebGLRenderer](Three.WebGLRenderer.html).

#### Default

```ts
undefined
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`customDistanceMaterial`](Three.Mesh.html#customdistancematerial)

***

### customSort

> **customSort**: `Function`

Defined in: three/src/objects/BatchedMesh.js:245

Takes a sort a function that is run before render. The function takes a list of instances to
sort and a camera. The objects in the list include a "z" field to perform a depth-ordered
sort with.

#### Default

```ts
null
```

***

### frustumCulled

> **frustumCulled**: `boolean`

Defined in: three/src/core/Object3D.js:312

When set to `true`, the 3D object is honored by view frustum culling.

#### Default

```ts
true
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`frustumCulled`](Three.Mesh.html#frustumculled)

***

### geometry

> **geometry**: [`BufferGeometry`](Three.BufferGeometry.html)

Defined in: three/src/objects/Mesh.js:67

The mesh geometry.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`geometry`](Three.Mesh.html#geometry)

***

### isBatchedMesh

> `readonly` **isBatchedMesh**: `boolean`

Defined in: three/src/objects/BatchedMesh.js:201

This flag can be used for type testing.

#### Default

```ts
true
```

***

### isMesh

> `readonly` **isMesh**: `boolean`

Defined in: three/src/objects/Mesh.js:58

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`isMesh`](Three.Mesh.html#ismesh)

***

### isObject3D

> `readonly` **isObject3D**: `boolean`

Defined in: three/src/core/Object3D.js:79

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`isObject3D`](Three.Mesh.html#isobject3d)

***

### layers

> **layers**: [`Layers`](Three.Layers.html)

Defined in: three/src/core/Object3D.js:280

The layer membership of the 3D object. The 3D object is only visible if it has
at least one layer in common with the camera in use. This property can also be
used to filter out unwanted objects in ray-intersection tests when using [Raycaster](Three.Raycaster.html).

#### Inherited from

[`Mesh`](Three.Mesh.html).[`layers`](Three.Mesh.html#layers)

***

### material

> **material**: `any`

Defined in: three/src/objects/Mesh.js:75

The mesh material.

#### Default

```ts
MeshBasicMaterial
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`material`](Three.Mesh.html#material)

***

### matrix

> **matrix**: [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/core/Object3D.js:232

Represents the object's transformation matrix in local space.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`matrix`](Three.Mesh.html#matrix)

***

### matrixAutoUpdate

> **matrixAutoUpdate**: `boolean`

Defined in: three/src/core/Object3D.js:251

When set to `true`, the engine automatically computes the local matrix from position,
rotation and scale every frame.

The default values for all 3D objects is defined by `Object3D.DEFAULT_MATRIX_AUTO_UPDATE`.

#### Default

```ts
true
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`matrixAutoUpdate`](Three.Mesh.html#matrixautoupdate)

***

### matrixWorld

> **matrixWorld**: [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/core/Object3D.js:240

Represents the object's transformation matrix in world space.
If the 3D object has no parent, then it's identical to the local transformation matrix

#### Inherited from

[`Mesh`](Three.Mesh.html).[`matrixWorld`](Three.Mesh.html#matrixworld)

***

### matrixWorldAutoUpdate

> **matrixWorldAutoUpdate**: `boolean`

Defined in: three/src/core/Object3D.js:262

When set to `true`, the engine automatically computes the world matrix from the current local
matrix and the object's transformation hierarchy.

The default values for all 3D objects is defined by `Object3D.DEFAULT_MATRIX_WORLD_AUTO_UPDATE`.

#### Default

```ts
true
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`matrixWorldAutoUpdate`](Three.Mesh.html#matrixworldautoupdate)

***

### matrixWorldNeedsUpdate

> **matrixWorldNeedsUpdate**: `boolean`

Defined in: three/src/core/Object3D.js:271

When set to `true`, it calculates the world matrix in that frame and resets this property
to `false`.

#### Default

```ts
false
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`matrixWorldNeedsUpdate`](Three.Mesh.html#matrixworldneedsupdate)

***

### morphTargetDictionary

> **morphTargetDictionary**: `any`

Defined in: three/src/objects/Mesh.js:85

A dictionary representing the morph targets in the geometry. The key is the
morph targets name, the value its attribute index. This member is `undefined`
by default and only set when morph targets are detected in the geometry.

#### Default

```ts
undefined
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`morphTargetDictionary`](Three.Mesh.html#morphtargetdictionary)

***

### morphTargetInfluences

> **morphTargetInfluences**: `number`[]

Defined in: three/src/objects/Mesh.js:95

An array of weights typically in the range `[0,1]` that specify how much of the morph
is applied. This member is `undefined` by default and only set when morph targets are
detected in the geometry.

#### Default

```ts
undefined
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`morphTargetInfluences`](Three.Mesh.html#morphtargetinfluences)

***

### name

> **name**: `string`

Defined in: three/src/core/Object3D.js:103

The name of the 3D object.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`name`](Three.Mesh.html#name)

***

### parent

> **parent**: [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:120

A reference to the parent object.

#### Default

```ts
null
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`parent`](Three.Mesh.html#parent)

***

### perObjectFrustumCulled

> **perObjectFrustumCulled**: `boolean`

Defined in: three/src/objects/BatchedMesh.js:209

When set ot `true`, the individual objects of a batch are frustum culled.

#### Default

```ts
true
```

***

### receiveShadow

> **receiveShadow**: `boolean`

Defined in: three/src/core/Object3D.js:304

When set to `true`, the 3D object is affected by shadows in the scene.

#### Default

```ts
false
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`receiveShadow`](Three.Mesh.html#receiveshadow)

***

### renderOrder

> **renderOrder**: `number`

Defined in: three/src/core/Object3D.js:324

This value allows the default rendering order of scene graph objects to be
overridden although opaque and transparent objects remain sorted independently.
When this property is set for an instance of [Group](Three.Group.html),all descendants
objects will be sorted and rendered together. Sorting is from lowest to highest
render order.

#### Default

```ts
0
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`renderOrder`](Three.Mesh.html#renderorder)

***

### sortObjects

> **sortObjects**: `boolean`

Defined in: three/src/objects/BatchedMesh.js:219

When set to `true`, the individual objects of a batch are sorted to improve overdraw-related artifacts.
If the material is marked as "transparent" objects are rendered back to front and if not then they are
rendered front to back.

#### Default

```ts
true
```

***

### type

> **type**: `string`

Defined in: three/src/objects/Mesh.js:60

The type property is used for detecting the object type
in context of serialization/deserialization.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`type`](Three.Mesh.html#type)

***

### up

> **up**: [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:137

Defines the `up` direction of the 3D object which influences
the orientation via methods like [Object3D#lookAt](Three.Object3D.html#lookat).

The default values for all 3D objects is defined by `Object3D.DEFAULT_UP`.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`up`](Three.Mesh.html#up)

***

### userData

> **userData**: `any`

Defined in: three/src/core/Object3D.js:362

An object that can be used to store custom data about the 3D object. It
should not hold references to functions as these will not be cloned.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`userData`](Three.Mesh.html#userdata)

***

### uuid

> `readonly` **uuid**: `string`

Defined in: three/src/core/Object3D.js:96

The UUID of the 3D object.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`uuid`](Three.Mesh.html#uuid)

***

### visible

> **visible**: `boolean`

Defined in: three/src/core/Object3D.js:288

When set to `true`, the 3D object gets rendered.

#### Default

```ts
true
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`visible`](Three.Mesh.html#visible)

***

### DEFAULT\_MATRIX\_AUTO\_UPDATE

> `static` **DEFAULT\_MATRIX\_AUTO\_UPDATE**: `boolean`

Defined in: three/src/core/Object3D.js:1605

The default setting for [Object3D#matrixAutoUpdate](Three.Object3D.html#matrixautoupdate) for
newly created 3D objects.

#### Static

#### Default

```ts
true
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`DEFAULT_MATRIX_AUTO_UPDATE`](Three.Mesh.html#default_matrix_auto_update)

***

### DEFAULT\_MATRIX\_WORLD\_AUTO\_UPDATE

> `static` **DEFAULT\_MATRIX\_WORLD\_AUTO\_UPDATE**: `boolean`

Defined in: three/src/core/Object3D.js:1615

The default setting for [Object3D#matrixWorldAutoUpdate](Three.Object3D.html#matrixworldautoupdate) for
newly created 3D objects.

#### Static

#### Default

```ts
true
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`DEFAULT_MATRIX_WORLD_AUTO_UPDATE`](Three.Mesh.html#default_matrix_world_auto_update)

***

### DEFAULT\_UP

> `static` **DEFAULT\_UP**: [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:1595

The default up direction for objects, also used as the default
position for [DirectionalLight](Three.DirectionalLight.html) and [HemisphereLight](Three.HemisphereLight.html).

#### Static

#### Default

```ts
(0,1,0)
```

#### Inherited from

[`Mesh`](Three.Mesh.html).[`DEFAULT_UP`](Three.Mesh.html#default_up)

## Accessors

### instanceCount

#### Get Signature

> **get** **instanceCount**(): `number`

Defined in: three/src/objects/BatchedMesh.js:303

The instance count.

##### Returns

`number`

***

### maxInstanceCount

#### Get Signature

> **get** **maxInstanceCount**(): `number`

Defined in: three/src/objects/BatchedMesh.js:291

The maximum number of individual instances that can be stored in the batch.

##### Returns

`number`

***

### unusedIndexCount

#### Get Signature

> **get** **unusedIndexCount**(): `number`

Defined in: three/src/objects/BatchedMesh.js:327

The number of unused indices.

##### Returns

`number`

***

### unusedVertexCount

#### Get Signature

> **get** **unusedVertexCount**(): `number`

Defined in: three/src/objects/BatchedMesh.js:315

The number of unused vertices.

##### Returns

`number`

## Methods

### \_computeIntersections()

> **\_computeIntersections**(`raycaster`, `intersects`, `rayLocalSpace`): `void`

Defined in: three/src/objects/Mesh.js:263

#### Parameters

##### raycaster

`any`

##### intersects

`any`

##### rayLocalSpace

`any`

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`_computeIntersections`](Three.Mesh.html#_computeintersections)

***

### \_initColorsTexture()

> **\_initColorsTexture**(): `void`

Defined in: three/src/objects/BatchedMesh.js:365

#### Returns

`void`

***

### \_initializeGeometry()

> **\_initializeGeometry**(`reference`): `void`

Defined in: three/src/objects/BatchedMesh.js:379

#### Parameters

##### reference

`any`

#### Returns

`void`

***

### \_initIndirectTexture()

> **\_initIndirectTexture**(): `void`

Defined in: three/src/objects/BatchedMesh.js:353

#### Returns

`void`

***

### \_initMatricesTexture()

> **\_initMatricesTexture**(): `void`

Defined in: three/src/objects/BatchedMesh.js:333

#### Returns

`void`

***

### \_validateGeometry()

> **\_validateGeometry**(`geometry`): `void`

Defined in: three/src/objects/BatchedMesh.js:416

#### Parameters

##### geometry

`any`

#### Returns

`void`

***

### add()

> **add**(`object`, ...`args`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:720

Adds the given 3D object as a child to this 3D object. An arbitrary number of
objects may be added. Any current parent on an object passed in here will be
removed, since an object can have at most one parent.

#### Parameters

##### object

[`Object3D`](Three.Object3D.html)

The 3D object to add.

##### args

...`any`

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#added

#### Fires

Object3D#childadded

#### Inherited from

[`Mesh`](Three.Mesh.html).[`add`](Three.Mesh.html#add)

***

### addEventListener()

> **addEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:31

Adds the given event listener to the given event type.

#### Parameters

##### type

`string`

The type of event to listen to.

##### listener

`Function`

The function that gets called when the event is fired.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`addEventListener`](Three.Mesh.html#addeventlistener)

***

### addGeometry()

> **addGeometry**(`geometry`, `reservedVertexCount`?, `reservedIndexCount`?): `number`

Defined in: three/src/objects/BatchedMesh.js:625

Adds the given geometry to the batch and returns the associated
geometry id referring to it to be used in other functions.

#### Parameters

##### geometry

[`BufferGeometry`](Three.BufferGeometry.html)

The geometry to add.

##### reservedVertexCount?

`number` = `- 1`

Optional parameter specifying the amount of
vertex buffer space to reserve for the added geometry. This is necessary if it is planned
to set a new geometry at this index at a later time that is larger than the original geometry.
Defaults to the length of the given geometry vertex buffer.

##### reservedIndexCount?

`number` = `- 1`

Optional parameter specifying the amount of index
buffer space to reserve for the added geometry. This is necessary if it is planned to set a
new geometry at this index at a later time that is larger than the original geometry. Defaults to
the length of the given geometry index buffer.

#### Returns

`number`

The geometry ID.

***

### addInstance()

> **addInstance**(`geometryId`): `number`

Defined in: three/src/objects/BatchedMesh.js:559

Adds a new instance to the batch using the geometry of the given ID and returns
a new id referring to the new instance to be used by other functions.

#### Parameters

##### geometryId

`number`

The ID of a previously added geometry via [BatchedMesh#addGeometry](#addgeometry).

#### Returns

`number`

The instance ID.

***

### applyMatrix4()

> **applyMatrix4**(`matrix`): `void`

Defined in: three/src/core/Object3D.js:422

Applies the given transformation matrix to the object and updates the object's position,
rotation and scale.

#### Parameters

##### matrix

[`Matrix4`](Three.Matrix4.html)

The transformation matrix.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`applyMatrix4`](Three.Mesh.html#applymatrix4)

***

### applyQuaternion()

> **applyQuaternion**(`q`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:438

Applies a rotation represented by given the quaternion to the 3D object.

#### Parameters

##### q

[`Quaternion`](Three.Quaternion.html)

The quaternion.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`applyQuaternion`](Three.Mesh.html#applyquaternion)

***

### attach()

> **attach**(`object`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:848

Adds the given 3D object as a child of this 3D object, while maintaining the object's world
transform. This method does not support scene graphs having non-uniformly-scaled nodes(s).

#### Parameters

##### object

[`Object3D`](Three.Object3D.html)

The 3D object to attach.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#added

#### Fires

Object3D#childadded

#### Inherited from

[`Mesh`](Three.Mesh.html).[`attach`](Three.Mesh.html#attach)

***

### clear()

> **clear**(): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:833

Removes all child objects.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#removed

#### Fires

Object3D#childremoved

#### Inherited from

[`Mesh`](Three.Mesh.html).[`clear`](Three.Mesh.html#clear)

***

### clone()

> **clone**(`recursive`?): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:1525

Returns a new 3D object with copied values from this instance.

#### Parameters

##### recursive?

`boolean`

When set to `true`, descendants of the 3D object are also cloned.

#### Returns

[`Object3D`](Three.Object3D.html)

A clone of this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`clone`](Three.Mesh.html#clone)

***

### computeBoundingBox()

> **computeBoundingBox**(): `void`

Defined in: three/src/objects/BatchedMesh.js:497

Computes the bounding box, updating [BatchedMesh#boundingBox](#boundingbox).
Bounding boxes aren't computed by default. They need to be explicitly computed,
otherwise they are `null`.

#### Returns

`void`

***

### computeBoundingSphere()

> **computeBoundingSphere**(): `void`

Defined in: three/src/objects/BatchedMesh.js:527

Computes the bounding sphere, updating [BatchedMesh#boundingSphere](#boundingsphere).
Bounding spheres aren't computed by default. They need to be explicitly computed,
otherwise they are `null`.

#### Returns

`void`

***

### copy()

> **copy**(`source`): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:1426

Copies the values of the given 3D object to this instance.

#### Parameters

##### source

`any`

The 3D object to copy.

#### Returns

`BatchedMesh`

A reference to this instance.

#### Overrides

[`Mesh`](Three.Mesh.html).[`copy`](Three.Mesh.html#copy)

***

### deleteGeometry()

> **deleteGeometry**(`geometryId`): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:824

Deletes the geometry defined by the given ID from this batch. Any instances referencing
this geometry will also be removed as a side effect.

#### Parameters

##### geometryId

`number`

The ID of the geometry to remove from the batch.

#### Returns

`BatchedMesh`

A reference to this batched mesh.

***

### deleteInstance()

> **deleteInstance**(`instanceId`): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:859

Deletes an existing instance from the batch using the given ID.

#### Parameters

##### instanceId

`number`

The ID of the instance to remove from the batch.

#### Returns

`BatchedMesh`

A reference to this batched mesh.

***

### dispatchEvent()

> **dispatchEvent**(`event`): `void`

Defined in: three/src/core/EventDispatcher.js:101

Dispatches an event object.

#### Parameters

##### event

`any`

The event that gets fired.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`dispatchEvent`](Three.Mesh.html#dispatchevent)

***

### dispose()

> **dispose**(): `void`

Defined in: three/src/objects/BatchedMesh.js:1471

Frees the GPU-related resources allocated by this instance. Call this
method whenever this instance is no longer used in your app.

#### Returns

`void`

***

### getBoundingBoxAt()

> **getBoundingBoxAt**(`geometryId`, `target`): [`Box3`](Three.Box3.html)

Defined in: three/src/objects/BatchedMesh.js:974

Returns the bounding box for the given geometry.

#### Parameters

##### geometryId

`number`

The ID of the geometry to return the bounding box for.

##### target

[`Box3`](Three.Box3.html)

The target object that is used to store the method's result.

#### Returns

[`Box3`](Three.Box3.html)

The geometry's bounding box. Returns `null` if no geometry has been found for the given ID.

***

### getBoundingSphereAt()

> **getBoundingSphereAt**(`geometryId`, `target`): [`Sphere`](Three.Sphere.html)

Defined in: three/src/objects/BatchedMesh.js:1019

Returns the bounding sphere for the given geometry.

#### Parameters

##### geometryId

`number`

The ID of the geometry to return the bounding sphere for.

##### target

[`Sphere`](Three.Sphere.html)

The target object that is used to store the method's result.

#### Returns

[`Sphere`](Three.Sphere.html)

The geometry's bounding sphere. Returns `null` if no geometry has been found for the given ID.

***

### getColorAt()

> **getColorAt**(`instanceId`, `color`): [`Color`](Three.Color.html)

Defined in: three/src/objects/BatchedMesh.js:1130

Returns the color of the defined instance.

#### Parameters

##### instanceId

`number`

The ID of an instance to get the color of.

##### color

[`Color`](Three.Color.html)

The target object that is used to store the method's result.

#### Returns

[`Color`](Three.Color.html)

The instance's color.

***

### getGeometryIdAt()

> **getGeometryIdAt**(`instanceId`): `number`

Defined in: three/src/objects/BatchedMesh.js:1199

Returns the geometry ID of the defined instance.

#### Parameters

##### instanceId

`number`

The ID of an instance to get the geometry ID of.

#### Returns

`number`

The instance's geometry ID.

***

### getGeometryRangeAt()

> **getGeometryRangeAt**(`geometryId`, `target`?): `object`

Defined in: three/src/objects/BatchedMesh.js:1219

Get the range representing the subset of triangles related to the attached geometry,
indicating the starting offset and count, or `null` if invalid.

#### Parameters

##### geometryId

`number`

The id of the geometry to get the range of.

##### target?

`any` = `{}`

The target object that is used to store the method's result.

#### Returns

`object`

The result object with range data.

##### count

> **count**: `number`

##### indexCount

> **indexCount**: `number`

##### indexStart

> **indexStart**: `number`

##### reservedIndexCount

> **reservedIndexCount**: `number`

##### reservedVertexCount

> **reservedVertexCount**: `number`

##### start

> **start**: `number`

##### vertexCount

> **vertexCount**: `number`

##### vertexStart

> **vertexStart**: `number`

***

### getMatrixAt()

> **getMatrixAt**(`instanceId`, `matrix`): [`Matrix4`](Three.Matrix4.html)

Defined in: three/src/objects/BatchedMesh.js:1092

Returns the local transformation matrix of the defined instance.

#### Parameters

##### instanceId

`number`

The ID of an instance to get the matrix of.

##### matrix

[`Matrix4`](Three.Matrix4.html)

The target object that is used to store the method's result.

#### Returns

[`Matrix4`](Three.Matrix4.html)

The instance's local transformation matrix.

***

### getObjectById()

> **getObjectById**(`id`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:891

Searches through the 3D object and its children, starting with the 3D object
itself, and returns the first with a matching ID.

#### Parameters

##### id

`number`

The id.

#### Returns

[`Object3D`](Three.Object3D.html)

The found 3D object. Returns `undefined` if no 3D object has been found.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`getObjectById`](Three.Mesh.html#getobjectbyid)

***

### getObjectByName()

> **getObjectByName**(`name`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:904

Searches through the 3D object and its children, starting with the 3D object
itself, and returns the first with a matching name.

#### Parameters

##### name

`string`

The name.

#### Returns

[`Object3D`](Three.Object3D.html)

The found 3D object. Returns `undefined` if no 3D object has been found.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`getObjectByName`](Three.Mesh.html#getobjectbyname)

***

### getObjectByProperty()

> **getObjectByProperty**(`name`, `value`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:918

Searches through the 3D object and its children, starting with the 3D object
itself, and returns the first with a matching property value.

#### Parameters

##### name

`string`

The name of the property.

##### value

`any`

The value.

#### Returns

[`Object3D`](Three.Object3D.html)

The found 3D object. Returns `undefined` if no 3D object has been found.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`getObjectByProperty`](Three.Mesh.html#getobjectbyproperty)

***

### getObjectsByProperty()

> **getObjectsByProperty**(`name`, `value`, `result`): [`Object3D`](Three.Object3D.html)[]

Defined in: three/src/core/Object3D.js:948

Searches through the 3D object and its children, starting with the 3D object
itself, and returns all 3D objects with a matching property value.

#### Parameters

##### name

`string`

The name of the property.

##### value

`any`

The value.

##### result

[`Object3D`](Three.Object3D.html)[] = `[]`

The method stores the result in this array.

#### Returns

[`Object3D`](Three.Object3D.html)[]

The found 3D objects.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`getObjectsByProperty`](Three.Mesh.html#getobjectsbyproperty)

***

### getVertexPosition()

> **getVertexPosition**(`index`, `target`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/objects/Mesh.js:167

Returns the local-space position of the vertex at the given index, taking into
account the current animation state of both morph targets and skinning.

#### Parameters

##### index

`number`

The vertex index.

##### target

[`Vector3`](Three.Vector3.html)

The target object that is used to store the method's result.

#### Returns

[`Vector3`](Three.Vector3.html)

The vertex position in local space.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`getVertexPosition`](Three.Mesh.html#getvertexposition)

***

### getVisibleAt()

> **getVisibleAt**(`instanceId`): `boolean`

Defined in: three/src/objects/BatchedMesh.js:1167

Returns the visibility state of the defined instance.

#### Parameters

##### instanceId

`number`

The ID of an instance to get the visibility state of.

#### Returns

`boolean`

Whether the instance is visible or not.

***

### getWorldDirection()

> **getWorldDirection**(`target`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:1016

Returns a vector representing the ("look") direction of the 3D object in world space.

#### Parameters

##### target

[`Vector3`](Three.Vector3.html)

The target vector the result is stored to.

#### Returns

[`Vector3`](Three.Vector3.html)

The 3D object's direction in world space.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`getWorldDirection`](Three.Mesh.html#getworlddirection)

***

### getWorldPosition()

> **getWorldPosition**(`target`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:970

Returns a vector representing the position of the 3D object in world space.

#### Parameters

##### target

[`Vector3`](Three.Vector3.html)

The target vector the result is stored to.

#### Returns

[`Vector3`](Three.Vector3.html)

The 3D object's position in world space.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`getWorldPosition`](Three.Mesh.html#getworldposition)

***

### getWorldQuaternion()

> **getWorldQuaternion**(`target`): [`Quaternion`](Three.Quaternion.html)

Defined in: three/src/core/Object3D.js:984

Returns a Quaternion representing the position of the 3D object in world space.

#### Parameters

##### target

[`Quaternion`](Three.Quaternion.html)

The target Quaternion the result is stored to.

#### Returns

[`Quaternion`](Three.Quaternion.html)

The 3D object's rotation in world space.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`getWorldQuaternion`](Three.Mesh.html#getworldquaternion)

***

### getWorldScale()

> **getWorldScale**(`target`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:1000

Returns a vector representing the scale of the 3D object in world space.

#### Parameters

##### target

[`Vector3`](Three.Vector3.html)

The target vector the result is stored to.

#### Returns

[`Vector3`](Three.Vector3.html)

The 3D object's scale in world space.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`getWorldScale`](Three.Mesh.html#getworldscale)

***

### hasEventListener()

> **hasEventListener**(`type`, `listener`): `boolean`

Defined in: three/src/core/EventDispatcher.js:58

Returns `true` if the given event listener has been added to the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to check.

#### Returns

`boolean`

Whether the given event listener has been added to the given event type.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`hasEventListener`](Three.Mesh.html#haseventlistener)

***

### localToWorld()

> **localToWorld**(`vector`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:637

Converts the given vector from this 3D object's local space to world space.

#### Parameters

##### vector

[`Vector3`](Three.Vector3.html)

The vector to convert.

#### Returns

[`Vector3`](Three.Vector3.html)

The converted vector.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`localToWorld`](Three.Mesh.html#localtoworld)

***

### lookAt()

> **lookAt**(`x`, `y`?, `z`?): `void`

Defined in: three/src/core/Object3D.js:668

Rotates the object to face a point in world space.

This method does not support objects having non-uniformly-scaled parent(s).

#### Parameters

##### x

The x coordinate in world space. Alternatively, a vector representing a position in world space

`number` | [`Vector3`](Three.Vector3.html)

##### y?

`number`

The y coordinate in world space.

##### z?

`number`

The z coordinate in world space.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`lookAt`](Three.Mesh.html#lookat)

***

### onAfterRender()

> **onAfterRender**(): `void`

Defined in: three/src/core/Object3D.js:414

A callback that is executed immediately after a 3D object is rendered.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`onAfterRender`](Three.Mesh.html#onafterrender)

***

### onAfterShadow()

> **onAfterShadow**(): `void`

Defined in: three/src/core/Object3D.js:390

A callback that is executed immediately after a 3D object is rendered to a shadow map.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`onAfterShadow`](Three.Mesh.html#onaftershadow)

***

### onBeforeRender()

> **onBeforeRender**(`renderer`, `scene`, `camera`, `geometry`, `material`): `void`

Defined in: three/src/objects/BatchedMesh.js:1491

A callback that is executed immediately before a 3D object is rendered.

#### Parameters

##### renderer

`any`

The renderer.

##### scene

`any`

##### camera

`any`

The camera that is used to render the scene.

##### geometry

`any`

The 3D object's geometry.

##### material

`any`

The 3D object's material.

#### Returns

`void`

#### Overrides

[`Mesh`](Three.Mesh.html).[`onBeforeRender`](Three.Mesh.html#onbeforerender)

***

### onBeforeShadow()

> **onBeforeShadow**(`renderer`, `object`, `camera`, `shadowCamera`, `geometry`, `depthMaterial`): `void`

Defined in: three/src/objects/BatchedMesh.js:1632

A callback that is executed immediately before a 3D object is rendered to a shadow map.

#### Parameters

##### renderer

`any`

The renderer.

##### object

`any`

The 3D object.

##### camera

`any`

The camera that is used to render the scene.

##### shadowCamera

`any`

The shadow camera.

##### geometry

`any`

The 3D object's geometry.

##### depthMaterial

`any`

The depth material.

#### Returns

`void`

#### Overrides

[`Mesh`](Three.Mesh.html).[`onBeforeShadow`](Three.Mesh.html#onbeforeshadow)

***

### optimize()

> **optimize**(): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:878

Repacks the sub geometries in [name] to remove any unused space remaining from
previously deleted geometry, freeing up space to add new geometry.

#### Returns

`BatchedMesh`

A reference to this batched mesh.

***

### raycast()

> **raycast**(`raycaster`, `intersects`): `void`

Defined in: three/src/objects/BatchedMesh.js:1364

Computes intersection points between a casted ray and this line.

#### Parameters

##### raycaster

`any`

The raycaster.

##### intersects

`any`

The target array that holds the intersection points.

#### Returns

`void`

#### Overrides

[`Mesh`](Three.Mesh.html).[`raycast`](Three.Mesh.html#raycast)

***

### remove()

> **remove**(`object`, ...`args`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:772

Removes the given 3D object as child from this 3D object.
An arbitrary number of objects may be removed.

#### Parameters

##### object

[`Object3D`](Three.Object3D.html)

The 3D object to remove.

##### args

...`any`

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#removed

#### Fires

Object3D#childremoved

#### Inherited from

[`Mesh`](Three.Mesh.html).[`remove`](Three.Mesh.html#remove)

***

### removeEventListener()

> **removeEventListener**(`type`, `listener`): `void`

Defined in: three/src/core/EventDispatcher.js:74

Removes the given event listener from the given event type.

#### Parameters

##### type

`string`

The type of event.

##### listener

`Function`

The listener to remove.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`removeEventListener`](Three.Mesh.html#removeeventlistener)

***

### removeFromParent()

> **removeFromParent**(): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:812

Removes this 3D object from its current parent.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Fires

Object3D#removed

#### Fires

Object3D#childremoved

#### Inherited from

[`Mesh`](Three.Mesh.html).[`removeFromParent`](Three.Mesh.html#removefromparent)

***

### rotateOnAxis()

> **rotateOnAxis**(`axis`, `angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:505

Rotates the 3D object along an axis in local space.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`rotateOnAxis`](Three.Mesh.html#rotateonaxis)

***

### rotateOnWorldAxis()

> **rotateOnWorldAxis**(`axis`, `angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:525

Rotates the 3D object along an axis in world space.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`rotateOnWorldAxis`](Three.Mesh.html#rotateonworldaxis)

***

### rotateX()

> **rotateX**(`angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:545

Rotates the 3D object around its X axis in local space.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`rotateX`](Three.Mesh.html#rotatex)

***

### rotateY()

> **rotateY**(`angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:557

Rotates the 3D object around its Y axis in local space.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`rotateY`](Three.Mesh.html#rotatey)

***

### rotateZ()

> **rotateZ**(`angle`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:569

Rotates the 3D object around its Z axis in local space.

#### Parameters

##### angle

`number`

The angle in radians.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`rotateZ`](Three.Mesh.html#rotatez)

***

### setColorAt()

> **setColorAt**(`instanceId`, `color`): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:1106

Sets the given color to the defined instance.

#### Parameters

##### instanceId

`number`

The ID of an instance to set the color of.

##### color

[`Color`](Three.Color.html)

The color to set the instance to.

#### Returns

`BatchedMesh`

A reference to this batched mesh.

***

### setCustomSort()

> **setCustomSort**(`func`): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:485

Takes a sort a function that is run before render. The function takes a list of instances to
sort and a camera. The objects in the list include a "z" field to perform a depth-ordered sort with.

#### Parameters

##### func

`Function`

The custom sort function.

#### Returns

`BatchedMesh`

A reference to this batched mesh.

***

### setGeometryAt()

> **setGeometryAt**(`geometryId`, `geometry`): `number`

Defined in: three/src/objects/BatchedMesh.js:712

Replaces the geometry at the given ID with the provided geometry. Throws an error if there
is not enough space reserved for geometry. Calling this will change all instances that are
rendering that geometry.

#### Parameters

##### geometryId

`number`

The ID of the geometry that should be replaced with the given geometry.

##### geometry

[`BufferGeometry`](Three.BufferGeometry.html)

The new geometry.

#### Returns

`number`

The geometry ID.

***

### setGeometryIdAt()

> **setGeometryIdAt**(`instanceId`, `geometryId`): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:1182

Sets the geometry ID of the instance at the given index.

#### Parameters

##### instanceId

`number`

The ID of the instance to set the geometry ID of.

##### geometryId

`number`

The geometry ID to be use by the instance.

#### Returns

`BatchedMesh`

A reference to this batched mesh.

***

### setGeometrySize()

> **setGeometrySize**(`maxVertexCount`, `maxIndexCount`): `void`

Defined in: three/src/objects/BatchedMesh.js:1307

Resizes the available space in the batch's vertex and index buffer attributes to the provided sizes.
If the provided arguments shrink the geometry buffers but there is not enough unused space at the
end of the geometry attributes then an error is thrown.

#### Parameters

##### maxVertexCount

`number`

The maximum number of vertices to be used by all unique geometries to resize to.

##### maxIndexCount

`number`

The maximum number of indices to be used by all unique geometries to resize to.

#### Returns

`void`

***

### setInstanceCount()

> **setInstanceCount**(`maxInstanceCount`): `void`

Defined in: three/src/objects/BatchedMesh.js:1246

Resizes the necessary buffers to support the provided number of instances.
If the provided arguments shrink the number of instances but there are not enough
unused Ids at the end of the list then an error is thrown.

#### Parameters

##### maxInstanceCount

`number`

The max number of individual instances that can be added and rendered by the batch.

#### Returns

`void`

***

### setMatrixAt()

> **setMatrixAt**(`instanceId`, `matrix`): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:1072

Sets the given local transformation matrix to the defined instance.
Negatively scaled matrices are not supported.

#### Parameters

##### instanceId

`number`

The ID of an instance to set the matrix of.

##### matrix

[`Matrix4`](Three.Matrix4.html)

A 4x4 matrix representing the local transformation of a single instance.

#### Returns

`BatchedMesh`

A reference to this batched mesh.

***

### setRotationFromAxisAngle()

> **setRotationFromAxisAngle**(`axis`, `angle`): `void`

Defined in: three/src/core/Object3D.js:452

Sets the given rotation represented as an axis/angle couple to the 3D object.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### angle

`number`

The angle in radians.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`setRotationFromAxisAngle`](Three.Mesh.html#setrotationfromaxisangle)

***

### setRotationFromEuler()

> **setRotationFromEuler**(`euler`): `void`

Defined in: three/src/core/Object3D.js:465

Sets the given rotation represented as Euler angles to the 3D object.

#### Parameters

##### euler

[`Euler`](Three.Euler.html)

The Euler angles.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`setRotationFromEuler`](Three.Mesh.html#setrotationfromeuler)

***

### setRotationFromMatrix()

> **setRotationFromMatrix**(`m`): `void`

Defined in: three/src/core/Object3D.js:477

Sets the given rotation represented as rotation matrix to the 3D object.

#### Parameters

##### m

[`Matrix4`](Three.Matrix4.html)

Although a 4x4 matrix is expected, the upper 3x3 portion must be
a pure rotation matrix (i.e, unscaled).

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`setRotationFromMatrix`](Three.Mesh.html#setrotationfrommatrix)

***

### setRotationFromQuaternion()

> **setRotationFromQuaternion**(`q`): `void`

Defined in: three/src/core/Object3D.js:490

Sets the given rotation represented as a Quaternion to the 3D object.

#### Parameters

##### q

[`Quaternion`](Three.Quaternion.html)

The Quaternion

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`setRotationFromQuaternion`](Three.Mesh.html#setrotationfromquaternion)

***

### setVisibleAt()

> **setVisibleAt**(`instanceId`, `visible`): `BatchedMesh`

Defined in: three/src/objects/BatchedMesh.js:1144

Sets the visibility of the instance.

#### Parameters

##### instanceId

`number`

The id of the instance to set the visibility of.

##### visible

`boolean`

Whether the instance is visible or not.

#### Returns

`BatchedMesh`

A reference to this batched mesh.

***

### toJSON()

> **toJSON**(`meta`): `any`

Defined in: three/src/core/Object3D.js:1224

Serializes the 3D object into JSON.

#### Parameters

##### meta

`any`

An optional value holding meta information about the serialization.

#### Returns

`any`

A JSON object representing the serialized 3D object.

#### See

[ObjectLoader#parse](Three.ObjectLoader.html#parse)

#### Inherited from

[`Mesh`](Three.Mesh.html).[`toJSON`](Three.Mesh.html#tojson)

***

### translateOnAxis()

> **translateOnAxis**(`axis`, `distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:582

Translate the 3D object by a distance along the given axis in local space.

#### Parameters

##### axis

[`Vector3`](Three.Vector3.html)

The (normalized) axis vector.

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`translateOnAxis`](Three.Mesh.html#translateonaxis)

***

### translateX()

> **translateX**(`distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:601

Translate the 3D object by a distance along its X-axis in local space.

#### Parameters

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`translateX`](Three.Mesh.html#translatex)

***

### translateY()

> **translateY**(`distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:613

Translate the 3D object by a distance along its Y-axis in local space.

#### Parameters

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`translateY`](Three.Mesh.html#translatey)

***

### translateZ()

> **translateZ**(`distance`): [`Object3D`](Three.Object3D.html)

Defined in: three/src/core/Object3D.js:625

Translate the 3D object by a distance along its Z-axis in local space.

#### Parameters

##### distance

`number`

The distance in world units.

#### Returns

[`Object3D`](Three.Object3D.html)

A reference to this instance.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`translateZ`](Three.Mesh.html#translatez)

***

### traverse()

> **traverse**(`callback`): `void`

Defined in: three/src/core/Object3D.js:1044

Executes the callback on this 3D object and all descendants.

Note: Modifying the scene graph inside the callback is discouraged.

#### Parameters

##### callback

`Function`

A callback function that allows to process the current 3D object.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`traverse`](Three.Mesh.html#traverse)

***

### traverseAncestors()

> **traverseAncestors**(`callback`): `void`

Defined in: three/src/core/Object3D.js:1089

Like [Object3D#traverse](Three.Object3D.html#traverse), but the callback will only be executed for all ancestors.

Note: Modifying the scene graph inside the callback is discouraged.

#### Parameters

##### callback

`Function`

A callback function that allows to process the current 3D object.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`traverseAncestors`](Three.Mesh.html#traverseancestors)

***

### traverseVisible()

> **traverseVisible**(`callback`): `void`

Defined in: three/src/core/Object3D.js:1066

Like [Object3D#traverse](Three.Object3D.html#traverse), but the callback will only be executed for visible 3D objects.
Descendants of invisible 3D objects are not traversed.

Note: Modifying the scene graph inside the callback is discouraged.

#### Parameters

##### callback

`Function`

A callback function that allows to process the current 3D object.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`traverseVisible`](Three.Mesh.html#traversevisible)

***

### updateMatrix()

> **updateMatrix**(): `void`

Defined in: three/src/core/Object3D.js:1107

Updates the transformation matrix in local space by computing it from the current
position, rotation and scale values.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`updateMatrix`](Three.Mesh.html#updatematrix)

***

### updateMatrixWorld()

> **updateMatrixWorld**(`force`?): `void`

Defined in: three/src/core/Object3D.js:1126

Updates the transformation matrix in world space of this 3D objects and its descendants.

To ensure correct results, this method also recomputes the 3D object's transformation matrix in
local space. The computation of the local and world matrix can be controlled with the
[Object3D#matrixAutoUpdate](Three.Object3D.html#matrixautoupdate) and [Object3D#matrixWorldAutoUpdate](Three.Object3D.html#matrixworldautoupdate) flags which are both
`true` by default.  Set these flags to `false` if you need more control over the update matrix process.

#### Parameters

##### force?

`boolean`

When set to `true`, a recomputation of world matrices is forced even
when [Object3D#matrixWorldAutoUpdate](Three.Object3D.html#matrixworldautoupdate) is set to `false`.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`updateMatrixWorld`](Three.Mesh.html#updatematrixworld)

***

### updateMorphTargets()

> **updateMorphTargets**(): `void`

Defined in: three/src/objects/Mesh.js:128

Sets the values of [Mesh#morphTargetDictionary](Three.Mesh.html#morphtargetdictionary) and [Mesh#morphTargetInfluences](Three.Mesh.html#morphtargetinfluences)
to make sure existing morph targets can influence this 3D object.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`updateMorphTargets`](Three.Mesh.html#updatemorphtargets)

***

### updateWorldMatrix()

> **updateWorldMatrix**(`updateParents`?, `updateChildren`?): `void`

Defined in: three/src/core/Object3D.js:1173

An alternative version of [Object3D#updateMatrixWorld](Three.Object3D.html#updatematrixworld) with more control over the
update of ancestor and descendant nodes.

#### Parameters

##### updateParents?

`boolean`

Whether ancestor nodes should be updated or not.

##### updateChildren?

`boolean`

Whether descendant nodes should be updated or not.

#### Returns

`void`

#### Inherited from

[`Mesh`](Three.Mesh.html).[`updateWorldMatrix`](Three.Mesh.html#updateworldmatrix)

***

### validateGeometryId()

> **validateGeometryId**(`geometryId`): `void`

Defined in: three/src/objects/BatchedMesh.js:467

Validates the geometry defined by the given ID.

#### Parameters

##### geometryId

`number`

The geometry to validate.

#### Returns

`void`

***

### validateInstanceId()

> **validateInstanceId**(`instanceId`): `void`

Defined in: three/src/objects/BatchedMesh.js:451

Validates the instance defined by the given ID.

#### Parameters

##### instanceId

`number`

The instance to validate.

#### Returns

`void`

***

### worldToLocal()

> **worldToLocal**(`vector`): [`Vector3`](Three.Vector3.html)

Defined in: three/src/core/Object3D.js:651

Converts the given vector from this 3D object's word space to local space.

#### Parameters

##### vector

[`Vector3`](Three.Vector3.html)

The vector to convert.

#### Returns

[`Vector3`](Three.Vector3.html)

The converted vector.

#### Inherited from

[`Mesh`](Three.Mesh.html).[`worldToLocal`](Three.Mesh.html#worldtolocal)
