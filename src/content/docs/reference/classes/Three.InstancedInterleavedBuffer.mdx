---
editUrl: false
next: false
prev: false
title: "InstancedInterleavedBuffer"
---

Defined in: three/src/core/InstancedInterleavedBuffer.js:8

An instanced version of an interleaved buffer.

## Extends

- [`InterleavedBuffer`](Three.InterleavedBuffer.html)

## Constructors

### Constructor

> **new InstancedInterleavedBuffer**(`array`, `stride`, `meshPerAttribute`?): `InstancedInterleavedBuffer`

Defined in: three/src/core/InstancedInterleavedBuffer.js:17

Constructs a new instanced interleaved buffer.

#### Parameters

##### array

`TypedArray`

A typed array with a shared buffer storing attribute data.

##### stride

`number`

The number of typed-array elements per vertex.

##### meshPerAttribute?

`number` = `1`

Defines how often a value of this interleaved buffer should be repeated.

#### Returns

`InstancedInterleavedBuffer`

#### Overrides

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`constructor`](Three.InterleavedBuffer.html#constructor)

## Properties

### array

> **array**: `TypedArray`

Defined in: three/src/core/InterleavedBuffer.js:34

A typed array with a shared buffer storing attribute data.

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`array`](Three.InterleavedBuffer.html#array)

***

### count

> `readonly` **count**: `number`

Defined in: three/src/core/InterleavedBuffer.js:49

The total number of elements in the array

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`count`](Three.InterleavedBuffer.html#count)

***

### isInstancedInterleavedBuffer

> `readonly` **isInstancedInterleavedBuffer**: `boolean`

Defined in: three/src/core/InstancedInterleavedBuffer.js:28

This flag can be used for type testing.

#### Default

```ts
true
```

***

### isInterleavedBuffer

> `readonly` **isInterleavedBuffer**: `boolean`

Defined in: three/src/core/InterleavedBuffer.js:27

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`isInterleavedBuffer`](Three.InterleavedBuffer.html#isinterleavedbuffer)

***

### meshPerAttribute

> **meshPerAttribute**: `number`

Defined in: three/src/core/InstancedInterleavedBuffer.js:37

Defines how often a value of this buffer attribute should be repeated,
see [InstancedBufferAttribute#meshPerAttribute](Three.InstancedBufferAttribute.html#meshperattribute).

#### Default

```ts
1
```

***

### stride

> **stride**: `number`

Defined in: three/src/core/InterleavedBuffer.js:41

The number of typed-array elements per vertex.

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`stride`](Three.InterleavedBuffer.html#stride)

***

### updateRanges

> **updateRanges**: `any`[]

Defined in: three/src/core/InterleavedBuffer.js:68

This can be used to only update some components of stored vectors (for example, just the
component related to color). Use the `addUpdateRange()` function to add ranges to this array.

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`updateRanges`](Three.InterleavedBuffer.html#updateranges)

***

### usage

> **usage**: `any`

Defined in: three/src/core/InterleavedBuffer.js:60

Defines the intended usage pattern of the data store for optimization purposes.

Note: After the initial use of a buffer, its usage cannot be changed. Instead,
instantiate a new one and set the desired usage before the next render.

#### Default

```ts
StaticDrawUsage
```

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`usage`](Three.InterleavedBuffer.html#usage)

***

### uuid

> `readonly` **uuid**: `string`

Defined in: three/src/core/InterleavedBuffer.js:83

The UUID of the interleaved buffer.

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`uuid`](Three.InterleavedBuffer.html#uuid)

***

### version

> **version**: `number`

Defined in: three/src/core/InterleavedBuffer.js:75

A version number, incremented every time the `needsUpdate` is set to `true`.

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`version`](Three.InterleavedBuffer.html#version)

## Accessors

### needsUpdate

#### Set Signature

> **set** **needsUpdate**(`value`): `void`

Defined in: three/src/core/InterleavedBuffer.js:101

Flag to indicate that this attribute has changed and should be re-sent to
the GPU. Set this to `true` when you modify the value of the array.

##### Default

```ts
false
```

##### Parameters

###### value

`boolean`

##### Returns

`void`

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`needsUpdate`](Three.InterleavedBuffer.html#needsupdate)

## Methods

### addUpdateRange()

> **addUpdateRange**(`start`, `count`): `void`

Defined in: three/src/core/InterleavedBuffer.js:127

Adds a range of data in the data array to be updated on the GPU.

#### Parameters

##### start

`number`

Position at which to start update.

##### count

`number`

The number of components to update.

#### Returns

`void`

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`addUpdateRange`](Three.InterleavedBuffer.html#addupdaterange)

***

### clearUpdateRanges()

> **clearUpdateRanges**(): `void`

Defined in: three/src/core/InterleavedBuffer.js:136

Clears the update ranges.

#### Returns

`void`

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`clearUpdateRanges`](Three.InterleavedBuffer.html#clearupdateranges)

***

### clone()

> **clone**(`data`): [`InterleavedBuffer`](Three.InterleavedBuffer.html)

Defined in: three/src/core/InstancedInterleavedBuffer.js:51

Returns a new interleaved buffer with copied values from this instance.

#### Parameters

##### data

`any`

An object with shared array buffers that allows to retain shared structures.

#### Returns

[`InterleavedBuffer`](Three.InterleavedBuffer.html)

A clone of this instance.

#### Overrides

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`clone`](Three.InterleavedBuffer.html#clone)

***

### copy()

> **copy**(`source`): `InstancedInterleavedBuffer`

Defined in: three/src/core/InstancedInterleavedBuffer.js:41

Copies the values of the given interleaved buffer to this instance.

#### Parameters

##### source

`any`

The interleaved buffer to copy.

#### Returns

`InstancedInterleavedBuffer`

A reference to this instance.

#### Overrides

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`copy`](Three.InterleavedBuffer.html#copy)

***

### copyAt()

> **copyAt**(`index1`, `interleavedBuffer`, `index2`): [`InterleavedBuffer`](Three.InterleavedBuffer.html)

Defined in: three/src/core/InterleavedBuffer.js:169

Copies a vector from the given interleaved buffer to this one. The start
and destination position in the attribute buffers are represented by the
given indices.

#### Parameters

##### index1

`number`

The destination index into this interleaved buffer.

##### interleavedBuffer

[`InterleavedBuffer`](Three.InterleavedBuffer.html)

The interleaved buffer to copy from.

##### index2

`number`

The source index into the given interleaved buffer.

#### Returns

[`InterleavedBuffer`](Three.InterleavedBuffer.html)

A reference to this instance.

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`copyAt`](Three.InterleavedBuffer.html#copyat)

***

### onUpload()

> **onUpload**(`callback`): [`InterleavedBuffer`](Three.InterleavedBuffer.html)

Defined in: three/src/core/InterleavedBuffer.js:242

Sets the given callback function that is executed after the Renderer has transferred
the array data to the GPU. Can be used to perform clean-up operations after
the upload when data are not needed anymore on the CPU side.

#### Parameters

##### callback

`Function`

The `onUpload()` callback.

#### Returns

[`InterleavedBuffer`](Three.InterleavedBuffer.html)

A reference to this instance.

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`onUpload`](Three.InterleavedBuffer.html#onupload)

***

### onUploadCallback()

> **onUploadCallback**(): `void`

Defined in: three/src/core/InterleavedBuffer.js:91

A callback function that is executed after the renderer has transferred the attribute array
data to the GPU.

#### Returns

`void`

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`onUploadCallback`](Three.InterleavedBuffer.html#onuploadcallback)

***

### set()

> **set**(`value`, `offset`?): [`InterleavedBuffer`](Three.InterleavedBuffer.html)

Defined in: three/src/core/InterleavedBuffer.js:191

Sets the given array data in the interleaved buffer.

#### Parameters

##### value

`any`

The array data to set.

##### offset?

`number` = `0`

The offset in this interleaved buffer's array.

#### Returns

[`InterleavedBuffer`](Three.InterleavedBuffer.html)

A reference to this instance.

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`set`](Three.InterleavedBuffer.html#set)

***

### setUsage()

> **setUsage**(`value`): [`InterleavedBuffer`](Three.InterleavedBuffer.html)

Defined in: three/src/core/InterleavedBuffer.js:113

Sets the usage of this interleaved buffer.

#### Parameters

##### value

`any`

The usage to set.

#### Returns

[`InterleavedBuffer`](Three.InterleavedBuffer.html)

A reference to this interleaved buffer.

#### Inherited from

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`setUsage`](Three.InterleavedBuffer.html#setusage)

***

### toJSON()

> **toJSON**(`data`): `any`

Defined in: three/src/core/InstancedInterleavedBuffer.js:61

Serializes the interleaved buffer into JSON.

#### Parameters

##### data

`any`

An optional value holding meta information about the serialization.

#### Returns

`any`

A JSON object representing the serialized interleaved buffer.

#### Overrides

[`InterleavedBuffer`](Three.InterleavedBuffer.html).[`toJSON`](Three.InterleavedBuffer.html#tojson)
