---
editUrl: false
next: false
prev: false
title: "ParametricGeometry"
---

> **ParametricGeometry**\<\> = (`u`, `v`, `target`) => `any`

Defined in: three/addons/geometries/ParametricGeometry.js:171

## Type Parameters

## Parameters

### u

`number`

The `u` coordinate on the surface in the range `[0,1]`.

### v

`number`

The `v` coordinate on the surface in the range `[0,1]`.

### target

`Vector3`

The target vector that is used to store the method's result.

## Returns

`any`
