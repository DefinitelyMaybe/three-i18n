---
editUrl: false
next: false
prev: false
title: "Capsule"
---

Defined in: three/addons/math/Capsule.js:11

A capsule is essentially a cylinder with hemispherical caps at both ends.
It can be thought of as a swept sphere, where a sphere is moved along a line segment.

Capsules are often used as bounding volumes (next to AABBs and bounding spheres).

## Constructors

### Constructor

> **new Capsule**(`start`?, `end`?, `radius`?): `Capsule`

Defined in: three/addons/math/Capsule.js:20

Constructs a new capsule.

#### Parameters

##### start?

`Vector3` = `...`

The start vector.

##### end?

`Vector3` = `...`

The end vector.

##### radius?

`number` = `1`

The capsule's radius.

#### Returns

`Capsule`

## Properties

### end

> **end**: `Vector3`

Defined in: three/addons/math/Capsule.js:34

The end vector.

***

### radius

> **radius**: `number`

Defined in: three/addons/math/Capsule.js:42

The capsule's radius.

#### Default

```ts
1
```

***

### start

> **start**: `Vector3`

Defined in: three/addons/math/Capsule.js:27

The start vector.

## Methods

### clone()

> **clone**(): `Capsule`

Defined in: three/addons/math/Capsule.js:51

Returns a new capsule with copied values from this instance.

#### Returns

`Capsule`

A clone of this instance.

***

### copy()

> **copy**(`capsule`): `Capsule`

Defined in: three/addons/math/Capsule.js:82

Copies the values of the given capsule to this instance.

#### Parameters

##### capsule

`Capsule`

The capsule to copy.

#### Returns

`Capsule`

A reference to this capsule.

***

### getCenter()

> **getCenter**(`target`): `Vector3`

Defined in: three/addons/math/Capsule.js:98

Returns the center point of this capsule.

#### Parameters

##### target

`Vector3`

The target vector that is used to store the method's result.

#### Returns

`Vector3`

The center point.

***

### intersectsBox()

> **intersectsBox**(`box`): `boolean`

Defined in: three/addons/math/Capsule.js:125

Returns `true` if the given bounding box intersects with this capsule.

#### Parameters

##### box

`Box3`

The bounding box to test.

#### Returns

`boolean`

Whether the given bounding box intersects with this capsule.

***

### set()

> **set**(`start`, `end`, `radius`): `Capsule`

Defined in: three/addons/math/Capsule.js:66

Sets the capsule components to the given values.
Please note that this method only copies the values from the given objects.

#### Parameters

##### start

`Vector3`

The start vector.

##### end

`Vector3`

The end vector

##### radius

`number`

The capsule's radius.

#### Returns

`Capsule`

A reference to this capsule.

***

### translate()

> **translate**(`v`): `Capsule`

Defined in: three/addons/math/Capsule.js:110

Adds the given offset to this capsule, effectively moving it in 3D space.

#### Parameters

##### v

`Vector3`

The offset that should be used to translate the capsule.

#### Returns

`Capsule`

A reference to this capsule.
