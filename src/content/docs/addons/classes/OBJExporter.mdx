---
editUrl: false
next: false
prev: false
title: "OBJExporter"
---

Defined in: three/addons/exporters/OBJExporter.js:20

An exporter for OBJ.

`OBJExporter` is not able to export material data into MTL files so only geometry data are supported.

```js
const exporter = new OBJExporter();
const data = exporter.parse( scene );
```

## Constructors

### Constructor

> **new OBJExporter**(): `OBJExporter`

#### Returns

`OBJExporter`

## Methods

### parse()

> **parse**(`object`): `string`

Defined in: three/addons/exporters/OBJExporter.js:30

Parses the given 3D object and generates the OBJ output.

If the 3D object is composed of multiple children and geometry, they are merged into a single mesh in the file.

#### Parameters

##### object

`Object3D`

The 3D object to export.

#### Returns

`string`

The exported OBJ.
