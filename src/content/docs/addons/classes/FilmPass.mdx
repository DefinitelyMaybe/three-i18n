---
editUrl: false
next: false
prev: false
title: "FilmPass"
---

Defined in: three/addons/postprocessing/FilmPass.js:18

This pass can be used to create a film grain effect.

```js
const filmPass = new FilmPass();
composer.addPass( filmPass );
```

## Extends

- [`Pass`](Pass.html)

## Constructors

### Constructor

> **new FilmPass**(`intensity`?, `grayscale`?): `FilmPass`

Defined in: three/addons/postprocessing/FilmPass.js:26

Constructs a new film pass.

#### Parameters

##### intensity?

`number` = `0.5`

The grain intensity in the range `[0,1]` (0 = no effect, 1 = full effect).

##### grayscale?

`boolean` = `false`

Whether to apply a grayscale effect or not.

#### Returns

`FilmPass`

#### Overrides

[`Pass`](Pass.html).[`constructor`](Pass.html#constructor)

## Properties

### \_fsQuad

> **\_fsQuad**: [`FullScreenQuad`](FullScreenQuad.html)

Defined in: three/addons/postprocessing/FilmPass.js:63

***

### clear

> **clear**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:53

If set to `true`, the pass clears its buffer before rendering

#### Default

```ts
false
```

#### Inherited from

[`Pass`](Pass.html).[`clear`](Pass.html#clear)

***

### enabled

> **enabled**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:37

If set to `true`, the pass is processed by the composer.

#### Default

```ts
true
```

#### Inherited from

[`Pass`](Pass.html).[`enabled`](Pass.html#enabled)

***

### isPass

> `readonly` **isPass**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:29

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`Pass`](Pass.html).[`isPass`](Pass.html#ispass)

***

### material

> **material**: `ShaderMaterial`

Defined in: three/addons/postprocessing/FilmPass.js:49

The pass material.

***

### needsSwap

> **needsSwap**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:45

If set to `true`, the pass indicates to swap read and write buffer after rendering.

#### Default

```ts
true
```

#### Inherited from

[`Pass`](Pass.html).[`needsSwap`](Pass.html#needsswap)

***

### renderToScreen

> **renderToScreen**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:62

If set to `true`, the result of the pass is rendered to screen. The last pass in the composers
pass chain gets automatically rendered to screen, no matter how this property is configured.

#### Default

```ts
false
```

#### Inherited from

[`Pass`](Pass.html).[`renderToScreen`](Pass.html#rendertoscreen)

***

### uniforms

> **uniforms**: `any`

Defined in: three/addons/postprocessing/FilmPass.js:42

The pass uniforms. Use this object if you want to update the
`intensity` or `grayscale` values at runtime.
```js
pass.uniforms.intensity.value = 1;
pass.uniforms.grayscale.value = true;
```

## Methods

### dispose()

> **dispose**(): `void`

Defined in: three/addons/postprocessing/FilmPass.js:102

Frees the GPU-related resources allocated by this instance. Call this
method whenever the pass is no longer used in your app.

#### Returns

`void`

#### Overrides

[`Pass`](Pass.html).[`dispose`](Pass.html#dispose)

***

### render()

> **render**(`renderer`, `writeBuffer`, `readBuffer`, `deltaTime`): `void`

Defined in: three/addons/postprocessing/FilmPass.js:78

Performs the film pass.

#### Parameters

##### renderer

`WebGLRenderer`

The renderer.

##### writeBuffer

`WebGLRenderTarget`

The write buffer. This buffer is intended as the rendering
destination for the pass.

##### readBuffer

`WebGLRenderTarget`

The read buffer. The pass can access the result from the
previous pass from this buffer.

##### deltaTime

`number`

The delta time in seconds.

#### Returns

`void`

#### Overrides

[`Pass`](Pass.html).[`render`](Pass.html#render)

***

### setSize()

> `abstract` **setSize**(): `void`

Defined in: three/addons/postprocessing/Pass.js:73

Sets the size of the pass.

#### Returns

`void`

#### Inherited from

[`Pass`](Pass.html).[`setSize`](Pass.html#setsize)
