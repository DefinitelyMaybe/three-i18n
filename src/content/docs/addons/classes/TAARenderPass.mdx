---
editUrl: false
next: false
prev: false
title: "TAARenderPass"
---

Defined in: three/addons/postprocessing/TAARenderPass.js:24

Temporal Anti-Aliasing Render Pass.

When there is no motion in the scene, the TAA render pass accumulates jittered camera
samples across frames to create a high quality anti-aliased result.

Note: This effect uses no reprojection so it is no TRAA implementation.

```js
const taaRenderPass = new TAARenderPass( scene, camera );
taaRenderPass.unbiased = false;
composer.addPass( taaRenderPass );
```

## Extends

- [`SSAARenderPass`](SSAARenderPass.html)

## Constructors

### Constructor

> **new TAARenderPass**(`scene`, `camera`, `clearColor`?, `clearAlpha`?): `TAARenderPass`

Defined in: three/addons/postprocessing/TAARenderPass.js:34

Constructs a new TAA render pass.

#### Parameters

##### scene

`Scene`

The scene to render.

##### camera

`Camera`

The camera.

##### clearColor?

`any`

The clear color of the render pass.

##### clearAlpha?

`number`

The clear alpha of the render pass.

#### Returns

`TAARenderPass`

#### Overrides

[`SSAARenderPass`](SSAARenderPass.html).[`constructor`](SSAARenderPass.html#constructor)

## Properties

### \_copyMaterial

> **\_copyMaterial**: `any`

Defined in: three/addons/postprocessing/SSAARenderPass.js:105

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`_copyMaterial`](SSAARenderPass.html#_copymaterial)

***

### \_copyUniforms

> **\_copyUniforms**: `any`

Defined in: three/addons/postprocessing/SSAARenderPass.js:103

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`_copyUniforms`](SSAARenderPass.html#_copyuniforms)

***

### \_fsQuad

> **\_fsQuad**: [`FullScreenQuad`](FullScreenQuad.html)

Defined in: three/addons/postprocessing/SSAARenderPass.js:116

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`_fsQuad`](SSAARenderPass.html#_fsquad)

***

### \_holdRenderTarget

> **\_holdRenderTarget**: `any`

Defined in: three/addons/postprocessing/TAARenderPass.js:66

***

### \_oldClearColor

> **\_oldClearColor**: `any`

Defined in: three/addons/postprocessing/SSAARenderPass.js:101

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`_oldClearColor`](SSAARenderPass.html#_oldclearcolor)

***

### \_sampleRenderTarget

> **\_sampleRenderTarget**: `any`

Defined in: three/addons/postprocessing/TAARenderPass.js:65

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`_sampleRenderTarget`](SSAARenderPass.html#_samplerendertarget)

***

### accumulate

> **accumulate**: `boolean`

Defined in: three/addons/postprocessing/TAARenderPass.js:53

Whether to accumulate frames or not. This enables
the TAA.

#### Default

```ts
false
```

***

### accumulateIndex

> **accumulateIndex**: `number`

Defined in: three/addons/postprocessing/TAARenderPass.js:61

The accumulation index.

#### Default

```ts
-1
```

***

### camera

> **camera**: `Camera`

Defined in: three/addons/postprocessing/SSAARenderPass.js:51

The camera.

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`camera`](SSAARenderPass.html#camera)

***

### clear

> **clear**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:53

If set to `true`, the pass clears its buffer before rendering

#### Default

```ts
false
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`clear`](SSAARenderPass.html#clear)

***

### clearAlpha

> **clearAlpha**: `number`

Defined in: three/addons/postprocessing/SSAARenderPass.js:95

The clear alpha of the render pass.

#### Default

```ts
0
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`clearAlpha`](SSAARenderPass.html#clearalpha)

***

### clearColor

> **clearColor**: `any`

Defined in: three/addons/postprocessing/SSAARenderPass.js:87

The clear color of the render pass.

#### Default

```ts
0x000000
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`clearColor`](SSAARenderPass.html#clearcolor)

***

### enabled

> **enabled**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:37

If set to `true`, the pass is processed by the composer.

#### Default

```ts
true
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`enabled`](SSAARenderPass.html#enabled)

***

### isPass

> `readonly` **isPass**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:29

This flag can be used for type testing.

#### Default

```ts
true
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`isPass`](SSAARenderPass.html#ispass)

***

### needsSwap

> **needsSwap**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:45

If set to `true`, the pass indicates to swap read and write buffer after rendering.

#### Default

```ts
true
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`needsSwap`](SSAARenderPass.html#needsswap)

***

### renderToScreen

> **renderToScreen**: `boolean`

Defined in: three/addons/postprocessing/Pass.js:62

If set to `true`, the result of the pass is rendered to screen. The last pass in the composers
pass chain gets automatically rendered to screen, no matter how this property is configured.

#### Default

```ts
false
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`renderToScreen`](SSAARenderPass.html#rendertoscreen)

***

### sampleLevel

> **sampleLevel**: `number`

Defined in: three/addons/postprocessing/SSAARenderPass.js:60

The sample level. Specified as n, where the number of
samples is 2^n, so sampleLevel = 4, is 2^4 samples, 16.

#### Default

```ts
4
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`sampleLevel`](SSAARenderPass.html#samplelevel)

***

### scene

> **scene**: `Scene`

Defined in: three/addons/postprocessing/SSAARenderPass.js:44

The scene to render.

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`scene`](SSAARenderPass.html#scene)

***

### stencilBuffer

> **stencilBuffer**: `boolean`

Defined in: three/addons/postprocessing/SSAARenderPass.js:79

Whether to use a stencil buffer or not. This property can't
be changed after the first render.

#### Default

```ts
false
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`stencilBuffer`](SSAARenderPass.html#stencilbuffer)

***

### unbiased

> **unbiased**: `boolean`

Defined in: three/addons/postprocessing/SSAARenderPass.js:70

Whether the pass should be unbiased or not. This property has the most
visible effect when rendering to a RGBA8 buffer because it mitigates
rounding errors. By default RGBA16F is used.

#### Default

```ts
true
```

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`unbiased`](SSAARenderPass.html#unbiased)

## Methods

### dispose()

> **dispose**(): `void`

Defined in: three/addons/postprocessing/TAARenderPass.js:200

Frees the GPU-related resources allocated by this instance. Call this
method whenever the pass is no longer used in your app.

#### Returns

`void`

#### Overrides

[`SSAARenderPass`](SSAARenderPass.html).[`dispose`](SSAARenderPass.html#dispose)

***

### render()

> **render**(`renderer`, `writeBuffer`, `readBuffer`, `deltaTime`): `void`

Defined in: three/addons/postprocessing/TAARenderPass.js:81

Performs the TAA render pass.

#### Parameters

##### renderer

`WebGLRenderer`

The renderer.

##### writeBuffer

`WebGLRenderTarget`

The write buffer. This buffer is intended as the rendering
destination for the pass.

##### readBuffer

`WebGLRenderTarget`

The read buffer. The pass can access the result from the
previous pass from this buffer.

##### deltaTime

`number`

The delta time in seconds.

#### Returns

`void`

#### Overrides

[`SSAARenderPass`](SSAARenderPass.html).[`render`](SSAARenderPass.html#render)

***

### setSize()

> **setSize**(`width`, `height`): `void`

Defined in: three/addons/postprocessing/SSAARenderPass.js:145

Sets the size of the pass.

#### Parameters

##### width

`number`

The width to set.

##### height

`number`

The width to set.

#### Returns

`void`

#### Inherited from

[`SSAARenderPass`](SSAARenderPass.html).[`setSize`](SSAARenderPass.html#setsize)
